{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "298d544f4b809baec5c2ecfa99ec9cdc51006c28f5b73112789c538ff2374214",
  "bytes": 1257861,
  "reason": "#302: RautoMap's U2 MaZ block mirrors the game's row filter for filter — on !== false (not !on), the times==-2/tx repeat arm, the getMaxSettings bound, and the game's (!cell && nextCell==1) arm, all of which the un-edited factory row setZoneU2:[{world:10}] needs."
}
