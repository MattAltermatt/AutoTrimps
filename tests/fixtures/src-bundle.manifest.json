{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "4d516c62982a09cf7d9f4ff3ba8a803106db50b79f435b529a64dce4e430438c",
  "bytes": 1225678,
  "reason": "#151 Tier B (1/2): add src/modules/settings-migrations.ts and call migrateLegacyId() from createSetting immediately above the persisted read. Bundle delta is PURELY ADDITIVE — 24 lines added, ZERO removed (verified by multiset line diff vs the previous golden). The settings-engine.ts section marker now appears twice because esbuild emits that module's export map, then its new dependency, then its body; no existing code moved. Production migration table ships EMPTY, so this commit is behaviour-inert for every user."
}
