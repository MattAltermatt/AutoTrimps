{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "0ed77f1f3fef941805bc627bb3298174fbc017a6929284d687e024a67b122665",
  "bytes": 983054,
  "reason": "#108: adds the InvestSpareMetal setting (default off) + its disjunct in autoLevelEquipment's two level-buy guards; L0 baseline-zero is green, so the emit change is inert with the setting off"
}
