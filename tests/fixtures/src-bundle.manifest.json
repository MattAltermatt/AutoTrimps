{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "f6855e8a0e8a7a7ef688e97fa6d1a12820115f3d8a05578696db9262563d11f4",
  "bytes": 1253926,
  "reason": "Fix S6 review correction: AutoStructure's per-item purchase gate is setting[item].enabled (main.js:18264), not mere presence (main.js:18250, which only skips never-configured items). The first pass read only as far as :18250 and shipped a presence test, which reported 'both are buying' for a player who unchecked every building in the cog."
}
