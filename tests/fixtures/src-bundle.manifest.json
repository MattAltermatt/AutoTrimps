{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "6c35e5c2eec862ba3c29fe80f6ec1ad59bd9c8ca1885371ee78e63cd66f7a9df",
  "bytes": 1256045,
  "reason": "S9 Wave 3 (#221 #206 #185 #223): the MaZ mirror now walks getSetZone() with the game's own on/through/times filters; C2 Runner's challengeSquaredMode writes reach globalThis instead of a module-private var (the var is gone, so esbuild no longer emits it); needToTrap measures against realMax(); the finishExpOnBw -1 sentinel is tested before the 125 clamp. Reviewed diff: 55 changed lines, all four fixes and the finishExpOnBw tooltip, nothing else."
}
