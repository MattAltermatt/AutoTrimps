{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "825362f812d802c657f85037cdc9c90bbada421239bbeb3ffcfc951261a87949",
  "bytes": 1256656,
  "reason": "Wave 4 (#291 #162) + the review follow-ups: query.ts's seven challengeActive compares now call the game's helper and Size's 1.5 carries the game's food/wood/metal guard; the five paired-cell guards go through utils.pairedCellGateOpen; c2runner gates each arm on c2Selectable so a locked C2 cannot throw inside doPortal; two comment corrections and the finishExpOnBw 'cannot' line now excepts -1."
}
