{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "c5e3f02a99b4a39c6f9df0e4d059abb672e4ea42bb70fa281c0d93736a542d7e",
  "bytes": 1089310,
  "reason": "#114: autoshrine() now honours Hdshrine/Rdshrine option 2 ('DAS: Normal') — on a Daily it reads the NORMAL shrine id-set instead of the Daily one. Previously the value was never consulted and options 1 and 2 were identical."
}
