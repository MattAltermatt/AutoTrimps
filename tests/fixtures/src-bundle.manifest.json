{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "ce023c2521da9f3c56f6ed2ed227651e478fa88ce4f58b81ae052cbde297e92e",
  "bytes": 979596,
  "reason": "#102: src/main.ts now calls seedModuleDefaults() as its last statement, and compareModuleVars() gains a totality guard (skip modules with no recorded default). Both change the emitted src IIFE."
}
