{
  "_comment": "Written by scripts/regen-src-golden.mjs (#91). `sha256` must match tests/fixtures/src-bundle.golden.js — tests/src-bundle-parity.test.ts enforces it, so a hand-edited golden, or one regenerated by any means that skips the reason prompt, fails the gate.",
  "sha256": "c8fbb44f826724d456bc313d0fc8ffe957e8c4671481989a7ddda2ad72cdefa7",
  "bytes": 1229151,
  "reason": "#256: add the canonical '// falls through' marker to portal.ts's documented empty-case group so oxlint's newly-activated no-fallthrough rule accepts it — a comment-only emit change, no behaviour"
}
