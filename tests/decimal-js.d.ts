// decimal.js v7.2.4 ships no bundled type declarations; the tests only need the runtime value
// (assigned to globalThis.Decimal to back breedtimer's DecimalBreed clone). Declare it as `any`.
declare module 'decimal.js'
