[
 {
  "idx": 0,
  "sev": "HIGH",
  "file": "src/modules/ab.ts",
  "line": 111,
  "klass": "gate",
  "numeric": false,
  "title": "ABdustsimple crashes on an empty array when no SA item is equipped, killing the rest of mainLoop every tick",
  "instrument": "vitest test that boots the REAL Trimps clone (v5.10.1) into jsdom via the sim harness (`scripts/sim/boot.mjs`), then imports the REAL `src/modules/ab.ts` and calls `ABdustsimple()` / `ABswitch()` against the game's actual `autoBattle` object.\n\nWhat it CAN observe: the genuine `autoBattle.items` table, and the game's own `equip()`, `loadPreset()`, `savePreset()`, `resetAll()`, `upgradeCost()`, `upg",
  "evidence": "All 6 assertions green (crash paths asserted via `.toThrow(TypeError)`):\n\n  \u2713 POSITIVE CONTROL: with an item equipped, ABdustsimple runs and really upgrades it\n  \u2713 PATH A (player): equip() has a MAX guard but NO MINIMUM -- unequip all -> throws\n  \u2713 PATH B: loadPreset on an UNSAVED (empty) preset zeroes the loadout -> throws\n  \u2713 PATH B2 (end-to-end): AT's OWN ABswitch() picks the unsaved preset and zeroes the loadout\n  \u2713 SIBLING (ab.ts:128): ABdustsimplenonhid throws when every item is equipped-or-hidden\n  \u2713 REFUTES finder's cited path: resetAll() does NOT leave zero equipped\n  Test Files 1 passed (1) | Tests 6 passed (6)\n\nTHE THROW (bare semantics, node -e):\n    var equips = []; if (100 >= e",
  "fix": "Pure MECHANISM fix \u2014 no game-balance number is touched anywhere. Two one-line empty guards:\n\n1. src/modules/ab.ts:111 (ABdustsimple) \u2014 before dereferencing `equips[0]`:\n       equips.sort(function(a, b) { return a[1] - b[1]; });\n   +   if (equips.length === 0) return;\n       if (autoBattle.dust >= e",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 1,
  "sev": "HIGH",
  "file": "src/modules/ab.ts",
  "line": 472,
  "klass": "gate",
  "numeric": false,
  "title": "ABsolver re-equips (and therefore resetCombat's) every tick forever whenever a target item is not owned -- SA never kills anything",
  "instrument": "Vitest (jsdom) unit repro that imports the REAL `ABsolver` from src/modules/ab.ts and drives it against the REAL `autoBattle` object literal loaded straight out of the game clone (/Users/matt/dev/MattAltermatt/trimps-game/objects.js, executed in a node:vm sandbox) \u2014 so item ownership defaults and `equip()` semantics are the game's own, not a hand-written stub. It CAN observe: how many times ABsolv",
  "evidence": "Real-object facts read out of trimps-game/objects.js at load: `autoBattle.autoLevel === true` by default; owned-by-default items = [Menacing_Mask, Sword, Armor, Rusty_Dagger, Fists_of_Goo, Battery_Stick, Pants]; `items.Labcoat = {\"owned\":false,...,\"zone\":90}` (likewise Putrid_Pouch z78, Chemistry_Set z81). objects.js:3573-3586 `equip:` toggles `itemObj.equipped` and then calls `this.resetCombat(true)`; objects.js:3200-3216 `resetCombat` rebuilds trimp+enemy templates and sets `this.battleTime = 0`.\n\nRepro run (maxEnemyLevel = 7, Labcoat/Chemistry_Set/Putrid_Pouch NOT owned, getMaxItems deliberately raised to 6 so slot limits cannot be blamed). After one settling tick, 3 further ticks with NO",
  "fix": "Mechanism-only; touches NO game-balance numbers (the `level = [...]` target arrays and item lists stay exactly as they are).\n\nMake the equip block idempotent by computing the desired set over items the player actually OWNS, and by comparing it against what is currently equipped (src/modules/ab.ts:47",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 2,
  "sev": "HIGH",
  "file": "src/modules/breedtimer.ts",
  "line": 78,
  "klass": "parity",
  "numeric": true,
  "title": "potencyMod() omits the U2 GeneAttack/GeneHealth \u00f750 breed-speed penalty \u2014 breed prediction is 50\u00d7\u20132500\u00d7 too fast in U2",
  "instrument": "Three instruments, all agreeing:\n\n(c) READ THE GAME \u2014 quoted the authoritative formula from ../trimps-game (v5.10.1) `main.js` breed(), plus the mutation definitions in `objects.js` and the `u2Mutations` gating. This can see the exact chain position of the missing terms.\n\n(b) FAILING VITEST \u2014 one file `tests/_repro/f2.test.ts` (jsdom, modeled on the existing `tests/breedtimer.characterization.test",
  "evidence": "=== 1. THE GAME (authoritative) \u2014 ../trimps-game/main.js, breed() ===\nmain.js:5605-5606 (modifier collection):\n    mutGeneAttack: game.global.universe === 2 && u2Mutations.tree.GeneAttack.purchased,\n    mutGeneHealth: game.global.universe === 2 && u2Mutations.tree.GeneHealth.purchased\nmain.js:5631-5637 (the chain):\n    if (potencyModifiers.voidBreed) potencyMod *= 0.2;\n    potencyMod = calcHeirloomBonus('Shield', 'breedSpeed', potencyMod);\n    if (potencyModifiers.mutGeneAttack) potencyMod /= 50;      // <-- MISSING IN FORK\n    if (potencyModifiers.mutGeneHealth) potencyMod /= 50;      // <-- MISSING IN FORK\n    if (potencyModifiers.genes > 0) potencyMod = DecimalBreed(potencyMod).mul(Math.p",
  "fix": "MECHANISM fix \u2014 mirror two missing conditionals from the game. NOTE: the finding is flagged numeric=true, and `50` IS a game-balance constant \u2014 but I am NOT proposing to CHANGE a balance number. I am proposing to COPY the game's own constant into a mirror that currently omits it, which is the standa",
  "skepticVotes": "2/3 REAL"
 },
 {
  "idx": 3,
  "sev": "HIGH",
  "file": "src/modules/buildings.ts",
  "line": 653,
  "klass": "gate",
  "numeric": false,
  "title": "U2 housing/Microchip/storage automation dies whenever the player's game buy-amount selector is not 1 or Max (bare canAffordBuilding reads ambient game",
  "instrument": "Real-game jsdom boot (scripts/sim/boot.mjs `bootGame({withAutoTrimps:true, saveString: tests/fixtures/saves/04-u2-radon.txt})`) \u2014 the actual Trimps v5.10.1 clone (config/updates/objects/main.js) plus the real built `dist/autotrimps.user.js`. So `canAffordBuilding`, `getBuildingItemPrice`, `buyBuilding`, `mostEfficientHousing` and `RbuyBuildings` are all the REAL functions, not stubs. This instrume",
  "evidence": "CONTROLLED EXPERIMENT \u2014 fresh boot per row, IDENTICAL starting state (food=wood=30020, same save), the ONLY variable is `game.global.buyAmt` (the player's 1/10/25/100/Max UI button, persisted in the save because `save()` at main.js:57 does `JSON.stringify(game)` and buyAmt lives at `game.global.buyAmt`, config.js:77):\n\n    buyAmt=1    -> RbuyBuildings() bought: [\"House\",\"House\"]\n    buyAmt=10   -> RbuyBuildings() bought: []\n    buyAmt=25   -> RbuyBuildings() bought: []\n    buyAmt=100  -> RbuyBuildings() bought: []\n    buyAmt=Max  -> RbuyBuildings() bought: [\"House\",\"House\"]\n\nThe automation buys nothing at all on 10 / 25 / 100. Same resources, same buildings, same settings.\n\nWHY \u2014 the gate it",
  "fix": "Pure mechanism fix \u2014 NO game-balance number is touched. The `1` involved is not a tuning constant: it is the arity the adjacent `buyBuilding(x, true, true, 1)` call ALREADY hardcodes; the gate is simply asking about a different quantity than the purchase it guards. Restoring agreement between gate a",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 6,
  "sev": "HIGH",
  "file": "src/modules/calc.ts",
  "line": 926,
  "klass": "other",
  "numeric": false,
  "title": "calcCurrentStance measures damage-ALREADY-DEALT, not remaining enemy health \u2014 Windstacking is inverted",
  "instrument": "Two instruments. (1) Vitest L1 unit repro (`tests/_repro/f6.test.ts`, jsdom env) calling the REAL `calcCurrentStance()` from `src/modules/calc.ts` against a hydrated `u1CombatGame`-style fixture (copied from tests/calc.characterization.test.ts), with `getCurrentEnemy()` as the ONLY variable \u2014 it stubs `{maxHealth, health}` and reads back the returned stance code. This instrument can see exactly th",
  "evidence": "GAME SEMANTICS (authoritative, ../trimps-game/main.js):\n  11596:  cell.health *= healthMult;\n  11597:  cell.maxHealth = cell.health;        // at SPAWN: maxHealth := full health\n  15959:  cell.health -= trimpAttack;          // health DECREASES as we damage it\n  12201:  var percent = ((cell.health / cell.maxHealth) * 100);   // health = REMAINING\n\u21d2 `maxHealth - health` == damage ALREADY DEALT (0 on a fresh enemy), NOT remaining health.\n\nAT consumes it as hits-to-kill (src/modules/calc.ts):\n  926:  ehealth = (getCurrentEnemy().maxHealth - getCurrentEnemy().health);\n  944:  const hitslow  = (ehealth / attacklow);\n  945:  const hitshigh = (ehealth / attackhigh);\n\nVITEST RUN (npx vitest run test",
  "fix": "One-line mechanism fix in src/modules/calc.ts:926 \u2014 replace the damage-dealt expression with remaining health:\n\n    if (game.global.fighting) {\n        ehealth = getCurrentEnemy().health;      // remaining health, per main.js:12201\n    }\n\nDownstream `hitslow = ehealth / attacklow` / `hitshigh = ehea",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 7,
  "sev": "HIGH",
  "file": "src/modules/calc.ts",
  "line": 171,
  "klass": "brace-scope",
  "numeric": false,
  "title": "getCritMulti operator precedence: `high && A || B` \u2014 the Daily branch ignores the `high` argument entirely",
  "instrument": "Two instruments. (1) `node -e` on the bare expression \u2014 proves the precedence semantics directly. (2) A vitest unit repro (jsdom env) calling the real exported `getCritMulti` from src/modules/calc.ts against the existing `makeMinimalGame` harness (the same fixture tests/calc.test.ts's getCritMulti golden uses). This instrument CAN see calc.ts's pure prediction math directly \u2014 no sim, no L0 trace, ",
  "evidence": "1) Precedence, proven:\n   $ node -e \"const high=false,A=true,B=true;console.log('high&&A||B =',high&&A||B,'| high&&(A||B) =',high&&(A||B))\"\n   high&&A||B = true | high&&(A||B) = false\n   \u2192 with high=false the code STILL enters the branch whenever B is true.\n\n2) Behavioral repro against the real function (vitest):\n   FAIL tests/_repro/f7.test.ts > getCritMulti(false) must use the EQUIPPED shield crit\n   AssertionError: expected 1 to be 6 // Object.is equality\n     - Expected  6\n     + Received  1\n   PASS control: with use3daily OFF the same call is correct  \u2190 positive control: same fixture, only `use3daily` flipped to false, assertion passes. So the harness reaches the branch and 6 is the rig",
  "fix": "Mechanism-only, no numbers touched. calc.ts:169-173 \u2014 parenthesize the disjunction so `high` gates both arms:\n\n    if (\n        high && (\n            (getPageSetting('AutoStance') == 3 && getPageSetting('highdmg') != undefined && game.global.challengeActive !== \"Daily\") ||\n            (getPageSettin",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 8,
  "sev": "HIGH",
  "file": "src/modules/calc.ts",
  "line": 1255,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "RcalcOurDmg adds/subtracts three Daily multipliers instead of multiplying \u2014 U2 daily damage prediction silently ignores them",
  "instrument": "Vitest jsdom unit harness (tests/_repro/f8.test.ts, now deleted) calling the REAL RcalcOurDmg exported from src/modules/calc.ts, with a `game` fixture copied verbatim from the existing `u2CombatGame()` in tests/calc.characterization.test.ts and `dailyModifiers.getMult` stubs transcribed verbatim from the game's own formulas in ../trimps-game/main.js (14632 / 14645 / 14784).\n\nWHAT IT CAN SEE: the e",
  "evidence": "$ npx vitest run tests/_repro/f8.test.ts\n\n \u2713 BASELINE (instrument is live): neutral U2 avg damage = 60\n \u00d7 oddTrimpNerf str=40 on an ODD zone must MULTIPLY by 0.2 -> 12\n \u00d7 evenTrimpBuff str=5 on an EVEN zone must MULTIPLY by 2.0 -> 120\n \u00d7 rampage str=50 stacks=20 must MULTIPLY by 2.0 -> 120\n\nAssertionError: expected 60.2 to be close to 12, received difference is 48.2, but expected 5e-7\nAssertionError: expected 58 to be close to 120, received difference is 62, but expected 5e-7\nAssertionError: expected 58 to be close to 120, received difference is 62, but expected 5e-7\n\n Test Files  1 failed (1)\n      Tests  3 failed | 1 passed (4)\n\nREAD THE RETURNED NUMBERS \u2014 they are the smoking gun. Neutral",
  "fix": "OPERATOR-ONLY FIX. Touches NO numeric literal and introduces NO balance constant \u2014 it changes `+=`/`-=` to `*=` so the three Daily MULTIPLIERS are applied the way the game applies them. This is NOT a game-balance-number edit and does not require the sacrosanct-tuning gate.\n\nIn src/modules/calc.ts, R",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 9,
  "sev": "HIGH",
  "file": "src/modules/coordinator.ts",
  "line": 76,
  "klass": "gate",
  "numeric": false,
  "title": "computeTopTarget force-buys Warpstations behind the user's back \u2014 bypasses WarpstationCap/Gigastation cap, WarpstationWall3, the WarpstationCoordBuy t",
  "instrument": "vitest (jsdom) integration repro at tests/_repro/f9.test.ts, wiring the REAL src/modules/buildings.ts (buyGemEfficientHousing + safeBuyBuilding) and the REAL src/modules/coordinator.ts (computeTopTarget) against a shared game fixture, with the REAL getPageSetting/autoTrimpSettings and the native mutator `buyBuilding` spied. safeBuyBuilding is exposed on globalThis exactly as legacy-bridge.ts does ",
  "evidence": "Fixture = the normal Warpstation era: WarpstationCap ON, FirstGigastation 20, DeltaGigastation 2, Gigastation.done 0 \u2192 cap = floor(0*2)+20 = 20, Warpstation.owned = 30 (10 past the cap); WarpstationCoordBuy explicitly OFF; Coordination allowed 5 / done 2 with canAffordCoordinationTrimps() false; metal plentiful; PurchaseCoordinator ON.\n\nPASS (positive control / legacy path):\n  \u2713 LEGACY path (buyGemEfficientHousing) correctly REFUSES to buy past the Gigastation cap\n    buyCalls === []  and  Warpstation.owned stays 30\n\nFAIL (the bug), 3/3:\n  \u00d7 COORDINATOR path buys a Warpstation ANYWAY \u2014 past the cap, with WarpstationCoordBuy OFF\n    AssertionError: expected [ 'Warpstation' ] to deeply equal [",
  "fix": "Mechanism-only; NO game-balance numbers are touched (the fix ADDS back existing user settings/guards, it changes no literal).\n\n1. Extract the legacy coord-warp decision out of buildings.ts:181-210 (inside buyGemEfficientHousing) into one shared exported helper, e.g. `warpCoordBuyAllowed(): boolean`,",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 10,
  "sev": "HIGH",
  "file": "src/modules/dynprestige.ts",
  "line": 9,
  "klass": "never-reset",
  "numeric": false,
  "title": "Dynamic Prestige clobbers its own input: after one guiLoop tick the target dropdown collapses to \"Dagadder\" and the feature is a permanent no-op (and ",
  "instrument": "vitest + jsdom unit repro (`tests/_repro/f10.test.ts`, now deleted) calling the real `prestigeChanging2()` from src/modules/dynprestige.ts against a real jsdom `#Prestige` <select> built from the 14-entry list in settings-defs.ts:337, a makeMinimalGame() fixture, and the real `getPageSetting` (via an injected autoTrimpSettings). Between the two calls I replay the ONE line the finding names as the ",
  "evidence": "Two tests, one of which is the positive control.\n\nPOSITIVE CONTROL (PASSED) \u2014 without the write-back, the escalation branch is live and reachable:\n  select.selectedIndex = 11 ('Bestplate'), DynamicPrestige2 = 200, all mapUnlocks[*].last = 1, sLevel 0\n  world=1   \u2192 prestigeChanging2() \u2192 autoTrimpSettings.Prestige.selected === 'Dagadder'   \u2713 (dagger-only, correct)\n             and select.selectedIndex is still 11 (the algorithm never touches the DOM)\n  world=199 \u2192 prestigeChanging2() \u2192 selected === 'Bestplate'                              \u2713 (escalates as designed)\n  (d = 11 prestiges \u00d7 floor((200-1)/5)=39 = 429; h = ceil(429/11) = 39; 199 > 200-39=161 \u2192 escalate.)\n\nBUG (FAILED against current ",
  "fix": "Mechanism-only; NO game-balance number is touched (the 5-per-prestige / target-zone math is untouched).\n\nThe defect is that the user's TARGET and the algorithm's OUTPUT share one storage cell (`autoTrimpSettings.Prestige` / the #Prestige <select>), so the output overwrites the input. Give the target",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 11,
  "sev": "MEDIUM",
  "file": "src/modules/gather.ts",
  "line": 279,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Quest challenge: the whole gather else-if chain is skipped whenever the current quest isn't an active food/wood/metal/science quest \u2014 no setGather at ",
  "instrument": "vitest + jsdom, driving the REAL `RmanualLabor2` export from src/modules/gather.ts (tests/_repro/f11.test.ts), modeled on tests/gather.characterization.test.ts. Instrument = a spy on the global `setGather` (+ `safeBuyBuilding`), which is exactly the module's actuator seam: `gather.ts` is the ONLY caller of `setGather` in the whole fork (`grep -rn setGather src/` \u2192 the sole non-gather.ts hit is the",
  "evidence": "CONFIRMED. Final run: 8 failed | 2 passed.\n\n  \u2713 CONTROL: challengeActive=\"\"      -> setGather(\"trimps\")   [ladder runs]\n  \u2713 SANITY:  Quest + questcheck()==12 -> setGather(\"metal\")    [instrument sees inside the Quest block]\n  \u00d7 Quest + questcheck()===0  -> AssertionError: expected [] to not deeply equal []\n  \u00d7 Quest + questcheck()===3  -> []\n  \u00d7 Quest + questcheck()===4  -> []\n  \u00d7 Quest + questcheck()===5  -> []\n  \u00d7 Quest + questcheck()===6  -> []\n  \u00d7 Quest + questcheck()===7  -> []\n  \u00d7 Quest + questcheck()===13 -> []\n  \u00d7 Quest + questcheck()===23 -> []\n\nCONTROL and the 8 failures use a BYTE-IDENTICAL armed game state (trimps 10/100 so needToTrap is true, Trap.owned=100, RTrapTrimps on). The",
  "fix": "Pure MECHANISM fix \u2014 no game-balance number is touched (no damage/cost/rate/multiplier literal is read or written; the only literals involved are questcheck()'s existing opaque dispatch codes).\n\nThe bug is structural: the Quest arm's four inner tests are bare `if`s with no `else`, so entering the ar",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 12,
  "sev": "HIGH",
  "file": "src/modules/heirlooms.ts",
  "line": 600,
  "klass": "type-confusion",
  "numeric": false,
  "title": "gammaBurstPct sentinel is inverted (`: 1` instead of `: 0`) \u2014 AT computes 40% of its real damage for every player WITHOUT a Gamma Burst shield",
  "instrument": "Two instruments. (1) Direct expression evaluation of the sentinel ternary (pure semantics \u2014 can see exactly what value `gammaBurstPct` takes). (2) A vitest L1 characterization repro (`tests/_repro/f12.test.ts`, jsdom) importing the REAL `calcOurDmg` / `calcHDratio` / `calcOurHealth` / `calcBadGuyDmg` from `src/modules/calc.ts`, driven by the same `neutralCombat()` + `u1CombatGame()` fixtures as `t",
  "evidence": "REPRODUCED. Vitest output:\n\n  FAIL tests/_repro/f12.test.ts > STEP 2 \u2014 tanky trimps: calcOurDmg is cut to 40% of the true value\n  AssertionError: expected 24 to be 60 // Object.is equality\n  stdout: { ratio: Infinity, correct: 60, shipped: 24, factor: 0.4 }\n\n  FAIL tests/_repro/f12.test.ts > STEP 3 \u2014 calcHDratio inflates 2.5\u00d7; Burstier owned gives 50%\n  AssertionError: expected 1.2001457515142877e+29 to be 4.80058300605715e+28\n  stdout: { hdCorrect: 4.80058300605715e+28, hdShipped: 1.2001457515142877e+29, inflation: 2.5000000000000004 }\n  stdout: { burstierCorrect: 60, burstierShipped: 30, factor: 0.5 }\n\n  Tests: 2 failed | 1 passed (3)   \u2190 STEP 1 (sentinel === 1) passes, i.e. the inverted s",
  "fix": "Mechanism fix, NOT a balance number. Change the sentinel from `: 1` to `: 0` in BOTH writers, so that \"no Gamma Burst\" means gammaBurstPct === 0 and the `gammaBurstPct > 0` guard at calc.ts:360/365 (and 1281/1286) reads as \"has Gamma Burst\" as intended:\n\n  src/modules/heirlooms.ts:600  and  legacy/A",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 13,
  "sev": "HIGH",
  "file": "src/modules/import-export.ts",
  "line": 144,
  "klass": "other",
  "numeric": false,
  "title": "\"Delete Profile\" with a default option selected deletes an unrelated saved profile and corrupts the dropdown",
  "instrument": "jsdom vitest repro (tests/_repro/f13.test.ts, now deleted) that imports the REAL src/modules/import-export.ts. The module self-executes settingsProfileMakeGUI() + initializeSettingsProfiles() at load, so the dropdown under test is the production one, built by production code; localStorage is a real key/value store. No `game` snapshot is injected (this path only reads game.options.menu.*, the DOM a",
  "evidence": "Test 1 (passes) confirms the production dropdown really is: customProfileCurrent:Current | customProfileDefault:Reset to Default | customProfileNew:Save New... | customProfileRead:profA | customProfileRead:profB, with selectedIndex === 0 (set at import-export.ts:57).\n\nTest 2 (FAILS = the bug) \u2014 calling onDeleteProfile() with \"Current\" (index 0) selected:\n  OPTIONS AFTER DELETE: customProfileDefault:Reset to Default | customProfileNew:Save New... | customProfileRead:profA | customProfileRead:profB\n  STORED AFTER DELETE: [{\"name\":\"profB\",\"data\":{}}]\n  AssertionError: expected [ 'profB' ] to deeply equal [ 'profA', 'profB' ]\n\nSo a single click of \"<Delete Profile\" while the harmless-looking \"Cu",
  "fix": "Pure mechanism \u2014 no game-balance numbers touched. In onDeleteProfile() (src/modules/import-export.ts:144): (1) bail out unless the selected option is an actual profile \u2014 `const opt = $settingsProfiles.options[index]; if (!opt || opt.id !== 'customProfileRead') { ImportExportTooltip('message', 'Selec",
  "skepticVotes": "2/3 REAL"
 },
 {
  "idx": 14,
  "sev": "HIGH",
  "file": "src/modules/import-export.ts",
  "line": 891,
  "klass": "other",
  "numeric": false,
  "title": "\"Cleanup Saved Settings\" strips the ATversion key, so the next page reload silently resets ALL 571 settings to defaults",
  "instrument": "vitest + jsdom (tests/_repro/f14.test.ts), importing the REAL `cleanupAutoTrimps` (src/modules/import-export.ts:891) and the REAL `saveSettings` / `loadPageVariables` (src/modules/utils.ts) and driving the actual localStorage round-trip.\n\nWhat this instrument CAN see: the whole bug lives in plain JS object/DOM/localStorage semantics \u2014 `autoTrimpSettings` key deletion, `document.getElementById`, `s",
  "evidence": "$ npx vitest run tests/_repro/f14.test.ts\n\n \u276f tests/_repro/f14.test.ts (4 tests | 2 failed) 41ms\n     \u00d7 BUG: cleanup also deletes ATversion 3ms\n     \u00d7 BUG (end-to-end): after cleanup + any saveSettings + reload, ALL settings are lost 1ms\n\n FAIL  #14 > BUG: cleanup also deletes ATversion\nAssertionError: expected undefined to be 'v9.9.9-test' // Object.is equality\n- Expected: \"v9.9.9-test\"\n+ Received: undefined\n \u276f tests/_repro/f14.test.ts:62:44\n     61|     cleanupAutoTrimps()\n     62|     expect(autoTrimpSettings['ATversion']).toBe('v9.9.9-test')\n\n FAIL  #14 > BUG (end-to-end): after cleanup + any saveSettings + reload, ALL settings are lost\nAssertionError: expected undefined to be defined\n \u276f",
  "fix": "Mechanism fix, no game-balance numbers involved (this is settings persistence, not tuning).\n\nPrimary \u2014 make cleanupAutoTrimps only consider real setting objects, so bare non-setting values (ATversion, and any other bare key such as analyticsID) are never candidates for deletion. Replace src/modules/",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 15,
  "sev": "HIGH",
  "file": "src/modules/magmite.ts",
  "line": 85,
  "klass": "never-reset",
  "numeric": false,
  "title": "ratiospend with default ratios: miRatio() returns undefined \u2192 uncaught TypeError kills doPortal + the rest of every mainLoop tick",
  "instrument": "Vitest (jsdom env) L1 characterization harness, modeled on tests/magmite.characterization.test.ts: real `magmite.ts` module import, real `getPageSetting` reading a seeded global `autoTrimpSettings` (never mocked), a hand-built `game` whose `generatorUpgrades[*].cost` are LIVE FUNCTIONS (tripwire asserted: `typeof game.generatorUpgrades.Efficiency.cost === 'function'`, plus `getPageSetting('effrati",
  "evidence": "Run: `npx vitest run tests/_repro/f15.test.ts` \u2192 3 passed, 1 failed (the failure IS the bug).\n\nFAIL  f15 \u2014 ratiospend ON with factory-default ratios > autoMagmiteSpender() throws a TypeError\nAssertionError: expected [Function] to not throw an error but 'TypeError: Cannot read properties of undefined (reading 'cost')' was thrown\n  + Received: \"TypeError: Cannot read properties of undefined (reading 'cost')\"\n\nPASS  miRatio() falls off the end and returns undefined            (expect(magmite.miRatio()).toBeUndefined())\nPASS  TRIPWIRE: fixture is live \u2014 generatorUpgrades.Efficiency.cost is a function; getPageSetting('effratio') === -1; getPageSetting('ratiospend') === true; calcMiSpent('Efficien",
  "fix": "Pure mechanism, touches NO balance number (no ratio/cost/priceIncrease literal changes).\n\nmagmite.ts:84-85 \u2014 make the \"nothing to spend on\" case a no-op instead of a crash:\n    const tospend = miRatio();\n    if (!tospend) return;                      // ratios[] empty \u2192 nothing selected \u2192 do nothing",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 16,
  "sev": "HIGH",
  "file": "src/modules/magmite.ts",
  "line": 84,
  "klass": "gate",
  "numeric": false,
  "title": "ratiospend path is missing the Hybridization+Storage Overclocker guard \u2192 permanent magmite-spend deadlock",
  "instrument": "vitest (jsdom) L1 actuator spy-log harness, modelled on tests/magmite.characterization.test.ts: real `src/modules/magmite.ts` import, real `getPageSetting` reading a seeded `autoTrimpSettings`, real generator cost() formulas copied from trimps-game/config.js:6846-6914, and `buyGeneratorUpgrade` implemented as a FAITHFUL re-impl of trimps-game/main.js:9715-9729 \u2014 including the game's own refusal gu",
  "evidence": "GAME SOURCE (trimps-game/main.js:9715-9717), the authoritative refusal:\n  function buyGeneratorUpgrade(item){\n    if (item == \"Overclocker\" && (!game.permanentGeneratorUpgrades.Hybridization.owned || !game.permanentGeneratorUpgrades.Storage.owned))\n      return;\n\nREPRO RUN (`npx vitest run tests/_repro/f16.test.ts`), ratiospend on, all four ratios = 25, Hybridization/Storage owned:false, 1,000,000 magmite, 40 ticks:\n  \u2713 expect(picks.slice(-30)).toEqual(Array(30).fill('Overclocker'))   PASSED \u2014 the last 30 of 40 ticks all selected Overclocker\n  \u2713 expect(bought.length).toBeLessThan(10)                            PASSED\n  \u2713 expect(game.global.magmite).toBeGreaterThan(999_000)              PASSE",
  "fix": "Mechanism-only; changes NO numeric balance value. The picker must not be allowed to nominate an upgrade the game will refuse. Reuse the existing `-1` exclusion sentinel in miRatio() so Overclocker simply drops out of the candidate set when it is unbuyable:\n\n  in miRatio(), after computing ocfinal:\n ",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 17,
  "sev": "MEDIUM",
  "file": "src/modules/settings-engine.ts",
  "line": 240,
  "klass": "never-written",
  "numeric": false,
  "title": "magmiteSpenderChanged is never written true \u2014 the 5-second 'Spend Magmite Always' safety debounce is dead, so cycling the toggle instantly dumps the w",
  "instrument": "vitest + jsdom, driving the REAL `settingChanged()` from `src/modules/settings-engine.ts` (imported directly, not re-implemented), with the bridge globals it resolves at runtime (`autoTrimpSettings`, `magmiteSpenderChanged`, `saveSettings`, `updateCustomButtons`, `checkPortalSettings`) stood up the way `AutoTrimps2.js` does.\n\nWhat this instrument CAN see: `settingChanged` is a pure DOM+global-stat",
  "evidence": "Static fact first \u2014 the id in the guard does not exist as a setting. Across `src/` + `legacy/`, `AutoMagmiteSpender2` appears ONLY in (a) the two frozen default-settings JSON blobs (`utils.ts:51`, `utils.ts:54`) and (b) the dead comparison itself. The real control is `createSetting('spendmagmite', ['Spend Magmite OFF','Spend Magmite (Portal)','Spend Magmite Always'], \u2026, 'multitoggle', 1, null, 'Magma')` at `settings-defs.ts:740`. There is no `createSetting('AutoMagmiteSpender2', \u2026)` anywhere.\n\nThe id can never be the old name: `createSetting` wires the handler as `btn.setAttribute(\"onclick\", 'settingChanged(\"' + id + '\")')` (settings-engine.ts:72/150/188) \u2014 `settingChanged` is only ever invo",
  "fix": "One-word mechanism fix \u2014 rename the stale id in the comparison. `src/modules/settings-engine.ts:240`:\n\n    -  if (id == 'AutoMagmiteSpender2' && btn.value == 1) {\n    +  if (id == 'spendmagmite' && btn.value == 1) {\n\nThe `btn.value == 1` test is evaluated BEFORE the `btn.value++`, so it still means ",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 18,
  "sev": "MEDIUM",
  "file": "src/modules/mapfunctions.ts",
  "line": 1833,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "RmapRepeat's max-map-bonus stop-guard is INVERTED vs its U1 twin \u2014 U2 \"Max MapBonus After\" never repeats a map",
  "instrument": "A vitest unit repro (jsdom) that imports the REAL `RmapRepeat` from src/modules/mapfunctions.ts and calls it directly, with `repeatClicked` stubbed as the faithful pure toggle the game defines (../trimps-game/main.js:10983-10984 `if (!updateOnly) game.global.repeatMap = !game.global.repeatMap;`). The instrument observes exactly the mutator at issue: every `repeatClicked()` call and the resulting `",
  "evidence": "Two assertions, one passing (the tautology proof) and one FAILING against current code (the behavioral proof).\n\n1) TAUTOLOGY \u2014 passed. Driving mapBonus 0..12 through the exact recompute at maps.ts:1103\n   RdoMaxMapBonus = (maxMapBonusZ >= 0 && mapBonus < RMaxMapBonuslimit && world >= maxMapBonusZ)\n   the line-1833 guard `RdoMaxMapBonus && mapBonus < RMaxMapBonuslimit` equals `RdoMaxMapBonus` for\n   EVERY value. The second clause carries zero information: `if (...)` degenerates to `if (RdoMaxMapBonus)`,\n   so the \"stop repeating, we hit the limit\" click fires on every tick the feature is ACTIVE and never\n   fires when the limit is actually reached.\n\n2) BEHAVIOR \u2014 failed, as designed. State: u",
  "fix": "MECHANISM fix, one comparison, at src/modules/mapfunctions.ts:1833 \u2014 mirror the U1 twin (maps.ts:669) exactly:\n\n  - if (RdoMaxMapBonus && game.global.mapBonus < getPageSetting('RMaxMapBonuslimit')) {\n  + if (RdoMaxMapBonus && game.global.mapBonus >= getPageSetting('RMaxMapBonuslimit') - 1) {\n       ",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 19,
  "sev": "HIGH",
  "file": "src/modules/other-praiding.ts",
  "line": 1290,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "PraidHarder's \"can't afford\" failure path never restores AutoMaps \u2014 map automation stays OFF for the rest of the run",
  "instrument": "Direct unit repro (vitest + jsdom) that CALLS the real `PraidHarder()` from src/modules/other-praiding.ts with a seeded `autoTrimpSettings` + a plain `game` fixture, real `getPageSetting`/`debug`/`byId` imports from utils.ts, and vi.fn spies for the native map mutators (mapsClicked/buyMap/selectMap/runMap/repeatClicked/recycleMap/updateMapCost/toggleSetting). What it CAN observe: every write to `a",
  "evidence": "$ npx vitest run tests/_repro/f19.test.ts\n  Tests  2 failed | 2 passed (4)\n\nPASS  POSITIVE CONTROL: the harness DOES observe the restore on the frag-farm path\n      (farmFragments=true, fragments=0 \u2192 hits the `else` branch at other-praiding.ts:1295-1298,\n       which writes autoTrimpSettings['AutoMaps'].value = 1 \u2192 assertion AutoMaps()===1 PASSES.\n       So the instrument is NOT blind to the restore write.)\n\nPASS  POSITIVE CONTROL 2: the failure branch really executes\n      (failpraid===true, praidDone===true, prestraidon===false \u2014 the three writes that live\n       ONLY inside the `else if (!farmFragments)` block at :1290-1294. The path runs.)\n\nFAIL  BUG: failure branch leaves AutoMaps at 0\n",
  "fix": "Mechanism-only, no numbers touched. In `PraidHarder()`'s failure exit (src/modules/other-praiding.ts:1290-1294) restore what the entry at :1231 disabled, mirroring the sibling exits at :1295-1298 and :1355-1361:\n\n    else if (!farmFragments) {\n        failpraid = true;\n        prestraidon = false;\n ",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 20,
  "sev": "HIGH",
  "file": "src/modules/other-praiding.ts",
  "line": 1422,
  "klass": "other",
  "numeric": false,
  "title": "BWraiding's `while (!preMapsActive && !bwraidon) mapsClicked();` hangs the browser tab when mapsClicked() returns early",
  "instrument": "Vitest repro (`tests/_repro/f20.test.ts`) on the sim boot harness: `bootGame({withAutoTrimps:true})` (scripts/sim/boot.mjs) loads the REAL Trimps v5.10.1 clone (../trimps-game, so the REAL `mapsClicked` at main.js:10760, real `tooltip`, real `mapsSwitch`) into jsdom, then evals the REAL built AT bundle (dist/autotrimps.user.js) and runs its settings init \u2014 so `window.BWraiding` is the actual shipp",
  "evidence": "$ npx vitest run tests/_repro/f20.test.ts\n\n \u276f tests/_repro/f20.test.ts (3 tests | 2 failed)\n     \u2713 POSITIVE CONTROL: not in a map, the loop terminates (2 mapsClicked calls)\n     \u00d7 BUG: inside a Void map, BWraiding() spins forever on mapsClicked()  686ms\n     \u00d7 BUG: with the game Paused, BWraiding() spins forever on mapsClicked()  85ms\n\nAssertionError: expected [Function] to not throw an error but\n  \"Error: INFINITE_LOOP: mapsClicked called 5000+ times\" was thrown\n\nThe positive control PASSES in the same harness and same function: with `mapsActive=false`, the real\n`BWraiding()` runs the same `while` loop, `mapsClicked()` is called exactly 2 times, `preMapsActive`\nflips to true, and the loop e",
  "fix": "Mechanism-only; touches no numeric/balance value. Replace the unbounded loop at other-praiding.ts:1422 with the module's own established bounded idiom, including the `!mapsActive` guard the other three sites already have:\n\n    if (!game.global.preMapsActive && !game.global.mapsActive && !bwraidon) {",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 21,
  "sev": "HIGH",
  "file": "src/modules/other-praiding.ts",
  "line": 989,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "AutoMaps option 2 (\"Auto Maps: Unique\") reaches no branch in Praiding / dailyPraiding / BWraiding \u2014 the raid never disables AutoMaps, never fails out,",
  "instrument": "Vitest unit repro (jsdom env) calling the REAL `Praiding()` from `src/modules/other-praiding.ts` with the real `getPageSetting`/`debug` imports, a seeded `autoTrimpSettings`, a hand-built `game` fixture (`world:60`, `preMapsActive:true`, `fragments.owned:0`), and spies on the native mutators the raid drives (`mapsClicked`/`buyMap`/`updateMapCost`/`selectMap`/`runMap`). It observes exactly what the",
  "evidence": "Both arms run 5 ticks of `Praiding()` on the same fixture.\n\nARM A \u2014 POSITIVE CONTROL, `AutoMaps = 1` \u2192 PASSES. The instrument sees the full state machine execute:\n  failpraid = true, praidDone = true, prestraidon = false, AutoMaps restored to 1.\n(i.e. line 989 wrote AutoMaps\u21920, which opened the `== 0` failure gate at line 1065, which terminated the raid.)\n\nARM B \u2014 `AutoMaps = 2` (\"Auto Maps: Unique\") \u2192 FAILS. After 5 ticks:\n\n    AssertionError: expected { AutoMaps: 2, failpraid: false, \u2026(3) } to deeply equal { AutoMaps: +0, failpraid: true, \u2026(3) }\n    - Expected                    + Received\n    -   \"AutoMaps\": 0,            +   \"AutoMaps\": 2,\n    -   \"failpraid\": true,        +   \"failpraid",
  "fix": "Pure mechanism \u2014 no game-balance number is touched (the only literals involved are the AutoMaps multitoggle's own option indices, which are a setting's enum, not tuning).\n\nThe invariant the code MEANT to express is \"remember whatever AutoMaps was, force it off for the duration of the raid, restore i",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 22,
  "sev": "HIGH",
  "file": "src/modules/other.ts",
  "line": 164,
  "klass": "never-written",
  "numeric": false,
  "title": "ATspirebreed() never restores the pre-Spire Geneticist Assist timer \u2014 prespiretimer is always undefined at the restore site, and the 4x toggle then pa",
  "instrument": "Full-fidelity jsdom differential: `bootGame({withAutoTrimps:true})` from scripts/sim/boot.mjs \u2014 it evals the REAL Trimps v5.10.1 clone (config.js/objects.js/updates.js/main.js) plus the REAL built dist/autotrimps.user.js into one jsdom window. So `ATspirebreed` is AT's shipped code and `toggleGeneticistassist` / `GeneticistassistSteps` are the GAME's own code (no re-implementation, no mocks). This",
  "evidence": "Test 1 (the bug), RED against current code:\n\n  AssertionError: expected 30 to be 1 // Object.is equality\n   \u276f tests/_repro/f22.test.ts:47  expect(win.game.global.GeneticistassistSetting).toBe(1)\n  Tests  1 failed | 1 passed (2)\n\nSetup: user's vanilla GA = 1s, SpireBreedTimer = 45, IgnoreSpiresUntil = 1, world 200.\n  - spireActive = true  \u2192 ATspirebreed() \u2192 GeneticistassistSetting === 45  \u2705 (this assertion PASSED \u2014 the swap half works, and it proves the function really executed under this instrument: a live positive control inside the same call)\n  - spireActive = false \u2192 ATspirebreed() \u2192 GeneticistassistSetting === 30  \u274c (expected 1 \u2014 the user's setting is gone, permanently rewritten to 30s)\n",
  "fix": "Mechanism-only, no game-balance number touched. Hoist the saved value out of the per-call function scope so it survives across ticks \u2014 the same pattern the module already uses for its `spirebreeding` flag (other.ts:151):\n\n    globalThis.spirebreeding = false;\n    let prespiretimer: number | undefine",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 23,
  "sev": "HIGH",
  "file": "src/modules/perks.ts",
  "line": 23,
  "klass": "gate",
  "numeric": false,
  "title": "Universe gate is evaluated once at script load \u2014 a live U1\u2194U2 portal leaves the other universe's AutoPerks completely uninitialized, and autoPortal th",
  "instrument": "Vitest + jsdom repro (tests/_repro/f23.test.ts, now deleted) that executes perks.ts EXACTLY as production does: `npx esbuild src/modules/perks.ts --bundle --format=iife --tsconfig-raw={}` then indirect-`eval` of the IIFE in a jsdom global with `game.global.universe` seeded to 1 or 2, then flipped live afterwards (as the game does on a portal). This instrument can observe: (a) which methods exist o",
  "evidence": "4 tests: 2 POSITIVE CONTROLS PASS, 2 bug tests FAIL.\n\nPOSITIVE CONTROL (proves the instrument sees the gate flip both ways):\n  \u2713 loaded in U1: typeof AutoPerks.clickAllocate === 'function' AND document.getElementById('ratioPreset') !== null\n  \u2713 loaded in U2: document.getElementById('RratioPreset') !== null\n\nTHE BUG (captured console output of the real exception at the real callsite):\n  U2-load \u2192 U1 portal, autoPortal()   : TypeError: AutoPerks.clickAllocate is not a function\n  U1-load \u2192 U2 portal, RautoPortal()  : TypeError: Cannot read properties of null (reading 'selectedIndex')\n\n  FAIL \"loaded in U2 \u2192 live portal into U1\":\n    expect(typeof AutoPerks).not.toBe('undefined')          \u2192 PASS",
  "fix": "Mechanism-only; no game-balance number is touched (this finding involves no numeric literal at all).\n\n1. DEFINE unconditionally. Delete the `if (game.global.universe == 1) {` wrapper at perks.ts:23 and its closing `}` at :749 (a pure de-indent \u2014 the block contains only `AutoPerks.<method> = ...` def",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 24,
  "sev": "HIGH",
  "file": "src/modules/perks.ts",
  "line": 1410,
  "klass": "other",
  "numeric": false,
  "title": "RAutoPerks.perkHolder omits Masterfulness and Smithology \u2014 Allocate Perks in U2 counts their radon into the budget but never re-buys them, so a respec",
  "instrument": "A vitest repro (tests/_repro/f24.test.ts, node env) driving the REAL v5.10.1 game clone + the REAL built AT bundle in jsdom via scripts/sim/boot.mjs `bootGame({ withAutoTrimps: true })`. This instrument can see everything relevant here: it runs the game's own `game.portal` table from config.js, the game's `clearPerks()` / `buyPortalUpgrade()` / `commitPortalUpgrades()` from main.js, and AT's real ",
  "evidence": "All three claims of the finding reproduce against current code (3 of 4 assertions FAIL; the 4th, \"the game really does expose these as unlockable U2 perks with radLevel\", passes):\n\nEVIDENCE 1 \u2014 the perk is dropped from the plan:\n  AssertionError: expected [ 'resilience', 'greed', \u2026(19) ] to include 'masterfulness'\n  (the same list also lacks 'smithology'; meanwhile enumerating the live game \u2014 keys of game.portal with !radLocked && typeof radLevel !== 'undefined' \u2014 DOES contain both, asserted and passing in the same test)\n\nEVIDENCE 2 \u2014 the budget over-count is real (getRadon counts radon it can never spend):\n  expect(withPerks - without).toBe(0)\n  - Expected 0\n  + Received 19134            //",
  "fix": "Two mechanism fixes, neither of which touches a balance number:\n\n1. STOP THE WIPE (the HIGH part). In `RAutoPerks.applyCalculationsRespec` (src/modules/perks.ts:1248), after `clearPerks()` and before/alongside the re-buy loop, restore every perk the planner does not model: snapshot, BEFORE clearPerk",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 25,
  "sev": "HIGH",
  "file": "src/modules/portal.ts",
  "line": 100,
  "klass": "brace-scope",
  "numeric": false,
  "title": "dailyAutoPortal(): the whole He/Hr body is inside `if (!aWholeNewWorld) {` \u2014 with the DEFAULT buffer of 0, 'DP: He/Hr' never portals at all",
  "instrument": "vitest + jsdom unit repro (tests/_repro/f25.test.ts, now deleted) that imports the REAL src/modules/portal.ts and calls the REAL `dailyAutoPortal()` / `autoPortal()` with a seeded `autoTrimpSettings` + `game` on globalThis. Instrument = the free global `tooltip()` (\"Auto Portaling NOW!\"), which is called ONLY inside the portal block (portal.ts:43 for autoPortal, portal.ts:114 for dailyAutoPortal) ",
  "evidence": "$ npx vitest run tests/_repro/f25.test.ts\n\n \u276f tests/_repro/f25.test.ts (3 tests | 2 failed)\n     \u2713 POSITIVE CONTROL: autoPortal() (no brace) DOES portal at the zone boundary with buffer 0\n     \u00d7 BUG: dailyAutoPortal() never portals at the zone boundary with the default buffer 0\n     \u00d7 and the mid-zone (!aWholeNewWorld) path is ALSO dead with buffer 0\n\n FAIL > BUG: dailyAutoPortal() never portals at the zone boundary with the default buffer 0\n AssertionError: expected \"vi.fn()\" to be called at least once\n  110|     expect(tooltipSpy).toHaveBeenCalled()\n\n FAIL > and the mid-zone (!aWholeNewWorld) path is ALSO dead with buffer 0\n AssertionError: expected \"vi.fn()\" to be called at least once\n  1",
  "fix": "MECHANISM ONLY \u2014 no game-balance number is touched. Make dailyAutoPortal's guard a single statement, matching its already-correct sibling autoPortal (portal.ts:30-31):\n\n  src/modules/portal.ts:100  ->  if (!aWholeNewWorld)\n                                     heliumHrBuffer *= MODULES[\"portal\"].buff",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 26,
  "sev": "HIGH",
  "file": "src/modules/portal.ts",
  "line": 263,
  "klass": "never-reset",
  "numeric": false,
  "title": "`challengeSquaredMode` is set true and NEVER reset \u2014 after the first C2 Runner pick, Auto Start Daily and portal-challenge selection are dead for the ",
  "instrument": "In-process vitest (jsdom) unit drive of the REAL `src/modules/portal.ts`, calling the real exported `doPortal()` TWICE within one module lifetime. `autoTrimpSettings` is SEEDED (getPageSetting/debug are real imports \u2014 the maps/jobs precedent, never mocked); every native game collaborator (selectChallenge / activatePortal / portalClicked / checkCompleteDailies / getDailyChallenge / swapPortalUniver",
  "evidence": "Static half \u2014 `grep -rn 'challengeSquaredMode' src/ legacy/` returns ONLY:\n  portal.ts:12                     `var challengeSquaredMode: any;`   (module-scoped, initially undefined)\n  portal.ts:165,170,\u2026,215 (\u00d711)    `challengeSquaredMode = true;`     (one per C2 branch in c2runner())\n  portal.ts:263                    `if (challengeSquaredMode == true)` (the only read)\nThere is NO `challengeSquaredMode = false` anywhere in src/, nor in the built bundle golden (tests/fixtures/src-bundle.golden.js) or the pinned oracle (autotrimps.oracle.user.js). The write is monotonic true.\n\nDynamic half \u2014 three-case run of the real module (`npx vitest run tests/_repro/f26.test.ts`):\n\n  \u2713 portal 1: a C2 bel",
  "fix": "Pure mechanism fix \u2014 NO game-balance number is touched (no threshold, cost, rate or multiplier is edited; only a stale boolean's lifetime).\n\n`challengeSquaredMode` must be re-derived per evaluation instead of latching. In `src/modules/portal.ts`, reset it at the top of `c2runner()` so the 11 `= true",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 27,
  "sev": "HIGH",
  "file": "src/modules/settings-defs.ts",
  "line": 690,
  "klass": "type-confusion",
  "numeric": false,
  "title": "36 boolean settings are declared with a STRING default ('false'/'true') \u2014 getPageSetting returns the raw string, so BOTH `== true` and `== false` are ",
  "instrument": "Two instruments, both able to see the region.\n(1) `node -e` for the pure JS coercion semantics (can observe: the `==` truth table and `||` default-fallback \u2014 nothing else needed, this half of the finding is pure language semantics).\n(2) A jsdom vitest repro at tests/_repro/f27.test.ts (since deleted) driving the REAL production chain end-to-end: the real `createSetting` (src/modules/settings-engin",
  "evidence": "A. LANGUAGE SEMANTICS (node):\n  ('false'||false) = false        <- wait: JS prints the STRING 'false' here; node's console renders it unquoted. Verified separately: typeof ('false'||false) === 'string'.\n  'false' == false -> false\n  'false' == true  -> false\n  'true'  == true  -> false\n  'true'  == false -> false\n  !'false'         -> false        <- the first CLICK on such a toggle yields boolean `false` (still OFF)\n\nB. THE SHIPPED DECLARATIONS (36 of them, exact ids + defaults, from\n   `grep -rnoE \"createSetting\\('([A-Za-z0-9_]+)'.*'boolean', *'(true|false)'\" src/modules/`):\n  45stacks=false addpoison=false ATGA2=false AutoGigas=TRUE buynojobsc=false cfightforever=false\n  fuckjobs=false fu",
  "fix": "Quote removal at 36 createSetting callsites in src/modules/settings-defs.ts: `'boolean', 'false'` -> `'boolean', false` and `'boolean', 'true'` -> `'boolean', true`.\n\nNO GAME-BALANCE NUMBER IS TOUCHED \u2014 these are boolean setting defaults (a type bug), not tuning constants. The two `'true'` cases (Au",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 28,
  "sev": "HIGH",
  "file": "src/modules/settings-defs.ts",
  "line": 273,
  "klass": "type-confusion",
  "numeric": false,
  "title": "RBuyBuildingsNew default is the string 'true' \u2014 its only gate is `== true`, which is FALSE, so U2 AutoBuildings never runs on a fresh install while th",
  "instrument": "Two instruments, both of which CAN see this code.\n(1) `node -e` on the raw comparison \u2014 this finding is pure JS coercion semantics, so the expression evaluation IS the proof.\n(2) A vitest/jsdom repro (tests/_repro/f28.test.ts, since deleted) that drives the REAL `createSetting` (src/modules/settings-engine.ts) and the REAL `getPageSetting` (src/modules/utils.ts) with the verbatim argument list fro",
  "evidence": "=== (a) Pure semantics \u2014 `node -e` ===\n'true' == true  -> false\nNumber('true')  -> NaN\nNumber(true)    -> 1\nBoolean('true') -> true      (truthy, so the BUTTON renders ON)\ntrue == true    -> true      (what the other 28 booleans get)\n\n=== The definition (src/modules/settings-defs.ts:273) \u2014 string 'true', QUOTED ===\ncreateSetting('RBuyBuildingsNew', 'AutoBuildings', '...', 'boolean', 'true', null, \"Buildings\");\n\n=== The ONLY gate (legacy/AutoTrimps2.js:322-325) \u2014 verified sole caller ===\n        //RBuildings\n        if (getPageSetting('RBuyBuildingsNew') == true) {\n            RbuyBuildings();\n        }\n`grep -rn \"RbuyBuildings\\b\" src/ legacy/` -> defined at buildings.ts:567, called ONLY at A",
  "fix": "MECHANISM fix \u2014 a type correction, NOT a game-balance number. No damage/HP/cost/rate/multiplier literal is touched; the intended value (\"on by default\") is unchanged, only its TYPE is corrected so the existing gate can read it. This does not need the tuning gate.\n\n1) src/modules/settings-defs.ts:273",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 29,
  "sev": "HIGH",
  "file": "legacy/AutoTrimps2.js",
  "line": 188,
  "klass": "gate",
  "numeric": false,
  "title": "\"AM Always\" forces AutoMaps back to 1 every tick \u2014 kills the 3rd option AND undoes the Praiding/AMP automap-suspend handshake",
  "instrument": "The jsdom sim harness (scripts/sim/boot.mjs) booting the REAL built bundle (dist/autotrimps.user.js, which contains legacy/AutoTrimps2.js verbatim) over the real Trimps clone with the real mid-U1 save (tests/fixtures/saves/02-mid-u1.txt), then calling `window.mainLoop()` directly and reading back `window.autoTrimpSettings.AutoMaps.value`. This instrument CAN see this region: line 188 is plain sync",
  "evidence": "One real mainLoop() tick, AM Always ON, on a real mid-U1 save:\n\n  case (a) user picks \"Auto Maps: Unique\" (AutoMaps.value = 2)\n    AssertionError: expected 1 to be 2   ->  value was rewritten 2 -> 1\n\n  case (b) Praiding/AMP suspend write (AutoMaps.value = 0, exactly what\n           other-praiding.ts:990/1231 and mapfunctions-amp.ts:624/799 write)\n    AssertionError: expected 1 to be +0  ->  value was rewritten 0 -> 1\n\n  control  AM Always OFF, AutoMaps.value = 2\n    PASS -> value still 2 after the identical tick\n\nTest Files 1 failed; Tests 2 failed | 1 passed.\n\nThe culprit line, unchanged in the shipped bundle (legacy/AutoTrimps2.js:188, U1; :302 is the identical U2 twin on RAutoMaps/Rautoma",
  "fix": "Two-part mechanism fix; NO game-balance numbers involved (this touches a settings-multitoggle index, not tuning).\n\n1. Stop clobbering the 3rd option. Change the guard from `!= 1` to `== 0`, matching the already-correct sibling AM-Portal guard at AutoTrimps2.js:390/397:\n     legacy/AutoTrimps2.js:188",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 30,
  "sev": "MEDIUM",
  "file": "src/modules/stance.ts",
  "line": 397,
  "klass": "type-confusion",
  "numeric": false,
  "title": "windStance() can never enter X formation - setFormation(0) is a silent no-op (numeric 0 is falsy)",
  "instrument": "A failing vitest at tests/_repro/f30.test.ts that boots the REAL game (scripts/sim/boot.mjs \u2192 jsdom + ../trimps-game/main.js) and calls the game's authentic `setFormation`, rather than the `vi.fn()` stub used by tests/stance.characterization.test.ts. This instrument CAN see the region: it executes the real `setFormation` body including the `if (what)` guard and reads back `game.global.formation`. ",
  "evidence": "1) FAILING TEST (real game, real setFormation):\n\n  \u2713 TRIPWIRE: the game fixture is really hydrated (methods present)\n      expect(typeof game.buildings.Shed.cost.wood).toBe('function')  -> passed\n  \u2713 POSITIVE CONTROL: setFormation(\"0\") (string) DOES enter X\n      game.global.formation 2 -> 0                                   -> passed\n  \u00d7 BUG: setFormation(0) (the number stance.ts:397 passes) leaves the formation UNCHANGED\n      AssertionError: expected 2 to be +0\n      - Expected: 0\n      + Received: 2\n      at tests/_repro/f30.test.ts:38\n\n  Test Files 1 failed (1) | Tests 1 failed | 2 passed (3)\n\nThe two calls differ ONLY in '0' vs 0. String works; number is a silent no-op. That is the who",
  "fix": "MECHANISM-ONLY. No game-balance number is touched \u2014 this is a type/coercion fix (numeric 0 vs string \"0\"), and it changes nothing for stancey values 1/2/3/5 (all truthy, and the game already does parseInt(what,10) on them).\n\n1. src/modules/stance.ts:397\n     - setFormation(stancey);\n     + setFormat",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 31,
  "sev": "HIGH",
  "file": "src/modules/scryer.ts",
  "line": 152,
  "klass": "type-confusion",
  "numeric": false,
  "title": "Scryer smooth-transition arms call setFormation(x) with x === 0 - same falsy no-op, and they return, blocking the fallback",
  "instrument": "A vitest jsdom unit repro (tests/_repro/f31.test.ts, now deleted) that (1) mocks getPageSetting, (2) injects a hand-authored `game` fixture via makeMinimalGame, (3) stubs the ambient bridge globals scryer.ts resolves at runtime (survive / oneShotPower / getCurrentEnemy / getEmpowerment / \u2026), and (4) installs a `setFormation` that is the game's REAL gate copied verbatim from ../trimps-game/main.js:",
  "evidence": "Run: `npx vitest run tests/_repro/f31.test.ts` \u2192 \"Tests 2 failed | 2 passed (4)\".\n\nPASSED (positive control A \u2014 the instrument can see a formation write): flipping `survive` so the loop lands on the sibling \"B\" arm (scryer.ts:153, which passes the truthy literal 3) gives formationCalls == [3] and game.global.formation == 3.\nPASSED (positive control B \u2014 the string form works): calling the same game-faithful setFormation('0') moves game.global.formation 2 \u2192 0.\n\nFAILED (the bug, XB arm, scryer.ts:152):\n  AssertionError: expected 2 to be +0\n  expect(game.global.formation).toBe(0)  // it is still 2\nwith `expect(formationCalls).toEqual([0])` PASSING first \u2014 i.e. the arm genuinely executed and pass",
  "fix": "Mechanism fix, not a balance change \u2014 these are formation IDENTIFIERS, not tuning values; the fix makes the intended X formation actually get set, and changes no numeric game balance.\n\nIn src/modules/scryer.ts:44-51, make the X/W formation token a STRING so it is truthy at the game's `if (what)` gat",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 32,
  "sev": "HIGH",
  "file": "src/modules/import-export.ts",
  "line": 891,
  "klass": "never-written",
  "numeric": false,
  "title": "\"Cleanup Saved Settings\" deletes the ATversion key that loadPageVariables requires \u2014 every AT setting silently resets to default on the next page load",
  "instrument": "vitest (jsdom env), driving the REAL functions \u2014 no re-implementation: `cleanupAutoTrimps()` from src/modules/import-export.ts (the exact body the \"Cleanup Saved Settings\" button reaches via ImportExportTooltip('CleanupAutoTrimps') at import-export.ts:243), plus `serializeSettings()` / `saveSettings()` / `loadPageVariables()` from src/modules/utils.ts. What this instrument CAN see: the whole persi",
  "evidence": "FAILING RUN (npx vitest run tests/_repro/f32.test.ts), stdout + both assertions:\n\n  KEYS AFTER CLEANUP: [\"BuyBuildings\"]\n  FILE THAT WILL BE SAVED: {\"BuyBuildings\":false}\n  AFTER RELOAD: {}\n\n  \u00d7 cleanupAutoTrimps() deletes ATversion, and the next saved file is rejected at boot\n  AssertionError: expected [ 'BuyBuildings' ] to include 'ATversion'      (tests/_repro/f32.test.ts:50)\n  AssertionError: expected undefined to be false // Object.is equality    (tests/_repro/f32.test.ts:57)\n    - Expected: false      <- BuyBuildings, the user's saved value\n    + Received: undefined  <- loadPageVariables() rejected the file; autoTrimpSettings === {}\n\nPOSITIVE CONTROL (ran BEFORE the cleanup call in the",
  "fix": "Mechanism-only; touches NO game-balance number. In `cleanupAutoTrimps()` (src/modules/import-export.ts:891), skip non-record keys instead of deleting them \u2014 the loop's intent is \"drop setting records whose control no longer exists\", and ATversion is not a setting record:\n\n  export function cleanupAu",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 33,
  "sev": "HIGH",
  "file": "src/modules/perks.ts",
  "line": 21,
  "klass": "supply-chain",
  "numeric": false,
  "title": "AutoPerks depends on a third-party remote script (Zorn192.github.io) injected at load; the local copy exists but is not bundled",
  "instrument": "Three instruments, all of which CAN see this region (no harness blindness \u2014 this is a build/bundle-composition fact plus a pure-semantics fact, so neither the sim's 4-mutator L0 net nor jsdom's missing layout engine is involved):\n(1) `npm run build` + grep over the actual SHIPPED artifact `dist/autotrimps.user.js` (1,077,489 bytes). This observes exactly what a user's Tampermonkey executes.\n(2) `n",
  "evidence": "FACT 1 \u2014 the shipped bundle USES the symbol and NEVER DEFINES it.\n  $ npm run build   \u2192 [build] dist/autotrimps.user.js (1077489 bytes)\n  $ grep -n \"FastPriorityQueue\" dist/autotrimps.user.js\n    17214:  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\n    17484:      var effQueue = new FastPriorityQueue(function(a, b) {\n    17566:      var effQueue = new FastPriorityQueue(function(a, b) {\n    17884:  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\n    18157:    var effQueue = new FastPriorityQueue(function(a, b) {\n    18239:    var effQueue = new FastPriorityQueue(function(a, b) {\n  4 construction sites, 2 remote-injection sites, ",
  "fix": "Purely mechanical; touches NO game-balance number and changes NO automation behavior (the vendored file is\nbyte-identical to what is being fetched \u2014 sha1 8a89fcef\u2026 on both sides \u2014 so runtime semantics are unchanged).\n\n1. Bundle the local copy. In `scripts/build-userscript.mjs`, add `'FastPriorityQue",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 34,
  "sev": "HIGH",
  "file": "legacy/AutoTrimps2.js",
  "line": 218,
  "klass": "brace-scope",
  "numeric": false,
  "title": "Brace-scope inversion: BuyBuildingsNew option 3 ('Buy Storage') binds to `else` of `if (!usingRealTimeOffline)` \u2014 it can only ever run during the offl",
  "instrument": "Two instruments, both of which can demonstrably see the region.\n\n(1) DIRECT EXECUTION OF THE VERBATIM SOURCE BYTES. I read legacy/AutoTrimps2.js off disk, sliced lines 210-218 (the `//Buildings` dispatch) as raw text, and compiled it with `new Function('usingRealTimeOffline','getPageSetting','buyBuildings','buyStorage', block)`. This executes the real committed bytes \u2014 it cannot drift from the fil",
  "evidence": "=== A. VERBATIM SOURCE UNDER TEST (legacy/AutoTrimps2.js:210-218, printed by the harness) ===\n        //Buildings\n        if (!usingRealTimeOffline) {\n        if (getPageSetting('BuyBuildingsNew') === 0 && getPageSetting('hidebuildings') == true) buyBuildings();\n        else if (getPageSetting('BuyBuildingsNew') == 1) {\n            buyBuildings();\n            buyStorage();\n        } else if (getPageSetting('BuyBuildingsNew') == 2) buyBuildings();\n\t}\n        else if (getPageSetting('BuyBuildingsNew') == 3) buyStorage();\n\nThe `}` closes the OUTER `if (!usingRealTimeOffline)`. So `else if (... == 3)` is the outer `else`.\n\n=== B. EXECUTION RESULT (full cross-product) ===\noffline  opt  hidebuildi",
  "fix": "MECHANISM-ONLY. No numeric/tuning value is touched \u2014 this is a brace placement, not a balance constant. (The four option labels, the default of 1, and every cost/threshold stay exactly as they are.)\n\nlegacy/AutoTrimps2.js:210-218 \u2014 move the `== 3` arm INSIDE the `!usingRealTimeOffline` block so it c",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 36,
  "sev": "HIGH",
  "file": "scripts/sim/corpus.mjs",
  "line": 15,
  "klass": "harness-blind",
  "numeric": false,
  "title": "The L0 corpus's 1500-tick budget \u2014 not \"corpus depth\" \u2014 is what blinds the proof net to the entire map subsystem; corpus-coverage.test.ts has enshrine",
  "instrument": "A node script driving the EXISTING, UNMODIFIED L0 trace pipeline (scripts/sim/boot.mjs bootGame + installSeededRandom(1) + installFrozenClock + installRecorder + driver.mjs stepWithAT) against the EXISTING committed save tests/fixtures/saves/02-mid-u1.txt and the EXISTING frozen oracle bundle tests/fixtures/oracle/autotrimps.oracle.user.js. The ONLY variable changed was the tick budget (corpus.mjs",
  "evidence": "Same save, same seed, same oracle bundle, same pipeline. Only `ticks` differs:\n\n```text\nticks    wall     end state              trace events + mutator histogram\n 1500    4.8 s    world 5, maps=false     73  {buyEquipment:5, buyBuilding:53, buyJob:15}   <- EXACTLY the committed oracle trace\n15000   55.3 s    world 6, maps=true     823  {buyEquipment:12, buyBuilding:772, buyJob:34,\n                                                buyUpgrade:3, selectMap:1, runMap:1}        <- MAPS REACHED\n```\n\nProgression of the 15000-tick run (printed every 1500 ticks):\n```text\nat load  world=4  mapsUnlocked=false\nt=1500   world=5 cell=15 mapsUnlocked=false events=73\nt=10500  world=5 cell=92 mapsUnlocked=fals",
  "fix": "Pure harness/coverage change \u2014 touches NO game-balance number. `ticks` in scripts/sim/corpus.mjs is a harness budget knob (how long the sim self-plays), not a gameplay constant; it does not feed any formula the game reads.\n\n1. scripts/sim/corpus.mjs: add a map-reaching corpus entry rather than infla",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 37,
  "sev": "HIGH",
  "file": "scripts/sim/boot.mjs",
  "line": 25,
  "klass": "harness-blind",
  "numeric": false,
  "title": "The setTimeout stub means game triggers can NEVER fire in the sim \u2014 nothing trigger-gated ever unlocks (a live #66 residue; the fix was point-wise)",
  "instrument": "The real sim harness itself (`scripts/sim/boot.mjs` + `scripts/sim/driver.mjs`), driven from a node script against the committed fixture `tests/fixtures/saves/02-mid-u1.txt` \u2014 i.e. the exact instrument the oracle traces are recorded with. What it CAN see: the whole game object graph post-`load()` (tripwire `typeof game.buildings.Shed.cost.wood === 'function'` asserted before trusting any result), ",
  "evidence": "CONFIRMED on three independent axes.\n\n1) REACHABILITY, from the game source (../trimps-game/main.js). The non-force `checkTriggers()` \u2014 the only pass that fires NEW unlocks \u2014 has exactly ONE reachable caller in the entire codebase:\n```\nmain.js:4659  function checkTriggers(force) {\nmain.js:4662      if (force) { ...fire already-done triggers only...; continue; }   <-- force SKIPS pending\nmain.js:4669      if (trigger.done === 0 && canAffordTwoLevel(...)) { trigger.fire(); ... }  <-- the unlock branch\nmain.js:17965 function costUpdatesTimeout() {\nmain.js:17970     checkTriggers();          <-- the ONLY non-force callsite\nmain.js:17972     setTimeout(costUpdatesTimeout, 250);   <-- self-resched",
  "fix": "Pure mechanism \u2014 restores a call the harness suppressed. Touches NO game-balance number.\n\nIn `scripts/sim/driver.mjs`, `tickOnce()` must reproduce the cadence the browser gets from `costUpdatesTimeout`. The game's own timer runs `checkTriggers()` every 250 ms of wall time (main.js:17972), and a tick",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 38,
  "sev": "HIGH",
  "file": "src/modules/perks.ts",
  "line": 21,
  "klass": "supply-chain",
  "numeric": false,
  "title": "Shipped userscript loads third-party code from Zorn192.github.io on every page load \u2014 full code-exec in the game context",
  "instrument": "Two instruments, both able to see this region. (1) The SHIPPED ARTIFACT: `npm run build` \u2192 grep `dist/autotrimps.user.js` for URLs and for the FastPriorityQueue implementation. This can see exactly what a user's userscript manager executes. (2) The SIM's jsdom boot (`scripts/sim/boot.mjs`, `withAutoTrimps: true`), which evals the real dist bundle into a real DOM \u2014 so it can observe `document.head`",
  "evidence": "A. Remote code-exec is in the shipped bundle, unconditionally, at top level:\n\n    $ npm run build   \u2192 [build] dist/autotrimps.user.js (1077489 bytes)\n    $ grep -n FastPriorityQueue dist/autotrimps.user.js\n    17214:  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\n    17484:      var effQueue = new FastPriorityQueue(function(a, b) {\n    17566:      var effQueue = new FastPriorityQueue(function(a, b) {\n    17884:  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";   <-- second (U2) inject\n    18157:    var effQueue = new FastPriorityQueue(function(a, b) {\n    18239:    var effQueue = new FastPriorityQueue(function(a, b) {\n\n  dist:172",
  "fix": "Pure mechanism; touches NO game-balance number.\n1. Ship the dependency locally: add `'FastPriorityQueue.js'` to the `MANIFEST` in `scripts/build-userscript.mjs` (it must be concatenated BEFORE the src IIFE, alongside AutoTrimps2.js, since perks.ts's call sites resolve it as a bare global) \u2014 or, clea",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 39,
  "sev": "MEDIUM",
  "file": "tests/build-userscript.test.ts",
  "line": 76,
  "klass": "harness-blind",
  "numeric": false,
  "title": "The build test's \"no remote injection survives\" guard greps the wrong domain \u2014 deLoaderize only rewrites AutoTrimps2.js, so src/ module injections sai",
  "instrument": "Two instruments, both of which CAN see the region in question (this is a build-time/text property of the shipped artifact \u2014 no sim, no jsdom, no game state involved, so none of the usual harness blind spots apply).\n\n(1) `npm run build` + grep over the real shipped `dist/autotrimps.user.js`. This observes the exact bytes the CI Pages deploy publishes to users. It can see every `.src =` assignment a",
  "evidence": "The guard at tests/build-userscript.test.ts:75-77 is commented \"Loader neutered: no remote injection survives\" but only blocklists a single hostname. The claim is false against the artifact it tests.\n\n$ npm run build\n[build] dist/autotrimps.user.js (1077489 bytes)\n\n$ grep -n \"Quiaaaa\\|Zorn192.github.io/AutoTrimps/FastPriorityQueue\\|code.highcharts.com\" dist/autotrimps.user.js\n17214:  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\n17884:  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\n18960:  chartscript.src = \"https://code.highcharts.com/highcharts.js\";\n\nZero `Quiaaaa` hits \u2014 so the guard is green \u2014 while three remote script src",
  "fix": "Mechanism-only; touches no game-balance number and no runtime behavior.\n\nReplace the single-hostname blocklist at tests/build-userscript.test.ts:76 with a positive ALLOWLIST over the whole built output \u2014 the finder's recommendation is correct:\n\n    // Loader neutered: the ONLY remote script injectio",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 40,
  "sev": "MEDIUM",
  "file": "tests/sim/guard.ts",
  "line": 11,
  "klass": "gate",
  "numeric": false,
  "title": "The proof net has NEVER run in the deploy gate \u2014 11 suites / 34 tests (incl. the 10-test \"KEYSTONE\") silently describe.skip on CI",
  "instrument": "Shell measurement of the real suite + a throwaway rsync copy of the repo in scratchpad (no sibling ../trimps-game, i.e. a faithful CI runner) with mutations injected into src/. The real repo was never edited (git status clean throughout).\n\nWHAT THIS INSTRUMENT CAN SEE: (a) exactly which suites vitest skips and the process exit code, under both clone-present and clone-absent conditions \u2014 this is a ",
  "evidence": "THE MECHANISM IS CONFIRMED, DIRECTLY OBSERVED.\n\n1) .github/workflows/deploy.yml has exactly ONE `actions/checkout@v4` (the repo). It never fetches ../trimps-game. scripts/sim/boot.mjs:8 \u2192 DEFAULT_GAME_DIR = process.env.TRIMPS_GAME_DIR || resolve(HERE,'../../../trimps-game'). tests/sim/guard.ts:11 \u2192 `existsSync(resolve(DEFAULT_GAME_DIR,'index.html')) ? describe : describe.skip`.\n\n2) MEASURED, same tree, _repro excluded:\n   LOCAL  (clone present):  Test Files 48 passed (48) | Tests 621 passed (621) | 0 skipped | 45.3s\n   CI-SIM (no clone):       Test Files 37 passed | 11 SKIPPED (48) | Tests 587 passed | 34 SKIPPED (621) | 2.7s\n   REAL_EXIT_CODE = 0   \u2190 GREEN. Pages deploy proceeds.\n   The 2.7",
  "fix": "No game-balance numbers are touched \u2014 this is pure CI plumbing plus a guard change. Three parts:\n\n1) .github/workflows/deploy.yml \u2014 fetch the clone, pinned by SHA, before `npm ci`:\n     - uses: actions/checkout@v4\n       with:\n         repository: Trimps/Trimps.github.io\n         ref: <pinned-sha-fo",
  "skepticVotes": "3/3 REAL"
 },
 {
  "idx": 41,
  "sev": "MEDIUM",
  "file": "src/modules/ab.ts",
  "line": 119,
  "klass": "gate",
  "numeric": false,
  "title": "ABdustsimplenonhid spends SA dust upgrading items the player does not own",
  "instrument": "A vitest repro (tests/_repro/f41.test.ts, now deleted) driving the REAL game: `bootGame({withAutoTrimps:true})` from scripts/sim/boot.mjs loads the real v5.10.1 clone in jsdom (real `autoBattle` object \u2014 real items table, real `upgradeCost()`, real `upgrade()`) plus the real AT bundle, so `window.ABdustsimplenonhid` is the actual function under test reached through the legacy bridge. This instrume",
  "evidence": "Fresh real game, dust=1e9, shards=1e9, 40 calls of the real `ABdustsimplenonhid()`; TOTAL upgrades applied = 40 (tripwire OK). Levels bumped:\n\n  upgrades by item: { Menacing_Mask: 4, Armor: 4, Doppelganger_Diadem: 5, Doppelganger_Signet: 5,\n    The_Doomspring: 2, Rusty_Dagger: 3, Battery_Stick: 3, Fists_of_Goo: 2, Raincoat: 2,\n    Putrid_Pouch: 2, Snimp__Fanged_Blade: 1, Chemistry_Set: 2, Bad_Medkit: 2,\n    Wrath_Crafted_Hatchet: 1, Comfy_Boots: 1, Lifegiving_Gem: 1 }\n\n  UNOWNED items upgraded (autoBattle.items[k].owned === false):\n    [ 'Doppelganger_Diadem', 5 ], [ 'Doppelganger_Signet', 5 ], [ 'The_Doomspring', 2 ],\n    [ 'Raincoat', 2 ], [ 'Putrid_Pouch', 2 ], [ 'Snimp__Fanged_Blade', 1 ",
  "fix": "Pure mechanism, no game-balance numbers touched.\n\n1) src/modules/ab.ts:119 \u2014 add the missing ownership guard to the filter:\n     if (autoBattle.items[item].owned && !autoBattle.items[item].equipped && !autoBattle.items[item].hidden)\n   (This mirrors ABdustsimple, which filters on `.equipped` and is ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 42,
  "sev": "MEDIUM",
  "file": "src/modules/ab.ts",
  "line": 65,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "ABcheck dispatch hole: on a two-way resist tie the preset switcher does nothing if the currently-loaded preset is the third one",
  "instrument": "A vitest unit repro (`tests/_repro/f42.test.ts`, `// @vitest-environment jsdom` \u2014 ab.ts imports utils.ts which touches `document` at module scope) that imports the REAL `ABcheck`/`ABswitch`/`getCurrentAB`/`checkPreset` from `src/modules/ab.ts` and injects a minimal `globalThis.autoBattle` (three disjoint presets, one equipped item, `sessionEnemiesKilled < sessionTrimpsKilled` so the `winning` earl",
  "evidence": "Repro run (`npx vitest run tests/_repro/f42.test.ts`) \u2014 2 failed | 3 passed:\n\n  \u2713 sanity: fixture yields getCurrentAB(true)=='pb', checkPreset(3)==true, checkPreset(1)==false, checkPreset(2)==false\n  \u2713 POSITIVE CONTROL: same 'pb' tie but currently on preset 1 -> ABcheck()==2 and loadPreset called with 'p2'\n  \u00d7 BUG: same 'pb' tie, currently on preset 3\n        AssertionError: expected undefined not to be undefined\n        \u276f tests/_repro/f42.test.ts:45  expect(ABcheck()).not.toBeUndefined()\n        (and loadPreset was never called -> ABswitch() is a no-op)\n  \u00d7 BUG: equipped set matches NO preset (one manual equip)\n        AssertionError: expected undefined not to be undefined\n        \u276f tests/_",
  "fix": "Mechanism-only; touches NO game-balance number (ab.ts contains no tuning constants on this path \u2014 only preset indices 1/2/3, which are identities, not balance values).\n\nMake every arm total: drop the `&& (checkPreset(x) || checkPreset(y))` outer guards and give each arm a default target so it can ne",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 44,
  "sev": "MEDIUM",
  "file": "src/modules/breedtimer.ts",
  "line": 175,
  "klass": "gate",
  "numeric": false,
  "title": "dATGA2Auto option 1 (\"ATGA: Auto No Spire\") is inverted \u2014 it auto-overrides ONLY inside a Daily Spire, exactly what the option promises not to do",
  "instrument": "Vitest unit repro (jsdom) driving the real exported `ATGA2()` from src/modules/breedtimer.ts with an injected `game` fixture \u2014 file created at tests/_repro/f44.test.ts, run with `npx vitest run tests/_repro/f44.test.ts`, then deleted.\n\nWHAT IT CAN SEE: `dailyModifiers.bogged.getMult` / `dailyModifiers.plague.getMult` are called from EXACTLY ONE site in breedtimer.ts (lines 176-177, inside the auto",
  "evidence": "$ npx vitest run tests/_repro/f44.test.ts\n\n \u2713 #44 > sanity: option 2 (\"Auto Dailies\") auto-overrides in BOTH spire states (control)\n \u2713 #44 > sanity: option 0 (\"Manual\") never auto-overrides (control)\n \u2717 #44 > option 1 OUTSIDE the Daily Spire: MUST auto-override (it is \"Auto\")\n     AssertionError: expected false to be true\n     tests/_repro/f44.test.ts:132  expect(autoBranchRan()).toBe(true)\n \u2717 #44 > option 1 INSIDE the Daily Spire: MUST NOT auto-override (it is \"No Spire\")\n     AssertionError: expected true to be false\n     tests/_repro/f44.test.ts:138  expect(autoBranchRan()).toBe(false)\n\n Test Files  1 failed (1)\n      Tests  2 failed | 2 passed (4)\n\nThe two failures are the exact inversio",
  "fix": "Mechanism-only, one character. src/modules/breedtimer.ts:175 \u2014 negate the spire test for index 1:\n\n  -  if ((getPageSetting('dATGA2Auto')==2||(getPageSetting('dATGA2Auto')==1 && disActiveSpireAT() && game.global.challengeActive == \"Daily\")) && ...\n  +  if ((getPageSetting('dATGA2Auto')==2||(getPageS",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 45,
  "sev": "MEDIUM",
  "file": "src/modules/buildings.ts",
  "line": 85,
  "klass": "type-confusion",
  "numeric": false,
  "title": "GymWall's documented \"disabled\" value (-1, the default) is truthy \u2014 it silently cancels the DecaBuild/DoubleBuild bonewall reward for Gyms",
  "instrument": "Two instruments. (1) `node -e` for the pure semantics: `parseFloat('-1')` \u2192 -1, and `!!(-1)` \u2192 true, i.e. the documented-disabled default IS truthy. (2) A vitest jsdom characterization repro (tests/_repro/f45.test.ts, now deleted) that imports the REAL src/modules/buildings.ts, seeds the real `autoTrimpSettings` (never mocking getPageSetting \u2014 the jobs.ts/upgrades.ts precedent used by tests/buildi",
  "evidence": "CONFIRMED \u2014 the guard fires on the disabled default.\n\nSemantics (node):\n    parseFloat(-1) truthy? true | guard fires: true\n\nRepro run (`npx vitest run tests/_repro/f45.test.ts`) \u2014 3 passed / 1 failed:\n    \u2713 CONTROL: DecaBuild gives buyAmt=10 for a non-Gym building        (Hut \u2192 10)\n    \u2713 CONTROL: GymWall = 0 (the OTHER documented disable) \u2192 Gym buyAmt 10\n    \u00d7 BUG: GymWall = -1 (the DEFAULT, documented \"disable\") \u2192 Gym buyAmt 1\n        AssertionError: expected 1 to be 10\n        - Expected  10\n        + Received   1\n      \u276f tests/_repro/f45.test.ts:73\n    \u2713 BUG-adjacent: GymWall = 1 \u2192 buyAmt 1 (this one IS intended per the tooltip)\n\nThe two passing CONTROLs are the positive control for the ",
  "fix": "One-token guard-shape fix at src/modules/buildings.ts:85. NOT a balance number \u2014 no numeric tuning constant is touched; this changes a comparison operator on a user setting whose semantics are already documented in its own tooltip.\n\n    -    if (building === 'Gym' && getPageSetting('GymWall')) {\n   ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 46,
  "sev": "MEDIUM",
  "file": "src/modules/buildings.ts",
  "line": 120,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "Setting a housing Max to 0 is honoured everywhere EXCEPT buyFoodEfficientHousing, which reads 0 as \"unlimited\" and keeps buying",
  "instrument": "(1) Direct JS-semantics evaluation of the guard at src/modules/buildings.ts:120, and (2) a vitest jsdom L1b actuator spy-log repro (tests/_repro/f46.test.ts, modelled on tests/buildings.characterization.test.ts): real buildings.ts + real getPageSetting reading a seeded `autoTrimpSettings`, native `buyBuilding` spied. This instrument CAN see exactly what the finding claims \u2014 which housing name buyF",
  "evidence": "Guard (buildings.ts:117-127, buyFoodEfficientHousing over Hut/House/Mansion/Hotel/Resort):\n    if (game.buildings[b].owned < getPageSetting('Max' + b) || getPageSetting('Max' + b) < 1) return true;   // stays a buy candidate\nWith Max=0, owned=0:  `0 < 0` === false, but `0 < 1` === true  \u2192  eligible.\nSibling twin (buildings.ts:169, buyGemEfficientHousing): `owned < max || max == -1` \u2192 -1 is the sentinel, 0 correctly means \"none\".\n(RbuyFoodEfficientHousing:413 and mostEfficientHousing:468 also use the -1 sentinel. `< 1` is the lone outlier.)\n\nvitest run tests/_repro/f46.test.ts \u2014 1 failed | 2 passed:\n  \u2713 TRIPWIRE: owned<max === false, max<1 === true, (owned<max || max<1) === true\n  \u2713 POSITIVE ",
  "fix": "Mechanism-only (no game-balance number touched \u2014 this is a sentinel-semantics fix, not tuning).\n\nsrc/modules/buildings.ts:117-127, adopt the gem twin's normalize-then-compare form:\n\n    if (!ignoresLimit) {\n        unlockedHousing = unlockedHousing.filter(b => {\n            let max = getPageSetting(",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 47,
  "sev": "MEDIUM",
  "file": "src/modules/buildings.ts",
  "line": 200,
  "klass": "never-written",
  "numeric": false,
  "title": "\"Buy Warp to Hit Coord\" bypasses the Warpstation Cap/Wall even when no Coordination upgrade is available \u2014 the needCoord guard is computed and never u",
  "instrument": "Vitest (jsdom) L1b actuator spy-log on `buildings.buyGemEfficientHousing()`, modeled on the existing `tests/buildings.characterization.test.ts` harness: real module import, real `getPageSetting` reading a seeded `autoTrimpSettings`, native `buyBuilding` spied. This instrument CAN see exactly what matters here \u2014 the ordered native-mutator call log (`buyBuilding('Warpstation', \u2026)`), which is the one",
  "evidence": "1) STATIC \u2014 `needCoord` and `coordReplace` are written and read by NOTHING. `grep -rn \"needCoord\\|coordReplace\" src/` returns exactly two kinds of hit:\n   - src/modules/buildings.ts:200,201 \u2014 the two assignments (no reads anywhere in the function, module, or bundle: tests/fixtures/src-bundle.golden.js:3391-3392 carries them forward as dead consts).\n   - src/modules/coordinator.ts:56-57 \u2014 the SAME expression, and there it IS the guard:\n       const needCoord = game.upgrades.Coordination.allowed - game.upgrades.Coordination.done > 0;\n       if (!needCoord || game.buildings.Warpstation.locked || canAffordCoordinationTrimps()) return;\n     Two modules compute the identical predicate for the iden",
  "fix": "Pure mechanism fix \u2014 NO game-balance number is touched (no literal, no multiplier, no threshold changes).\n\nsrc/modules/buildings.ts:200-204 \u2014 use the guard that is already computed. Add `needCoord` to the condition, matching the sibling implementation in coordinator.ts:57 (`if (!needCoord || \u2026 || ca",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 48,
  "sev": "LOW",
  "file": "src/modules/buildings.ts",
  "line": 174,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "Three phantom settings in buildings.ts: the Gateway fragment wall and both \"Efficiency Ignores Max\" overrides are permanently dead",
  "instrument": "Two instruments, both able to see the region. (1) `node -e` on the pure semantics of `getPageSetting`'s unknown-key return. (2) A jsdom vitest repro (tests/_repro/f48.test.ts, now deleted) that imports the REAL src/modules/buildings.ts + REAL src/modules/utils.ts getPageSetting, seeds the real `autoTrimpSettings` global (never mocks getPageSetting), and spy-logs the native `buyBuilding` mutator \u2014 ",
  "evidence": "STATIC \u2014 the ids are never defined:\n  $ grep -c \"createSetting('GatewayWall'|createSetting('GemEfficiencyIgnoresMax'|createSetting('FoodEfficiencyIgnoresMax'\" src/modules/settings-defs.ts\n  0\n  $ grep -rn \"GatewayWall|IgnoresMax\" src/ legacy/   # ONLY read sites, zero define sites:\n  src/modules/buildings.ts:110:    const ignoresLimit = getPageSetting('FoodEfficiencyIgnoresMax')\n  src/modules/buildings.ts:169:        ... || (getPageSetting('GemEfficiencyIgnoresMax') && keysSorted[best] !== \"Gateway\")\n  src/modules/buildings.ts:174:            if (bestGemBuilding === \"Gateway\" && getPageSetting('GatewayWall') > 1) {\n  src/modules/buildings.ts:175:                if (getBuildingItemPrice(...) ",
  "fix": "Mechanism-only. Option A (zero-risk): DELETE the dead code \u2014 the `FoodEfficiencyIgnoresMax` read at buildings.ts:110 and the branches it gates (:117-127, :140), the `GemEfficiencyIgnoresMax` disjunct at :169, and the whole Gateway-wall block at :174-180. Byte-identical behavior, ~15 unreachable line",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 50,
  "sev": "MEDIUM",
  "file": "src/modules/calc.ts",
  "line": 889,
  "klass": "type-confusion",
  "numeric": false,
  "title": "`getPageSetting('highdmg') != undefined` is always true \u2014 the textValue default IS the string 'undefined'",
  "instrument": "Two instruments, both of which CAN see this code (no harness blindness):\n\n(a) `node -e \"console.log('undefined' != undefined)\"` \u2192 `true`. Pure JS semantics; sufficient to prove the guard is a tautology GIVEN that getPageSetting returns the string.\n\n(b) A vitest/jsdom repro (tests/_repro/f50.test.ts, since deleted) that exercises the REAL chain with nothing faked in the settings store: the REAL `cr",
  "evidence": "1) THE SETTING'S DEFAULT IS THE 9-CHARACTER STRING \"undefined\", NOT undefined.\n\n   settings-defs.ts:751 \u2014 createSetting('highdmg', 'WS: High Damage', ..., 'textValue', 'undefined', null, 'Heirlooms')\n   settings-defs.ts:157 \u2014 same for 'dhighdmg' (DHS: High Damage).\n   settings-engine.ts (textValue arm) \u2014 `value: loaded === undefined ? defaultValue : loaded`\n   utils.ts:66-67 \u2014 `else if (autoTrimpSettings[setting].type == 'textValue') return autoTrimpSettings[setting].value;`\n\n   Through the REAL engine + REAL getPageSetting, the repro observed:\n     typeof getPageSetting('highdmg')  === 'string'   (PASS)\n            getPageSetting('highdmg')  === 'undefined' (PASS)\n\n2) THE GUARD IS DEAD. The",
  "fix": "Pure MECHANISM fix \u2014 touches no game-balance number. No damage/health/cost/multiplier literal is read, written, or changed; the only edit is to a predicate that decides whether an existing code block runs at all.\n\nThe root cause is a sentinel-type confusion: the \"unset\" sentinel for these two textVa",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 51,
  "sev": "MEDIUM",
  "file": "src/modules/calc.ts",
  "line": 1739,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "getTotalHealthMod is missing Scruffy scaledHealth / Antenna array / Smithless \u2014 its RcalcOurHealth twin has all three",
  "instrument": "Layer-1 vitest characterization harness (jsdom env, `makeMinimalGame`/`deepMerge` from tests/harness/gameFixture.ts), calling the REAL exported `RcalcOurHealth()` and `getTotalHealthMod()` from src/modules/calc.ts against a hand-authored U2 `game` fixture. It can see exactly what equipment.ts:1081 sees: the ratio `RcalcOurHealth() / (getTotalHealthMod() * maxSoldiers)`, which the call site consume",
  "evidence": "INVARIANT UNDER TEST (what equipment.ts:1081 requires):\n  RcalcOurHealth() / (getTotalHealthMod() * maxSoldiers) === rawEquipmentHealth\nFixture: rawEquipmentHealth = 50 + (Shield healthCalculated 100 x level 5) = 550; maxSoldiers = 10.\n\nnpx vitest run tests/_repro/f51.test.ts\n  Tests  3 failed | 2 passed (5)\n\nPOSITIVE CONTROLS (both PASSED \u2014 the instrument goes green when there is no asymmetry):\n  \u2713 POSITIVE CONTROL a: all-neutral fixture -> invariant holds exactly (550 === 550)\n  \u2713 POSITIVE CONTROL b: a multiplier present in BOTH functions (Toughness radLevel 2 x modifier 1\n    => getTotalHealthMod() === 3) is correctly inverted -> 550. So the harness does not flag every\n    multiplier, onl",
  "fix": "Pure MECHANISM fix \u2014 no game-balance number is invented or changed. Every constant added is COPIED from\nthe already-shipped, already-correct twin `RcalcOurHealth` (which itself mirrors the game). Add the three\n(ideally four) missing multipliers to `getTotalHealthMod` (src/modules/calc.ts:1726-1789) ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 52,
  "sev": "MEDIUM",
  "file": "src/modules/coordinator.ts",
  "line": 76,
  "klass": "gate",
  "numeric": false,
  "title": "computeTopTarget's buy runs outside the BuyBuildingsNew dispatch AND outside the !usingRealTimeOffline gate in mainLoop",
  "instrument": "vitest (jsdom) integration repro at tests/_repro/f52.test.ts, now deleted. It wired the REAL src/modules/coordinator.ts `computeTopTarget` and the REAL src/modules/buildings.ts `safeBuyBuilding` (bridged by bare name, as the bundle does) and spied ONLY the native game mutator `buyBuilding`, capturing `game.global.buyAmt` / `maxSplit` AT CALL TIME (the buildings.characterization.test.ts L1b precede",
  "evidence": "Both assertions failed against current code (`npx vitest run tests/_repro/f52.test.ts` \u2192 Test Files 1 failed, Tests 2 failed):\n\nCASE 1 \u2014 BuyBuildingsNew = 0 (\"Buy Neither\"), PurchaseCoordinator ON, usingRealTimeOffline = false:\n  AssertionError: expected [ { building: 'Warpstation', \u2026(2) } ] to deeply equal []\n  + [ { \"building\": \"Warpstation\", \"buyAmt\": \"Max\", \"maxSplit\": 1 } ]\n  and `buyBuildingsCalls` was 0 \u2014 i.e. the user's setting suppressed EVERY building buyer, yet a\n  Warpstation was still purchased, at buyAmt='Max' (Warpstation.owned < 2 \u2192 buildings.ts:88-92 forces\n  game.global.buyAmt = 'Max', maxSplit = 1), spending the maximum affordable metal.\n\nCASE 2 \u2014 usingRealTimeOffline = tr",
  "fix": "Mechanism-only; touches no balance number.\n\nTwo independent escapes, two independent guards:\n\n1. usingRealTimeOffline \u2014 move the `computeTopTarget();` call at legacy/AutoTrimps2.js:208 INSIDE the existing `if (!usingRealTimeOffline) {` block at :211 (as its first statement). The coordinator is a per",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 53,
  "sev": "MEDIUM",
  "file": "src/modules/dynprestige.ts",
  "line": 10,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "U2 Dynamic Prestige does not exist: RprestigeChanging2 has zero callers, reads the phantom setting 'RDynamicPrestige2', and targets an 'RPrestige' set",
  "instrument": "Three instruments, all of which CAN see this region:\n(a) Static grep over src/ + legacy/ for call sites and createSetting ids \u2014 sufficient because the claim \"zero callers / no createSetting\" is a whole-repo textual property, and dynprestige.ts's two functions are reached only by name (no dynamic dispatch; the bridge does `Object.assign(globalThis, {...module})` so any caller would have to spell `R",
  "evidence": "LEG 1 \u2014 ZERO CALLERS (static, whole-repo):\n  $ grep -rn \"prestigeChanging2\" src legacy tests scripts\n  src/modules/dynprestige.ts:9    export function prestigeChanging2(){...}\n  src/modules/dynprestige.ts:10   export function RprestigeChanging2(){\n  legacy/AutoTrimps2.js:245       if (getPageSetting('DynamicPrestige2') > 0 && forcePrecZ) prestigeChanging2();\n  (remaining hits are tests/fixtures/*.golden.js + the oracle bundle \u2014 i.e. the bridge re-export\n   `RprestigeChanging2: () => RprestigeChanging2`, not a call.)\n  The repro's test (1) walked every .ts/.js in src/ + legacy/ and matched /\\bRprestigeChanging2\\s*\\(/ :\n      AssertionError: expected [] to not deeply equal []      \u2190 the caller",
  "fix": "Pure mechanism \u2014 NO game-balance number is touched anywhere in either option. Two coherent fixes; they are mutually exclusive and the choice is a product call:\n\nOPTION A \u2014 DELETE THE DEAD CODE (smallest, zero behavior change; recommended if U2 Dynamic Prestige is not a wanted feature):\n  1. src/modu",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 54,
  "sev": "MEDIUM",
  "file": "src/modules/equipment.ts",
  "line": 985,
  "klass": "other",
  "numeric": false,
  "title": "RautoEquip's do-while never loops \u2014 game's buyEquipment() returns undefined, so U2 gear buys exactly 1 level per stat per tick",
  "instrument": "TWO instruments, both of which can see this region.\n\n(1) DIRECT EXECUTION OF THE REAL GAME FUNCTION. I extracted `buyEquipment` verbatim from ../trimps-game/main.js (lines 16730-16745, the authoritative source) and ran it in a node `vm` context with a rich stub env (canAffordBuilding\u2192true, levelEquipment recorded). This instrument can observe exactly the thing in question: the function's return va",
  "evidence": "\u2550\u2550\u2550 EVIDENCE 1 \u2014 the real game function returns undefined even when the buy SUCCEEDS \u2550\u2550\u2550\nRan ../trimps-game/main.js:16730-16745 verbatim in a node vm:\n\n  levelEquipment called (the buy SUCCEEDED): [[\"Dagger\",1]]\n  buyEquipment returned: undefined | typeof: undefined | truthy: false\n\nThe source confirms it \u2014 every `return` is a bare early-exit and the function falls off the end:\n\n  function buyEquipment(what, confirmed, noTip, forceAmt) {\n      if (game.options.menu.pauseGame.enabled) return;\n      if (!confirmed && ... ) return;\n      var toBuy = game.equipment[what];\n      ...\n      if (typeof toBuy === 'undefined') return;\n      var canAfford = canAffordBuilding(what, false, false, true, f",
  "fix": "MECHANISM-ONLY. Touches no game-balance number, no cost/damage/multiplier literal, no setting default.\n\nThe loop needs a success signal that the game actually provides. `buyEquipment` mutates `game.equipment[name].level`, so read that:\n\n  // src/modules/equipment.ts, inside RautoEquip's buy do-while",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 55,
  "sev": "MEDIUM",
  "file": "src/modules/equipment.ts",
  "line": 207,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Block-stat equipment (Shield after Shieldblock) reaches NEITHER level-cap branch \u2014 silently hard-capped at the literal 100 instead of CapEquiparm",
  "instrument": "Vitest unit repro (tests/_repro/f55.test.ts, jsdom env) calling the REAL exported `evaluateEquipmentEfficiency` from src/modules/equipment.ts against a `makeMinimalGame` fixture, with the real `getPageSetting` reading a seeded `autoTrimpSettings` (never mocked) \u2014 the tests/equipment.characterization.test.ts precedent. This instrument observes exactly the region under test: the `cap` dispatch at eq",
  "evidence": "`npx vitest run tests/_repro/f55.test.ts` \u2192 6 tests, 3 passed / 3 failed. The 3 passes are the instrument's positive controls; the 3 failures are the bug.\n\nPASSING (instrument proves it can see this exact code):\n  \u2713 TRIPWIRE: harness tracks the CapEquiparm value, not a constant \u2014 (cap=3,lvl=2)\u2192Wall false; (cap=3,lvl=3)\u2192true; (cap=50,lvl=3)\u2192false; (cap=50,lvl=50)\u2192true.\n  \u2713 CONTROL A: Boots (Stat='health'), level 10, CapEquiparm=10 \u2192 Wall=true, Factor=0.\n  \u2713 CONTROL B: Shield WITHOUT blockNow (Stat='health'), level 10, CapEquiparm=10 \u2192 Wall=true.\n\nFAILING (same function, same fixture, same cap, only blockNow flipped):\n  \u00d7 BUG 1 \u2014 Shield WITH blockNow (Stat='block'), level 10, CapEquiparm=10:\n ",
  "fix": "Mechanism fix (a dispatch hole, not a tuning knob) \u2014 route the block Stat to the same Armor cap the Shield already used before Shieldblock, in equipment.ts:207:\n\n    if (equipmentList[equipName].Stat === 'health' || equipmentList[equipName].Stat === 'block')\n        cap = getPageSetting('CapEquiparm",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 56,
  "sev": "MEDIUM",
  "file": "src/modules/equipment.ts",
  "line": 414,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Gymystic prestige is gated on the WEAPONS setting (its own tooltip says Armor) \u2014 'Weapons: Buy Neither' freezes ALL Gym automation, including Gym buil",
  "instrument": "Vitest jsdom L1b spy-log on the real `autoLevelEquipment()` (modelled on tests/equipment.characterization.test.ts): the module is imported for real, `getPageSetting`/`debug` are the real imports reading a seeded `autoTrimpSettings`, and the native mutators `buyUpgrade`/`buyEquipment` are spies that record their ordered call log. This instrument CAN see exactly what the finding is about \u2014 every pre",
  "evidence": "POSITIVE CONTROL (passes) \u2014 with `BuyWeaponsNew=1` (\"Weapons: Buy Both\") and `BuyArmorNew=0` (\"Armor: Buy Neither\"), Gymystic unlocked and every other prestige locked:\n    buyUpgradeCalls === [['Gymystic', true, true]]   \u2705 the harness DOES observe the buy at equipment.ts:432.\n\nBUG (fails) \u2014 flip the two settings to `BuyWeaponsNew=0` (\"Weapons: Buy Neither\") + `BuyArmorNew=1` (\"Armor: Buy Both\"), identical game state otherwise:\n    FAIL  #56 > BUG \u2014 Armor: Buy Both + Weapons: Buy Neither buys NOTHING\n    AssertionError: expected [] to deeply equal [ [ 'Gymystic', true, true ] ]\n    Received: []\nGymystic is never bought, even though the Armor setting is fully ON. Its own tooltip (src/modules/s",
  "fix": "Pure dispatch/mechanism fix \u2014 no game-balance number is touched.\n\nAt src/modules/equipment.ts:413-415, route the 'block' Stat (Gym/Gymystic) through the ARMOR setting so it matches (a) its own tooltip at settings-defs.ts:331 and (b) the *levels* loop 40 lines below (equipment.ts:457) which already t",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 57,
  "sev": "MEDIUM",
  "file": "src/modules/equipment.ts",
  "line": 358,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "Phantom settings 'loomswap' / 'dloomswap' \u2014 the high-damage-heirloom damage compensation in autoLevelEquipment is permanently dead (calc.ts applies it",
  "instrument": "Three instruments, all in-repo vitest (jsdom env), plus shell grep:\n(1) CATALOG RUN \u2014 imported the REAL `src/modules/settings-defs.ts` and executed `initializeAllSettings()` with a recording `createSetting` (the settings-inventory.test.ts idiom). This can see exactly which setting ids the shipped catalog declares. It recorded 5xx ids.\n(2) REAL `getPageSetting` from `src/modules/utils.ts` against a",
  "evidence": "A. CATALOG (positive control built in): the real initializeAllSettings() run recorded >500 ids.\n   expect(ids).toContain('highdmg')    \u2192 PASS   (real sibling setting exists \u2014 instrument works)\n   expect(ids).toContain('dhighdmg')   \u2192 PASS\n   expect(ids).toContain('AutoStance') \u2192 PASS\n   expect(ids).not.toContain('loomswap')  \u2192 PASS  (phantom)\n   expect(ids).not.toContain('dloomswap') \u2192 PASS  (phantom)\n   grep -rn \"createSetting('d\\?loomswap\" src/ \u2192 NONE. Never existed in any revision.\n\nB. SEMANTICS, real getPageSetting, on a settings object with AutoStance=3 / highdmg='BigLoom' / use3daily=true / dhighdmg set (i.e. the exact heirloom-swap setup a user CAN configure):\n   getPageSetting('looms",
  "fix": "Mechanism-only; NO game-balance number is touched (trimpAA is the game's own heirloom trimpAttack multiplier; no literal changes).\n\nsrc/modules/equipment.ts:358-361 \u2014 replace the two phantom guards with the guards calc.ts already uses for the same compensation:\n    if (getPageSetting('AutoStance') =",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 58,
  "sev": "MEDIUM",
  "file": "src/modules/equipment.ts",
  "line": 457,
  "klass": "gate",
  "numeric": false,
  "title": "`MaxMapBonusAfterZone` = 0 ('use it always', per its own tooltip) is falsy \u2014 the max-map-bonus gear-to-cap override never fires",
  "instrument": "Two instruments, both able to see the region.\n\n(1) Static type-chain read. `MaxMapBonusAfterZone` is declared `createSetting(..., 'value', '-1', ...)` (settings-defs.ts:389). `getPageSetting` (utils.ts:68-69) routes `type == 'value'` through `parseFloat(...)` \u2192 a NUMBER. So the value 0 arrives at equipment.ts:457 as the number `0`, which is falsy.\n\n(2) Vitest repro driving the REAL `equipment.auto",
  "evidence": "The two runs are BYTE-IDENTICAL fixtures. `doMaxMapBonus = true` in both. `enoughHealthE` is TRUE in both (calcOurHealth 1e9). The ONLY difference is the string stored in the setting: '1' vs '0'.\n\n  \u2713 POSITIVE CONTROL: with the setting at 1, the override fires and armor is levelled to cap\n      \u2192 buyEquipment log = ['Shield', 'Boots']\n  \u00d7 BUG: with the setting at 0 (\"use it always\"), doMaxMapBonus is still TRUE but NO armor is bought\n      AssertionError: expected [] to deeply equal [ 'Shield', 'Boots' ]\n      - Expected            + Received\n      - [ \"Shield\", \"Boots\" ]   + []\n\n  Test Files  1 failed (1)\n       Tests  1 failed | 3 passed (4)\n\nThe two supporting tripwires both PASSED, pinni",
  "fix": "Mechanism-only, no game-balance number touched. One line, equipment.ts:457 \u2014 make the guard match its own sibling's `>= 0` semantics (maps.ts:476):\n\n    -    const maxmap = getPageSetting('MaxMapBonusAfterZone') && doMaxMapBonus;\n    +    const maxmap = getPageSetting('MaxMapBonusAfterZone') >= 0 &&",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 59,
  "sev": "LOW",
  "file": "src/modules/fight-info.ts",
  "line": 162,
  "klass": "type-confusion",
  "numeric": false,
  "title": "Nature-token glyphs (Poison/Wind/Ice) are dead code \u2014 they test cell.name, but the nature lives in cell.empowerment",
  "instrument": "Two instruments. (1) Node VM over ../trimps-game/config.js to enumerate the authoritative enemy-name universe (game.badGuys keys) \u2014 this can see exactly what values cell.name can ever hold. (2) A jsdom vitest repro (tests/_repro/f59.test.ts, since deleted) that imports src/modules/fight-info.ts (a side-effect IIFE registering MODULES.fightinfo), injects a game.global.gridArray of real populateGrid",
  "evidence": "1) The guards can never be true. Game source is authoritative:\n   - main.js:10037 populateGrid: `name: getRandomBadGuy(...)` \u2014 cell.name is a KEY of game.badGuys (getRandomBadGuy returns `selected`, a `for (var item in game.badGuys)` key).\n   - Node VM over config.js: `badGuy count: 85`; `names.filter(n=>/ice|wind|poison/i.test(n))` -> `[]`. No enemy name contains ice/wind/poison.\n   - The nature lives elsewhere: main.js:10051 `newCell.empowerment = getEmpowerment(-1)`; main.js:8203 `var activeEmpowerments = [\"Poison\",\"Wind\",\"Ice\"]`; main.js:10572-74 drawGrid: `if (cell.empowerment) { className.push(`empoweredCell${cell.empowerment}`); title = `Token of ${cell.empowerment}` }`.\n   So fight-i",
  "fix": "Mechanism-only, no numbers touched. In src/modules/fight-info.ts, replace the three token guards (lines ~162/171/180) with reads of the field the game actually sets:\n  if (cell.empowerment === 'Poison') { ... glyphicon-flask ... }\n  if (cell.empowerment === 'Wind')   { ... icon-air ... }\n  if (cell.",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 60,
  "sev": "LOW",
  "file": "src/modules/fight-info.ts",
  "line": 153,
  "klass": "gate",
  "numeric": false,
  "title": "Fast-imp branch overwrites cell innerHTML unconditionally (guard commented out), destroying the game's unlock/special icon for the rest of the zone",
  "instrument": "The REAL Trimps clone (../trimps-game v5.10.1) + the REAL built AT bundle (dist/autotrimps.user.js), booted together into jsdom via `bootGame({ withAutoTrimps: true })` from scripts/sim/boot.mjs, driven from a one-off vitest file. This instrument executes the actual `MODULES.fightinfo.Update()` against a grid produced entirely by the game's own `buildGrid() -> addSpecials() -> drawGrid()` pipeline",
  "evidence": "Vitest run \u2014 1 failed (the bug), 1 passed (the positive control):\n\n  \u2713 POSITIVE CONTROL: a POWERFUL imp on the special cell PRESERVES the unlock icon\n  \u00d7 BUG: a FAST imp on the same special cell DESTROYS the unlock icon\n\nConsole output from the BUG case (zone 5, the game itself placed the special):\n\n  BUG special=groundLumber\n      text=<span aria-label='Wood' title=\"Wood\"  class='glyphicon glyphicon-tree-deciduous '></span>\n  BUG before=\"<span aria-label=\\\"Wood\\\" title=\\\"Wood\\\" class=\\\"glyphicon glyphicon-tree-deciduous \\\"></span>\"\n       after=\"<span class=\\\"glyphicon glyphicon-forward\\\"></span>\"\n  BUG titles before=\"\" after=\"Chickimp\"\n\n  AssertionError: expected '<span class=\"glyphicon gl",
  "fix": "Mechanism-only, one line, zero numeric/balance content. In src/modules/fight-info.ts, uncomment the guard so the fast branch matches its six siblings:\n\n    else if(M[\"fightinfo\"].fast.indexOf(cell.name) > -1)     // Fast imp\n    {\n        if(cell.special.length === 0)                        // <-- r",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 61,
  "sev": "MEDIUM",
  "file": "src/modules/utils.ts",
  "line": 54,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Shipped \"550+ AT Settings\" preset sets BetterAutoFight=3 \u2014 an out-of-range index that reaches NO dispatch branch, killing all AutoFight management",
  "instrument": "jsdom vitest repro (tests/_repro/f61.test.ts, now deleted) driving the REAL production code path end-to-end: `serializeSettings550()` (utils.ts:53) \u2192 `JSON.parse` (what `loadAutoTrimps`/`resetAutoTrimps` do, import-export.ts:889-890) \u2192 the real `createSetting(...)` (settings-engine.ts:177-186, multitoggle branch) \u2192 the real `getPageSetting()` (utils.ts:70). The dispatch table and the option-name l",
  "evidence": "All pre-assertions PASSED (option list has exactly 3 entries; dispatch table extracted from legacy/AutoTrimps2.js is exactly [1, 2]; `serializeSettings550()` really does carry `\"BetterAutoFight\":3`; `getPageSetting('BetterAutoFight')` really does return 3 after the real import path \u2014 no clamp anywhere). Then the two bug assertions FAILED, which is the proof:\n\n  FAIL  f61 \u00b7 the preset value survives import with no clamp, and no dispatch branch handles it\n  AssertionError: expected [ 1, 2 ] to include 3\n   \u276f tests/_repro/f61.test.ts:56:21\n       55|     // THE BUG: value 3 reaches neither branch \u2192 AutoFight management is dead\n       56|     expect(handled).toContain(getPageSetting('BetterAutoF",
  "fix": "Two independent parts; NEITHER is a game-balance number (the `3` is a UI dispatch/option index, not tuning \u2014 no sacrosanct-tuning gate applies).\n\n1. DATA fix (the actual defect): src/modules/utils.ts:54, in `serializeSettings550()`, change `\"BetterAutoFight\":3` \u2192 `\"BetterAutoFight\":2` (index 2 = 'Va",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 62,
  "sev": "MEDIUM",
  "file": "src/modules/gather.ts",
  "line": 305,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "Daily Time Farm has no gather branch \u2014 the 'DTF: Gather Selection' setting (Rdtimefarmgather) is defined, editable, and consumed by nobody",
  "instrument": "Vitest + jsdom actuator spy-log, modelled on tests/gather.characterization.test.ts (the existing net for this exact function). RmanualLabor2's return is void; its CONTRACT is the ordered setGather(what) call log, so I spied globalThis.setGather / safeBuyBuilding and drove the else-if ladder with makeMinimalGame() from tests/harness/gameFixture.ts. WHAT THIS INSTRUMENT CAN SEE: every arm of Rmanual",
  "evidence": "$ npx vitest run tests/_repro/f62.test.ts\n\n \u2713 POSITIVE CONTROL: the NON-daily twin (Rshouldtimefarm) DOES honour Rtimefarmgather\n \u00d7 BUG: the DAILY twin (Rdshouldtimefarm) IGNORES Rdtimefarmgather and falls through\n   AssertionError: expected [ 'metal' ] to deeply equal [ 'wood' ]\n   - Expected\n   + Received\n     [\n   -   \"wood\",\n   +   \"metal\",\n     ]\n\nIdentical fixture, identical settings shape, identical world/zone-index. The only difference is which of the two flags mapfunctions.ts armed. With Rshouldtimefarm the instrument sees setGather('wood') (the user's DTF choice honoured). With Rdshouldtimefarm \u2014 the flag mapfunctions.ts:455 sets when `daily` is true \u2014 the TIMEFARM arm is skipped e",
  "fix": "Pure mechanism \u2014 no numeric/balance literal is touched.\n\nIn src/modules/gather.ts, add the missing daily arm to RmanualLabor2's else-if ladder immediately after the existing `else if (Rshouldtimefarm)` block (which ends ~line 320), mirroring the twin exactly and swapping the two setting ids (Rtimefa",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 63,
  "sev": "MEDIUM",
  "file": "src/modules/gather.ts",
  "line": 349,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "U2 'Mining/Building Only' (RManualGather2 == 2) still gathers science \u2014 one of the three science branches is unguarded",
  "instrument": "Vitest L1b actuator spy-log (jsdom env), modeled on tests/gather.characterization.test.ts. It calls gather.RmanualLabor2() directly against a makeMinimalGame() fixture with globalThis spies on the native mutators setGather(what) + safeBuyBuilding(name), and asserts the exact ordered call log. WHAT IT CAN SEE: every arm of RmanualLabor2's else-if ladder, the getPageSetting('RManualGather2') reads, ",
  "evidence": "Run: `npx vitest run tests/_repro/f63.test.ts` \u2192 Tests: 1 failed | 3 passed (4).\n\nTHE FAILURE (the bug):\n  FAIL  #63 \u2014 RManualGather2 == 2 (\"Mining/Building Only\") must never gather science\n        > science below 80% of RscienceNeeded \u2192 leaks into the unguarded science arm (gather.ts:349)\n  AssertionError: expected [ 'science' ] to not include 'science'\n   \u276f tests/_repro/f63.test.ts:103:29\n     expect(gatherCalls).not.toContain('science')\n\nFixture: settled mid-game U2 (world 50, traps full, no farm mode armed), RscienceNeeded=1000, science.owned=500, RManualGather2=2 (\"Mining/Building Only\"). Result: setGather('science'). Expected for mining mode: metal/buildings, never science.\n\nTHE POSITI",
  "fix": "Mechanism-only fix, ONE line, NO game-balance number touched (the 0.8 factor and RminScienceAmount stay exactly as they are \u2014 only the mode guard is added).\n\nsrc/modules/gather.ts:349 \u2014 add the same guard its two sibling arms already carry:\n\n  else if (getPageSetting('RManualGather2') != 2 &&\n      ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 64,
  "sev": "MEDIUM",
  "file": "src/modules/heirlooms.ts",
  "line": 599,
  "klass": "never-reset",
  "numeric": false,
  "title": "#49's `rarity < 10` early return excludes rarity 9 (Radiating) \u2014 the ONLY rarity that can roll Gamma Burst \u2014 and leaves gammaBurstPct/shieldEquipped s",
  "instrument": "A vitest repro (tests/_repro/f64.test.ts, node env) that boots the REAL Trimps clone (../trimps-game v5.10.1) via scripts/sim/boot.mjs \u2014 so `getHeirloomBonus`, `recalculateHeirloomBonuses`, `getHazardGammaBonus` and the whole `game` object are the game's own code, not stubs \u2014 then imports src/modules/heirlooms.ts and calls `HeirloomShieldSwapped()` across the seam (globalThis.game / globalThis.get",
  "evidence": "$ npx vitest run tests/_repro/f64.test.ts\n \u276f tests/_repro/f64.test.ts (3 tests | 2 failed) 168ms\n   \u2713 THE GAME: a rarity-9 shield with a rolled gammaBurst mod has a POSITIVE bonus (so the game bursts)\n   \u00d7 AT (a): swapping TO a rarity-9 gamma shield never credits it \u2014 gammaBurstPct stays stale\n     AssertionError: expected 1 to be 15   // gammaBurstPct stayed at the no-gamma default\n   \u00d7 AT (b): swapping FROM a rarity-10 gamma shield TO a rarity-9 non-gamma shield keeps the OLD gammaBurstPct forever\n     AssertionError: expected 239.99999999999997 to be 1   // still carrying the Hazardous shield's innate gammaMult\n\nThree facts, all read out of the real game:\n1. game returns bonus 1500 (>0) f",
  "fix": "Mechanism-only; NO game-balance numbers touched.\n\nsrc/modules/heirlooms.ts:595-602 \u2014 DELETE the `if (game.global.ShieldEquipped.rarity < 10) return;` early return. The game's own gate is the bonus itself (main.js:16022: `getHeirloomBonus('Shield','gammaBurst') > 0`), and getHeirloomBonus already han",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 65,
  "sev": "MEDIUM",
  "file": "src/modules/heirlooms.ts",
  "line": 575,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "Rdheirloomswap (Daily) reads the Rdhs* settings but calls the NON-daily Rhs* equip functions \u2014 all five Rdhs*equip fns are dead code",
  "instrument": "vitest (jsdom) unit repro at tests/_repro/f65.test.ts, dynamic-importing the real `Rdheirloomswap` from src/modules/heirlooms.ts with a real `autoTrimpSettings` shape (getPageSetting reads .enabled/.value by .type \u2014 utils.ts:57-72) and spies on the two native actuators `selectHeirloom`/`equipHeirloom`. It CAN see this region: the function is a pure settings-read \u2192 native-call actuator with no time",
  "evidence": "Both repro assertions FAIL against current code:\n\n FAIL tests/_repro/f65.test.ts > A: daily settings configured, regular settings at default \u2192 equips NOTHING\n AssertionError: expected \"vi.fn()\" to be called at least once\n   at expect(equipHeirloom).toHaveBeenCalled()\n\n FAIL tests/_repro/f65.test.ts > B: with a regular shield configured, the Daily swap equips the REGULAR one\n AssertionError: expected \"vi.fn()\" to be called with arguments: [ +0, 'heirloomsCarried', true ]\n Received: 1st vi.fn() call: [ 1, \"heirloomsCarried\", true ]     // 0 = DailyShield, 1 = RegularShield\n Number of calls: 1\n\nCase A: Rdhsshield=on, Rdhsz=50, Rdhs1/Rdhs2='DailyShield', world=60, 'DailyShield' carried, Rhs1/Rhs",
  "fix": "Mechanism-only, no numeric/balance change. In src/modules/heirlooms.ts:570-593, swap the five dispatches inside `Rdheirloomswap()` to their existing daily twins:\n  Rhsequip1()            -> Rdhsequip1()\n  Rhsequip2()            -> Rdhsequip2()\n  Rhsworldstaffequip()   -> Rdhsworldstaffequip()\n  Rhsm",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 66,
  "sev": "HIGH",
  "file": "src/modules/heirlooms.ts",
  "line": 589,
  "klass": "never-written",
  "numeric": false,
  "title": "Rdshouldtributefarm is read but NEVER written \u2014 the daily tribute-staff branch is dead, and setting a daily tribute staff kills ALL in-map staff swapp",
  "instrument": "vitest (jsdom) unit repro at tests/_repro/f66.test.ts, driving the REAL `Rdheirloomswap` export from src/modules/heirlooms.ts through the real `getPageSetting` seam (injected `autoTrimpSettings`, NOT a stubbed getPageSetting), with `selectHeirloom`/`equipHeirloom` as vi.fn() spies. This instrument CAN see: which of the two staff branches executes, whether a native equip actually fires, and whether",
  "evidence": "REPRODUCED \u2014 and the real defect is STRONGER than the finding claimed. The finding said the flag is \"always `undefined`\" so the branches silently evaluate false. That is wrong: converted modules read AT globals as FREE IDENTIFIERS, and a global that was never assigned is not `undefined` \u2014 it is a **ReferenceError** (true in sloppy AND strict mode; only *writes* differ). So `Rdheirloomswap()` THROWS.\n\nTest run (7 tests: 5 controls PASS, 2 bug tests FAIL):\n\n  \u2713 the global does not exist: reading it is a ReferenceError, not `undefined`\n  \u2713 POSITIVE CONTROL: define the flag = true  -> the tribute branch fires (1 equip)\n  \u2713 POSITIVE CONTROL: define the flag = false -> the map branch fires (1 equi",
  "fix": "MECHANISM ONLY \u2014 no game-balance number is touched.\n\nRecommended: at heirlooms.ts:586 and :589, read the flag that is actually maintained \u2014 `Rshouldtributefarm` \u2014 instead of the phantom `Rdshouldtributefarm`. A Daily run is not a separate tribute-farm state machine: `Rshouldtributefarm` is set/clear",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 67,
  "sev": "HIGH",
  "file": "src/modules/calc.ts",
  "line": 361,
  "klass": "parity",
  "numeric": true,
  "title": "PARITY: Gamma Burst is ADDITIVE in the game, but AT amortizes it as a replacement \u2014 (pct+1)/5 instead of (pct+5)/5",
  "instrument": "Two instruments, both able to see the region.\n(1) READ THE GAME \u2014 /Users/matt/dev/MattAltermatt/AutoTrimps/../trimps-game v5.10.1, main.js fight loop + calcHeirloomBonus + config.js heirloom def. This is the authoritative formula for a parity finding.\n(2) A vitest repro at tests/_repro/f67.test.ts (jsdom, modelled on tests/calc.characterization.test.ts + tests/harness/gameFixture.ts) that calls th",
  "evidence": "GAME TRUTH (../trimps-game v5.10.1):\n\n  main.js:15996 (fight loop, every attack)      cell.health -= trimpAttack;\n  main.js:16022-16033 (SAME tick, AFTER that):\n      if (game.global.soldierHealth > 0 && getHeirloomBonus('Shield', 'gammaBurst') > 0) {\n          let burst = game.heirlooms.Shield.gammaBurst;\n          burst.stacks++;\n          let triggerStacks = autoBattle.oneTimers.Burstier.owned ? 4 : 5;\n          if (Fluffy.isRewardActive('scruffBurst')) triggerStacks--;\n          if (burst.stacks >= triggerStacks) {\n              burst.stacks = triggerStacks;\n              if (cell.health > 0) {\n                  let burstDamage = calcHeirloomBonus('Shield', 'gammaBurst', trimpAttack);\n  ",
  "fix": "\ud83d\udeab NOT PROPOSING A NUMERIC CHANGE. Every candidate fix here edits a game-balance formula literal in\nAT's combat prediction \u2014 `(gammaBurstPct + 1) / 5` and `/ 4` at calc.ts:361/366 and the U2 twins at\ncalc.ts:1282/1287 \u2014 which is exactly the sacrosanct-tuning class. Flagging for a user decision only.\n",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 68,
  "sev": "MEDIUM",
  "file": "src/modules/jobs.ts",
  "line": 213,
  "klass": "brace-scope",
  "numeric": false,
  "title": "Magmamancer-locked early return also kills the job-protection safety net (dead for all of U1 pre-Magma)",
  "instrument": "vitest (jsdom env) unit repro importing src/modules/jobs.ts directly and calling the real buyJobs() with an injected `game` in the over-employed rounding-error state. The observed channel is a spy on the native global `buyJob` \u2014 which is the ONLY actuator safeFireJob/safeBuyJob reach (jobs.ts:41,48,80), so the spy sees every hire/fire buyJobs() performs, including the protection block's fingerprin",
  "evidence": "$ npx vitest run tests/_repro/f68.test.ts\n\n \u2713 POSITIVE CONTROL: Magmamancer UNLOCKED \u2192 protection fires 2 Farmers/Lumberjacks/Miners\n FAIL  BUG: Magmamancer LOCKED (all of U1 pre-Magma) \u2192 identical state, protection NEVER fires\nAssertionError: expected [] to deeply equal [ 'Farmer', 'Lumberjack', 'Miner' ]\n- Expected\n+ Received\n- [\n-   \"Farmer\",\n-   \"Lumberjack\",\n-   \"Miner\",\n- ]\n+ []\n \u276f tests/_repro/f68.test.ts:95:51\n\n Test Files  1 failed (1)\n      Tests  1 failed | 1 passed (2)\n\nThe passing test IS the positive control: with the SAME game state and only `Magmamancer.locked` flipped to false (and AutoMagmamancers OFF, so the magmamancer branch itself is inert), the protection block execute",
  "fix": "Pure control-flow move \u2014 NO numeric/tuning constant is touched, so this ships without a balance ask.\n\nIn src/modules/jobs.ts, replace the bare early return at :213 with a block scope around the magmamancer logic, so the universal job-protection net at :247-259 always runs:\n\n    -    if (game.jobs.Ma",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 69,
  "sev": "LOW",
  "file": "src/modules/jobs.ts",
  "line": 126,
  "klass": "other",
  "numeric": false,
  "title": "Watch challenge: scientist count is double-subtracted, so AT stalls at HALF its own scientist target",
  "instrument": "L1 unit repro: `tests/_repro/f69.test.ts` (jsdom, vitest) calling the real `jobs.buyJobs()` against a `makeMinimalGame()` fixture with `challengeActive('Watch')`, plus a MUTATING `buyJob` spy (hire/fire actually moves `jobs[X].owned` + `resources.trimps.employed`, like the native one) so multi-tick convergence is observable \u2014 not just a single-call arg log. What it CAN see: the ordered native `buy",
  "evidence": "POSITIVE CONTROL + BUG, same test, same region:\n\n1) Buy amount is `target - 2*owned` (confirmed, FAILING assertion):\n   Fixture: realMax 1000, Farmer 100 / Lumberjack 30 / Miner 50 / Scientist 10, employed 190.\n   freeWorkerSlots = ceil(1000/2) - 190 = 310; tdw = 310+100+30+50 = 490; scientistRatio2 = 10 \u2192 AT's own target = 49.\n   Correct first hire = 49 - 10 = 39.\n     AssertionError: expected 28 to be 39   (jobs.ts:128 `toBuy = buyScientists - owned` = (49-10-1 floor) - 10 = 28)\n   This same assertion IS the positive control: the spy DID see a Scientist `buyJob` on this path (`sci.length > 0`\n   passed), so the instrument is not blind to the Watch scientist branch \u2014 it sees the call and re",
  "fix": "Mechanism-only; touches NO game-balance number (scientistRatio2 = 10 and the 0.1/0.9 population fractions stay exactly as-is). Make the Watch branch use the delta the way its non-Watch twin (jobs.ts:167) already does \u2014 the delta IS the buy amount, and the gate must compare it to zero, not to `owned`",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 70,
  "sev": "MEDIUM",
  "file": "src/modules/mapfunctions-amp.ts",
  "line": 624,
  "klass": "gate",
  "numeric": false,
  "title": "An unaffordable Prestige Raid silently flips the RAutoMaps master switch to Off, killing ALL U2 map automation for the rest of the run",
  "instrument": "A vitest unit repro (jsdom env) that imports the real `src/modules/mapfunctions-amp.ts` and calls the real exported `RAMP()` with a controlled global seam: praiding on (`Rshoulddopraid = true`, `RAMPraidfrag` at its default 0 so `RAMPfragcheck` stays true), `Rgetequips() > 0` so all five `RAMPshouldrunmap()` gates pass, and `updateMapCost(true) = 1e9` vs `game.resources.fragments.owned = 0` (the \"",
  "evidence": "Test 1 FAILS against current code \u2014 the master switch is zeroed:\n\n  FAIL tests/_repro/f70.test.ts > leaves RAutoMaps ON after a failed (unaffordable) prestige raid\n  AssertionError: expected +0 to be 1 // Object.is equality\n    - Expected  1\n    + Received  0\n   \u276f tests/_repro/f70.test.ts:68  expect(G.autoTrimpSettings.RAutoMaps.value).toBe(1)\n\n`expect(G.buyMap).not.toHaveBeenCalled()` passed on that run, so the failure branch is exactly the one that executed: nothing affordable \u2192 all five `RAMPmapbought*` false \u2192 mapfunctions-amp.ts:616-625 runs \u2192 `autoTrimpSettings[\"RAutoMaps\"].value = 0;` (line 624).\n\nTest 2 PASSES \u2014 POSITIVE CONTROL: with `updateMapCost(true) = 10` and `fragments.owned =",
  "fix": "Scope the shutdown to the feature that failed, not the master switch. At mapfunctions-amp.ts:624 (RAMP) and :799 (dRAMP), delete `autoTrimpSettings[\"RAutoMaps\"].value = 0;` and instead clear the praid toggle that the user actually asked for: `setPageSetting('RAMPraid', false)` (boolean, settings-def",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 71,
  "sev": "MEDIUM",
  "file": "src/modules/mapfunctions.ts",
  "line": 1829,
  "klass": "gate",
  "numeric": false,
  "title": "RmapRepeat's health-map stop-condition is unreachable (dropped the `- 1` its U1 twin has) \u2014 U2 ignores 'Max MapBonus Health'",
  "instrument": "vitest + jsdom, calling the REAL `RmapRepeat` (src/modules/mapfunctions.ts:1745) directly with a fixture that reproduces the exact caller state from maps.ts:1092-1098 (RshouldDoMaps=true, shouldDoHealthMaps=true, all other farm flags off), spying `repeatClicked` (which toggles game.global.repeatMap, as the native one does). This instrument CAN see line 1829 \u2014 the positive-control test drives that ",
  "evidence": "3 tests, run against unmodified HEAD (d51f7a48):\n\n  \u2713 POSITIVE CONTROL: the harness DOES observe the stop branch when its guard is satisfied\n  \u00d7 BUG: at the U1 stop point (mapBonus === health - 1) U2 does NOT stop repeating\n  \u00d7 BUG (exhaustive): across EVERY mapBonus the caller can pass, the stop branch never fires\n\nPOSITIVE CONTROL (mapBonus=4, RMaxMapBonushealth=4 \u2014 a state the caller can never produce, but it proves the instrument is not blind to line 1829): repeatCalls === 1 and game.global.repeatMap flipped true->false. The branch IS observable.\n\nBUG #1 (mapBonus=3 == health-1, the EXACT and ONLY point at which the U1 twin stops):\n  AssertionError: expected true to be false\n   \u276f expect(",
  "fix": "src/modules/mapfunctions.ts:1829 \u2014 restore the `- 1` that the U1 twin (maps.ts:665) has, so the stop predicate overlaps the caller's precondition at the integer mapBonus === RMaxMapBonushealth - 1:\n\n  -  if (shouldDoHealthMaps && game.global.mapBonus >= getPageSetting('RMaxMapBonushealth')) {\n  +  i",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 72,
  "sev": "MEDIUM",
  "file": "src/modules/mapfunctions.ts",
  "line": 1837,
  "klass": "other",
  "numeric": false,
  "title": "RmapRepeat's exit-frag-map check: `game.global.repeatMap` guard binds to only 1 of 7 disjuncts (&& over ||) \u2014 turns Repeat back ON",
  "instrument": "Three instruments, no sim involved (the sim's L0 net is pinned blind to runMap/selectMap, so it would have been meaningless here; RmapRepeat is directly callable instead).\n\n(1) TypeScript compiler-API AST walk over src/modules/mapfunctions.ts's OWN BYTES (not a retyped copy of the snippet): locate the IfStatement whose start line is 1837, confirm the condition's top-level operator, and enumerate t",
  "evidence": "=== (1) AST of the ACTUAL if-statement at mapfunctions.ts:1837 (ts.createSourceFile over the file bytes) ===\nif at line 1837\ncondition kind: BinaryExpression\nTOP-LEVEL operator: ||\n\n--- 7 top-level || disjuncts ---\n[0] game.global.repeatMap && (Rshoulddoquest == 3 && game.global.mapBonus >= 4)\n     mentions game.global.repeatMap? true\n[1] (Rshoulddopraid && RAMPfragfarming && RAMPfrag(false) == true)\n     mentions game.global.repeatMap? false\n[2] (Rdshoulddopraid && RdAMPfragfarming && RAMPfrag(true) == true)\n     mentions game.global.repeatMap? false\n[3] (Rshouldinsanityfarm && Rinsanityfragfarming && RfragCheck(\"insanity\") == true)\n     mentions game.global.repeatMap? false\n[4] (Rshouldalc",
  "fix": "Pure MECHANISM fix \u2014 one pair of parentheses, no game-balance number is read, written, or changed.\n\nsrc/modules/mapfunctions.ts:1837 \u2014 hoist the existing `game.global.repeatMap` precondition so it governs all seven disjuncts instead of only the first:\n\n    if (game.global.repeatMap && (\n        (Rsh",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 74,
  "sev": "MEDIUM",
  "file": "src/modules/maps.ts",
  "line": 1425,
  "klass": "brace-scope",
  "numeric": false,
  "title": "`if (Rshoulddoquest == 6) selectedMap = \"world\";` is the head of an else-if chain \u2014 the assignment is dead AND it suppresses the map-chamber exit, fre",
  "instrument": "vitest + jsdom, calling the REAL `maps.RautoMap()` with the existing L1b actuator spy-log harness (modelled on `tests/maps.characterization.test.ts`), spying the native mutators (`mapsClicked`/`runMap`/`selectMap`/`buyMap`/\u2026). `questcheck` was bound to the REAL `other.questcheck` (not a canned stub) and driven by a game fixture carrying the real quest description string, so the 6-classification is",
  "evidence": "$ npx vitest run tests/_repro/f74.test.ts\n\n \u276f tests/_repro/f74.test.ts (2 tests | 1 failed) 55ms\n     \u2713 POSITIVE CONTROL: no Quest challenge \u2192 RautoMap exits the chamber via mapsClicked()\n     \u00d7 BUG: Quest \"Don't run a map before Cell 100\" (questcheck()==6) \u2192 NO action at all; stuck in the chamber\n\n FAIL > BUG: Quest \"Don't run a map before Cell 100\" (questcheck()==6) \u2192 NO action at all\n AssertionError: expected [] to include 'mapsClicked'\n \u276f tests/_repro/f74.test.ts:221:21\n     expect(names()).toContain('mapsClicked')\n\nBoth intermediate assertions inside the failing test PASSED before the final one, pinning the causal chain:\n  expect(questcheck()).toBe(6)                    // real other.qu",
  "fix": "Mechanism-only, one keyword, NO numeric/balance change: at src/modules/maps.ts:1428, change `else if (!game.global.preMapsActive && !game.global.mapsActive) {` \u2192 `if (!game.global.preMapsActive && !game.global.mapsActive) {`, i.e. demote the quest line at :1425 from the head of the chain to a standa",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 75,
  "sev": "MEDIUM",
  "file": "src/modules/maps.ts",
  "line": 483,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "U1 `vanillaMapatZone` reads the raw `.setZone` array and ignores every row filter \u2014 a disabled MAZ row (or MAZ preset B) still forces AutoTrimps into ",
  "instrument": "jsdom boot of the REAL Trimps clone (v5.10.1) + the REAL built AutoTrimps bundle (`scripts/sim/boot.mjs`, `withAutoTrimps:true`, save `tests/fixtures/saves/02-mid-u1.txt`), then a direct call to `window.autoMap()` \u2014 the U1 function that contains the finding at maps.ts:481-487. Readout = `window.shouldDoMaps` (the global the MAZ loop writes) compared against the game's own MAZ predicate `window.che",
  "evidence": "Differential run (each row = a fresh boot of game+AT on the same save, only the MAZ config differs):\n\n```text\nscenario                                  game.checkMapAtZoneWorld   AT shouldDoMaps\n----------------------------------------  ------------------------   ---------------\nCONTROL: no MAZ row at this zone          false                      false   <- readout attributable\nPOSITIVE CONTROL: enabled row at zone     false                      true    <- loop executes\nBUG (a): row at zone, on:false (DISABLED) false                      TRUE    <- BUG\nBUG (b): U1Mode='b' (preset B active),    false                      TRUE    <- BUG\n         stale row only in preset A\n```\n\nSame thing as vi",
  "fix": "Pure mechanism, NO game-balance number touched. In src/modules/maps.ts:482-487, read the ACTIVE preset via the game's own accessor and honour the row's `on` flag exactly as the game does (main.js:13413 uses `!== false`, so a legacy row with no `on` key still counts):\n\n    vanillaMapatZone = (game.op",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 76,
  "sev": "MEDIUM",
  "file": "src/modules/MAZ.ts",
  "line": 410,
  "klass": "type-confusion",
  "numeric": false,
  "title": "settingsWindowSave: the setting-key `var`s are clobbered by row data, so Save throws (and silently saves nothing) once a MAZ window holds 30 rows",
  "instrument": "vitest + jsdom, calling the real `settingsWindowSave` from src/modules/MAZ.ts with a synthetic MAZ window DOM (120 `windowZone{x}`/`windowCell{x}`/... inputs, unfilled rows carrying value \"-1\", exactly as MAZLookalike renders them) and a stub `autoTrimpSettings` holding only the 7 `Rtimefarm*` keys. `saveSettings` is vi.mock'd so I can assert whether persistence is ever reached; `byId`/`cancelTool",
  "evidence": "$ npx vitest run tests/_repro/f76.test.ts\n\n \u276f tests/_repro/f76.test.ts (3 tests | 1 failed)\n   \u2713 control: 29 filled rows save fine            (Rtimefarmzone.value.length === 29, saveSettings called 1\u00d7)\n   \u2713 BUG: 30 filled rows -> throws, saves nothing (threw TypeError; Rtimefarmzone.value.length === 0; saveSettings NOT called)\n   \u00d7 what the fix must make true: 30 filled rows save 30 presets\n\nTypeError: Cannot set properties of undefined (setting 'value')\n \u276f Module.settingsWindowSave src/modules/MAZ.ts:506:29\n    506|     autoTrimpSettings[zone].value = [];\n       |                             ^\n\nMechanism confirmed in source: `var zone/cell/setting/level/map/special/gather` are declared INSI",
  "fix": "Pure mechanism, no game-balance numbers touched. Separate the setting-KEY variables from the ROW-VALUE variables inside `settingsWindowSave` (MAZ.ts:311-597):\n\n1. Keep the key-id variables (`zone/cell/setting/level/map/special/gather`) assigned ONCE, hoisted OUT of the loop (the `titleText` dispatch",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 77,
  "sev": "MEDIUM",
  "file": "src/modules/nature.ts",
  "line": 57,
  "klass": "parity",
  "numeric": true,
  "title": "#22's convert-rate parity fix landed on ONE of the two twin branches \u2014 the single 'Convert to X' path still credits 6 tokens where the game gives 8",
  "instrument": "(b) + (c). (c) READ THE GAME: quoted the authoritative convert formula from ../trimps-game/main.js (v5.10.1) and audited every `nature2` reference in config.js/main.js. (b) FAILING TEST: a jsdom vitest differential at tests/_repro/f77.test.ts that runs AT's real `autoNatureTokens()` (dynamic import of src/modules/nature.ts, real `getPageSetting` via an injected `autoTrimpSettings`) against a verba",
  "evidence": "TEST OUTPUT (against current code, unmodified):\n\n  FAIL tests/_repro/f77.test.ts > f77 \u2014 nature convert-rate parity > AT credits the same tokens the game does\n  AssertionError: expected 6 to be 8 // Object.is equality\n    - Expected 8\n    + Received 6\n    at tests/_repro/f77.test.ts:62  expect(atWind).toBe(gameWind)\n\n  Test Files 1 failed (1) | Tests 1 failed | 1 passed (2)\n\nThe 1 PASSED test is a built-in POSITIVE CONTROL: the sibling \"Convert to Both\" branch (nature.ts:44, the one #22 actually fixed) credits 8 and 8 from the same harness. So the instrument does exercise `autoNatureTokens`, does reach the convert branches, and does observe the token credit \u2014 it sees 8 on the fixed branch an",
  "fix": "\u26a0\ufe0f NUMERIC / GAME-BALANCE \u2014 flagged numeric=true. I am NOT proposing or applying this change; it is user-gated (tuning is sacrosanct). Stating it only so the divergence is unambiguous:\n\nThe mechanism defect is that #22's parity fix was applied to only ONE of two twin branches. The mirror edit at src",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 78,
  "sev": "MEDIUM",
  "file": "src/modules/nature.ts",
  "line": 106,
  "klass": "gate",
  "numeric": false,
  "title": "autoEnlight picks exactly one nature and never checks the game's level-50 Enlightenment gate \u2014 an ineligible top pick permanently starves the eligible",
  "instrument": "Failing vitest at tests/_repro/f78.test.ts driving the REAL game clone booted in jsdom via scripts/sim/boot.mjs (`bootGame({withAutoTrimps:true})`) \u2014 no stubs. This instrument can see the whole path: the real `game.empowerments.*.getLevel()`, the real `naturePurchase()` (main.js:8535+, including the level-50 gate at 8571-8572), the real AT `autoEnlight()`/`purchaseEnlight()` from src/modules/natur",
  "evidence": "POSITIVE CONTROL (same function, same harness) PASSED \u2014 with Poison at level 55 (eligible) and top-scoring, one `autoEnlight()` call produced `game.global.uberNature === 'Poison'`. So the instrument demonstrably observes a successful enlightenment through this exact code path.\n\nBUG CASE FAILED:\n  FAIL tests/_repro/f78.test.ts > ineligible top pick starves the eligible runner-up\n  AssertionError: expected '' to be 'Wind' // Object.is equality\n  - Expected: Wind\n  + Received: (empty string)\n   \u276f tests/_repro/f78.test.ts:56:36  expect(game.global.uberNature).toBe('Wind')\n\nSetup (all real-game values, no invented numbers): uberNature='' (config.js:282 default), nextUberCost=0 for all three (conf",
  "fix": "Mechanism-only; touches NO game-balance number. The `50` is not a tuning knob \u2014 it is the game's own gate, mirrored exactly from ../trimps-game/main.js:8572 (`spendingEmpowerment.getLevel() < 50`), per the \"mirror game constants exactly\" convention.\n\n1. Make eligibility part of the candidate predica",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 79,
  "sev": "MEDIUM",
  "file": "src/modules/nature.ts",
  "line": 30,
  "klass": "other",
  "numeric": false,
  "title": "The 'Transfer' branch ignores the Token Threshold reserve it is documented to respect \u2014 it spends the cost out of the reserved floor",
  "instrument": "A vitest unit repro (jsdom env) calling the real exported `autoNatureTokens()` from src/modules/nature.ts against an injected `game` + `autoTrimpSettings` seam, with `getNextNatureCost` stubbed to the GAME'S OWN formula transcribed verbatim from ../trimps-game/main.js:8602 (`scale = forRetain?2:4; level = forRetain?retainLevel+1:level; return scale + (level-1)*scale`). This instrument can see exac",
  "evidence": "npx vitest run tests/_repro/f79.test.ts\n\n \u276f tests/_repro/f79.test.ts (3 tests | 1 failed)\n   \u2713 CONTROL: Empowerment branch respects the reserve (does not spend below thresh)\n   \u2713 CONTROL: Convert branch respects the reserve\n   \u00d7 BUG: Transfer branch spends straight through the reserve\n\n FAIL > #79 Token Threshold reserve > BUG: Transfer branch spends straight through the reserve\n AssertionError: expected 420 to be greater than or equal to 500\n \u276f tests/_repro/f79.test.ts:70:22\n      68|     expect(g.retainLevel).toBe(50) // it bought\n      69|     // Documented invariant (tooltip): \"If Tokens would go below this value it will not convert tokens.\"\n      70|     expect(g.tokens).toBeGreaterThan",
  "fix": "Mechanism-only, one line, NO game-balance number touched (`thresh` is a user setting, not a tuning constant). src/modules/nature.ts:32:\n\n    -        if (empowerment.tokens < cost) continue;\n    +        if (empowerment.tokens < cost + thresh) continue;\n\nThis makes Transfer match the invariant every",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 80,
  "sev": "MEDIUM",
  "file": "src/modules/other-praiding.ts",
  "line": 1440,
  "klass": "other",
  "numeric": false,
  "title": "BWraiding dereferences findLastBionic() with no guard \u2014 the documented \"you don't have a BW to raid\" path throws a TypeError and aborts the mainLoop t",
  "instrument": "Direct vitest unit repro (jsdom) that imports `BWraiding` from src/modules/other-praiding.ts and calls it against a hand-built `game` fixture + a REAL `autoTrimpSettings` (real `getPageSetting` from utils.ts; only `debug` stubbed). This instrument executes the actual exported function body, so it can observe: thrown exceptions, the `failbwraid`/`bwraided`/`bwraidon` globals, and the native actuato",
  "evidence": "FAILING RUN (`npx vitest run tests/_repro/f80.test.ts`) \u2014 2 failed | 2 passed (4):\n\n  \u00d7 must not throw \u2014 the \"you do not have a BW to raid\" path should bail cleanly\n    AssertionError: expected [Function] to not throw an error but\n    'TypeError: Cannot read properties of undefined (reading 'level')' was thrown\n\n  \u00d7 AutoMaps==2 (\"Auto Maps: Unique\"): failbwraid is NEVER latched, so it throws EVERY tick\n    AssertionError: expected [Function] to not throw an error but\n    'TypeError: Cannot read properties of undefined (reading 'level')' was thrown\n\n  \u2713 sanity: findLastBionic() returns undefined when no Bionic map is owned\n  \u2713 POSITIVE CONTROL: WITH a Bionic map owned, BWraiding() runs cleanl",
  "fix": "Pure mechanism \u2014 NO game-balance number is touched (no damage/cost/rate/zone literal changes; targetBW keeps coming from the user's BWraidingmax exactly as today).\n\nIn `BWraiding()` (src/modules/other-praiding.ts:1389):\n\n1. Hoist the lookup once, so both derefs share one nullable binding:\n     const",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 81,
  "sev": "MEDIUM",
  "file": "src/modules/other-praiding.ts",
  "line": 1680,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "dailyPraiding writes the U1 Praiding globals instead of its own daily twins \u2014 drepMap1..drepMap5 are never reset, and stale ids later reach recycleMap",
  "instrument": "(1) Source read of `src/modules/other-praiding.ts` (the U1 `Praiding()` reset block at :1163-1183 vs the daily `dailyPraiding()` reset block at :1666-1686) plus a `grep` of every write site of `drepMap1..5` / `repMap1..5`; (2) the game source at `../trimps-game/main.js` (`getMapIndex` :8187, `recycleMap` :10693); (3) a vitest jsdom repro (`tests/_repro/f81.test.ts`) that calls the REAL exported `d",
  "evidence": "STATIC (grep, src/modules/other-praiding.ts): `drepMap1` is written at exactly two places \u2014 :1588 (`drepMap1 = dpMap1` when map 1 is launched) and :1636-1638 (the success block: `recycleMap(getMapIndex(drepMap1)); drepMap1 = undefined;`). It is NEVER written in the reset block. The reset block (:1666-1686), which is the direct copy of U1's (:1163-1183), instead clears `pMap1` (:1675) and `repMap1..repMap5` (:1680-1684) \u2014 all five are `Praiding()`'s U1 globals (declared :972+, used :1085/:1133-1135/:1177). Same copy-paste miss twice more: :1624 the completion latch tests `pMap1 == undefined` (never checks `dpMap1`), and :1658 the daily success block sets `pMap1 = undefined` instead of `dpMap1",
  "fix": "Pure mechanism fix \u2014 no numbers touched, nothing balance-related. In `src/modules/other-praiding.ts`, `dailyPraiding()`, restore the missing `d` prefixes so the daily twin operates on its own state:\n\n- :1675 `pMap1 = undefined;`  \u2192 `dpMap1 = undefined;`   (reset block)\n- :1680-1684 `repMap1..repMap5",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 82,
  "sev": "MEDIUM",
  "file": "src/modules/other.ts",
  "line": 121,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "autoGoldenUpgradesAT(): choosing 'Void' for C2 AutoGoldenUpgrades reaches no fallback branch \u2014 after the 8 Void goldens are capped, no golden upgrade ",
  "instrument": "A vitest unit repro (tests/_repro/f82.test.ts, jsdom env) that imports the real src/modules/other.ts and calls `autoGoldenUpgradesAT()` against an injected `game` + `autoTrimpSettings` + a stub `buyGoldenUpgrade` whose return semantics are copied verbatim from the game's own function (../trimps-game/main.js:14310-14336: returns `undefined` for `what==\"Helium\"` while `runningChallengeSquared`, `und",
  "evidence": "Run of `npx vitest run tests/_repro/f82.test.ts` \u2014 2 passed (the positive controls), 1 failed (the bug):\n\n FAIL  f82 > BUG: plain \"Void\" buys NOTHING once Void caps (should fall back to Battle)\n AssertionError: expected [] to deeply equal [ 'Battle' ]\n - Expected: [ \"Battle\" ]\n + Received: []\n\nMechanism, confirmed by reading both sides:\n- Dispatch (legacy/AutoTrimps2.js:281): `if (cagu && cagu != 'Off' && game.global.runningChallengeSquared) autoGoldenUpgradesAT(cagu);` \u2014 so in a C2 the arg is the C2 setting's label, e.g. \"Void\".\n- other.ts:115-117 -> setting2 = \"Void\"; `buyGoldenUpgrade(\"Void\")` returns `undefined` once capped (../trimps-game/main.js:14315: `if (what == \"Void\" && (parseFloa",
  "fix": "Mechanism-only, no numbers touched. In `autoGoldenUpgradesAT` (src/modules/other.ts:118-126) the fallback must recognize the C2 \"Void\" selection. Add a C2 disjunct to the ->Battle clause at line 123:\n    ... || (autoTrimpSettings.cAutoGoldenUpgrades.selected == \"Void\" && game.global.runningChallenge",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 83,
  "sev": "MEDIUM",
  "file": "src/modules/other.ts",
  "line": 382,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "smithylogic() falls off the end and returns undefined when the smithy-close resource isn't the item's resource \u2014 Smithy Savings then blocks the purcha",
  "instrument": "Two instruments. (1) `node -e` on the raw comparisons that gate the ladder (pure semantics: `null > 100` \u2192 false, `null <= 100` \u2192 true). (2) A direct vitest unit call of the REAL `smithylogic` from src/modules/other.ts (tests/_repro/f83.test.ts, jsdom env), driving the real `getPageSetting` via the real `autoTrimpSettings` store and stubbing only the game-side seam (`getPsString`, `getBuildingItem",
  "evidence": "POSITIVE CONTROL (same function, covered branch) \u2014 PASSES, so the instrument demonstrably sees the ladder and can distinguish allow from block:\n  income={wood:1,metal:500,gems:1} \u2192 smithyclosemetal=true only\n  smithylogic('Dagger','metal',true) with Dagger cost 100 <= gate 250 \u2192 returns `true`  (branch 375)\n  smithylogic('Dagger','metal',true) with Dagger cost 400 >  gate 250 \u2192 returns `false` (branch 366)\n\nTHE BUG \u2014 3 failing cases, all returning `undefined`:\n  \u2717 BUG: metal equip + only WOOD close  \u2192 AssertionError: expected undefined not to be undefined\n  \u2717 BUG: wood building (Hut) + only METAL close \u2192 AssertionError: expected undefined not to be undefined\n  \u2717 BUG: gems building (Collector",
  "fix": "Mechanism-only; touches NO game-balance number, NO threshold, NO percentage. The ladder at src/modules/other.ts:358-382 is a set of explicit deny/allow rules whose intended default is \"allow\" (`var go = true` at :292, and every earlier guard returns that same `go`). The fall-through simply forgets t",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 84,
  "sev": "MEDIUM",
  "file": "src/modules/other.ts",
  "line": 123,
  "klass": "gate",
  "numeric": false,
  "title": "autoGoldenUpgradesAT(): the Void->Battle zone switch ignores the daily/C2 context guard used everywhere else in the same function, so a filler-only 'V",
  "instrument": "vitest unit repro (jsdom) importing the real src/modules/other.ts and calling autoGoldenUpgradesAT() directly, with the REAL getPageSetting (from utils.ts, driven off an autoTrimpSettings fixture) and spy stubs for the two native golden-upgrade calls (getAvailableGoldenUpgrades -> 1; buyGoldenUpgrade -> records its arg, returns false for 'Void' so the Void-exhausted fallback branch executes). This",
  "evidence": "Both leak directions FAIL against current code (src/modules/other.ts:123):\n\n  FAIL > DAILY at z500 with dvoidheliumbattle = -1 must fall back to Helium, not Battle\n  AssertionError: expected [ 'Void', 'Battle' ] to deeply equal [ 'Void', 'Helium' ]\n\n  FAIL > FILLER at z500 with voidheliumbattle = -1 but dvoidheliumbattle = 500 must not buy Battle\n  AssertionError: expected [ 'Void', 'Battle' ] to deeply equal [ 'Void', 'Helium' ]\n\n  Test Files  1 failed (1) | Tests  2 failed | 1 passed (3)\n\nTrace of run 1 (a Daily; caller AutoTrimps2.js:280 passes the daily selector): buyGoldenUpgrade('Void') fails -> line 121's *guarded* group correctly sets setting2='Helium' (dAutoGoldenUpgrades=='Void' &&",
  "fix": "Mechanism-only; no numeric/balance literal is touched. src/modules/other.ts:123 \u2014 give the \"Void\" zone-switch group the same context guard its neighbour already has:\n\n  ((autoTrimpSettings.AutoGoldenUpgrades.selected == \"Void\" && !game.global.dailyChallenge.seed && !game.global.runningChallengeSquar",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 85,
  "sev": "MEDIUM",
  "file": "src/modules/perks.ts",
  "line": 21,
  "klass": "supply-chain",
  "numeric": false,
  "title": "AutoPerks executes a third-party remote script (Zorn192.github.io) on every page load and hard-depends on it; a byte-equivalent local copy exists but ",
  "instrument": "Static + runtime evidence, no sim needed (this is a dependency-provenance finding, not a semantics one):\n(1) grep over src/, scripts/, dist/ for every definition and every use of `FastPriorityQueue`;\n(2) `curl` of the remote URL + byte-diff against the local copy;\n(3) `node -e` evaluation of the exact construct expression with the global absent (proves the failure mode);\n(4) an attempted jsdom imp",
  "evidence": "1. THE ONLY SOURCE OF THE SYMBOL IS THE THIRD-PARTY ORIGIN.\n   src/modules/perks.ts:18-22 (top-level, unconditional, runs on every page load):\n     queuescript.src = 'https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js';\n     head.appendChild(queuescript);\n   and again at perks.ts:754-758 (the U2 block; declares Rhead/Rqueuescript but appends head/queuescript \u2014 a pre-existing legacy bug already noted in the module header).\n   Shipped bundle still points remote \u2014 4 hits:\n     $ grep -c \"Zorn192\" dist/autotrimps.user.js  ->  4\n   And the bundle NEVER defines the symbol:\n     $ grep -n \"FastPriorityQueue.prototype\\|function FastPriorityQueue\" dist/autotrimps.user.js\n     (no matches)\n\n2. ",
  "fix": "Self-host it \u2014 no game-balance number is touched anywhere in this fix.\n1. Add `'FastPriorityQueue.js'` to the MANIFEST in scripts/build-userscript.mjs:11 (the file is already at legacy/FastPriorityQueue.js and is byte-identical to the remote), placed BEFORE the src IIFE so the symbol exists when per",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 86,
  "sev": "MEDIUM",
  "file": "src/modules/query.ts",
  "line": 18,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "getPerSecBeforeManual reads raw `.level` perks instead of getPerkLevel(), so its U2 estimate is driven by the player's *U1* perk allocation",
  "instrument": "Live differential in the real clone: `bootGame({ withAutoTrimps: true })` (scripts/sim/boot.mjs) loads the ACTUAL Trimps v5.10.1 sources (config/objects/main.js) + the built `dist/autotrimps.user.js` into one jsdom scope, so AT's `getPerSecBeforeManual` and the game's own production fn `simpleSeconds()` / `getPerkLevel()` are the real implementations. This instrument can see pure-read query predic",
  "evidence": "npx vitest run tests/_repro/f86.test.ts \u2192 2 failed | 1 passed\n\nCase A (U2, U1-helium perks levelled, zero radon spent: Scientist.owned=100, Motivation.level=100/radLevel=0, Motivation_II.level=100):\n  stdout: U2 game truth: 50   AT estimate: 600\n  AssertionError: expected 600 to be close to 50 (12\u00d7 inflation)\n  Sanity asserts passed: window.getPerkLevel('Motivation') === 0 and getPerkLevel('Motivation_II') === 0 in U2.\n\nCase B (U2, the REAL U2 investment: Motivation.level=0, Motivation.radLevel=50):\n  stdout: radLevel=50 \u2192 game truth: 175   AT estimate: 50\n  AssertionError: expected 50 to be close to 175 \u2014 AT ignores radLevel entirely (returns the bare owned\u00b7modifier base).\n\nCase C \u2014 POSITIV",
  "fix": "Mechanism-only (no game-balance NUMBER is touched \u2014 every constant/modifier stays as-is; the fix only changes WHICH perk field is read, to match the game's own accessor).\n\nIn src/modules/query.ts `getPerSecBeforeManual` (lines 18-23), replace the raw `.level` reads with the game's ambient `getPerkLe",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 87,
  "sev": "MEDIUM",
  "file": "src/modules/scryer.ts",
  "line": 124,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Scryer's Overkill check is stance-blind: oneShotPower() accepts a stance argument and never uses it",
  "instrument": "Three instruments, all in agreement.\n\n(1) **vitest + the newGame-free unit harness** (`tests/_repro/f87.test.ts`, jsdom env, modelled on `tests/stance.characterization.test.ts`). `stance.oneShotPower` reads NO game methods \u2014 it consumes only ambient free identifiers (`calcOurDmg`, `addPoison`, `getCurrentEnemy`, `calcSpecificEnemyHealth`) plus plain-data `game.portal.Overkill.level` / `game.talent",
  "evidence": "All three assertions FAILED against current code \u2014 i.e. all three reproduce.\n\n```text\n FAIL  tests/_repro/f87.test.ts (3 tests | 3 failed)\n   \u00d7 A. the specificStance argument is DEAD: every stance yields the same answer\n   \u00d7 B. calcOurDmg never receives the requested stance\n   \u00d7 C. CONSEQUENCE: AT claims a one-shot in S for an enemy S cannot actually one-shot\n```\n\n**A \u2014 the parameter is provably dead.** Fixture: enemy health 900, stance-neutral damage 1000 (min == max, so `maxOrMin` cannot account for any difference). Called `oneShotPower(s, 0, true)` for s \u2208 {\"S\",\"D\",\"X\",undefined}:\n\n```text\nAssertionError: expected [ 1, 1, 1, 1 ] to deeply equal [ 1, +0, 1, +0 ]\n```\n\nEvery stance returns *",
  "fix": "**Mechanism fix \u2014 apply the requested stance's multiplier to the damage `oneShotPower` reasons about.** No AT tuning number is touched.\n\n`src/modules/stance.ts:107-110` currently:\n```ts\nexport function oneShotPower(specificStance?: unknown, offset: number = 0, maxOrMin?: boolean): number {\n    const",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 88,
  "sev": "MEDIUM",
  "file": "src/modules/scryer.ts",
  "line": 29,
  "klass": "type-confusion",
  "numeric": false,
  "title": "'Die To Use S' (ScryerDieZ) decimal zone.cell never fires in its own zone, and gates every later zone on the cell",
  "instrument": "(a) Direct expression evaluation in `node` of the verbatim `readyToSwitch()` die-computation (src/modules/scryer.ts:28-37), fed the REAL production inputs traced from source rather than assumed: `settings-defs.ts:724` declares `createSetting('ScryerDieZ', ..., 'value', 230.60, ...)` and `utils.ts:69` returns `parseFloat(autoTrimpSettings[setting].value)` for type `'value'` \u2014 so the value reaching ",
  "evidence": "$ node -e \"<verbatim die logic>\"\nparseFloat(230.60) = 230.6 | toString = 230.6\nouter guard 230 >= 230.6 : false\nworld 230 lastClearedCell 0  -> die = false\nworld 230 lastClearedCell 58 -> die = false\nworld 230 lastClearedCell 97 -> die = false   <-- zone 230, cell 98: setting says \"die from 230.60\", AT never dies\nworld 231 lastClearedCell 0  -> die = false\nworld 231 lastClearedCell 58 -> die = false\nworld 231 lastClearedCell 97 -> die = true\nworld 300 lastClearedCell 4  -> die = false   <-- z300 cell 5: cell filter STILL applied 70 zones later\nworld 300 lastClearedCell 70 -> die = true\nworld 500 lastClearedCell 10 -> die = false   <-- and forever\n\nTwo independent defects, both confirmed:\n1. ",
  "fix": "MECHANISM-ONLY (touches no game-balance number; the 230.60 default stays exactly as shipped).\n\nParse the setting once into (zone, cell) and gate on the INTEGER zone, applying the cell floor only inside the starting zone:\n\n  const raw = getPageSetting('ScryerDieZ');                    // e.g. 230.6\n ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 89,
  "sev": "MEDIUM",
  "file": "src/modules/scryer.ts",
  "line": 69,
  "klass": "gate",
  "numeric": false,
  "title": "'Boss: NEVER (Above VoidLevel)' compares the wrong way and is inert at the default VoidMaps=0",
  "instrument": "Two instruments, both of which CAN see this region.\n(1) `node -e` evaluation of the exact expression semantics (pure comparison \u2014 nothing to hide).\n(2) An executable vitest repro (jsdom) that calls the REAL `useScryerStance()` from src/modules/scryer.ts with a stubbed `autoTrimpSettings` (matching what `getPageSetting` at utils.ts:57 actually reads) and a `setFormation` spy. This instrument observ",
  "evidence": "\u2500\u2500 (1) EXPRESSION SEMANTICS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n`VoidMaps` is createSetting'd as type 'value' with default '0' (settings-defs.ts:398), and getPageSetting returns `parseFloat(value)` for 'value' (utils.ts:68-69) \u2192 0.\n\n$ node -e \"...\":\n  VoidMaps default (parseFloat of the createSetting default) = 0\n  world 61  -> (world < VoidMaps) = false\n  world 100 -> (world < VoidMaps) = false\n  world 200 -> (world < VoidMaps) = false\n  world 550 -> (world < VoidMaps) = false\n  world 600 -> (world < VoidMaps) = false\n  --- with VoidMaps = 550 (a user who DOES set it) ---\n  boss at z400 (BELOW void level): guard fires (never scry)?  true\n  boss at z560 (ABOVE void level): guard",
  "fix": "MECHANISM-ONLY. This edits a comparison OPERATOR and adds an activity guard \u2014 it touches NO game-balance number, no damage/cost/rate/multiplier literal. The `> 0` below is a \"is this threshold configured at all\" check (the same idiom the file already uses for `ScryerMaxZone` at scryer.ts:62: `getPag",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 90,
  "sev": "MEDIUM",
  "file": "src/modules/scryer.ts",
  "line": 77,
  "klass": "type-confusion",
  "numeric": false,
  "title": "'World Min & Max Only' is a boolean but is compared to 2 \u2014 two of its three code paths are permanently dead",
  "instrument": "TWO instruments, both agreeing.\n\n(1) A vitest repro at tests/_repro/f90.test.ts (jsdom) that calls the REAL `useScryerStance()` and `scryingCorruption()` from src/modules/scryer.ts against the REAL `getPageSetting()` from src/modules/utils.ts, driven by a hand-authored `globalThis.autoTrimpSettings` whose entries have the exact shapes `createSetting()` produces (settings-engine.ts:61-69). This ins",
  "evidence": "TYPE CHAIN (the code says so):\n  settings-defs.ts:708   createSetting('onlyminmaxworld', 'World Min & Max Only', 'Forces Scryer to only work in world regardless of other settings. ', 'boolean', false, null, 'Scryer');\n  settings-engine.ts:68  (type=='boolean') -> autoTrimpSettings[id] = { ..., enabled: loaded === undefined ? (defaultValue || false) : loaded }\n  settings-engine.ts:234 settingChanged: btn.enabled = !btn.enabled            // only ever a boolean\n  utils.ts:61-62         if (type == 'boolean') return autoTrimpSettings[setting].enabled;   // -> true | false, NEVER 2\n\nnode -e evaluation of the three read sites:\n  onlyminmaxworld = true  | :19  v>=2 -> false | :77  v==2 -> false | ",
  "fix": "NO numeric / game-balance literal is touched by any of this \u2014 it is a pure type-confusion mechanism fix. (It does change automation behavior, but only for users who have opted into `onlyminmaxworld`, which defaults to `false`.)\n\nThe two sites are NOT the same fix, and one of them needs a maintainer ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 91,
  "sev": "MEDIUM",
  "file": "src/modules/settings-boot.ts",
  "line": 25,
  "klass": "supply-chain",
  "numeric": false,
  "title": "The fork's own tabs.css NEVER loads \u2014 basepath always resolves to an unrelated upstream fork's GitHub Pages, and the repo's tabs.css is not even in th",
  "instrument": "Four instruments, none of which needed the sim (this is a build/asset-resolution bug, not a game-logic one, so the L0 proof net's four-mutator blindness is irrelevant here):\n(1) STATIC GREP over the whole repo + the game clone for the element id the basepath override keys on;\n(2) `npm run build` + grep of the SHIPPED `dist/autotrimps.user.js` \u2014 this can see exactly what users execute;\n(3) LIVE NET",
  "evidence": "1) THE OVERRIDE NEVER FIRES. `legacy/AutoTrimps2.js:2-5` keys on `getElementById('AutoTrimps-script')`. Repo-wide grep for that id \u2192 only its own two definitions (AutoTrimps2.js:2 and the oracle fixture). `grep -rn \"AutoTrimps-script\" ../trimps-game` \u2192 no matches. Every real loader uses a DIFFERENT id: `scripts/build-userscript.mjs:56` \u2192 `s.id='AutoTrimps-Zek'`; `legacy/mods.js:2` \u2192 `'AutoTrimps-Zek'`; Tampermonkey injects no <script> element at all.\n\n2) RUNTIME EVALUATION (jsdom, replaying the shipped bundle verbatim):\n    atscript          = null\n    basepath          = https://Zorn192.github.io/AutoTrimps/\n    injected <link>   = <link rel=\"stylesheet\" type=\"text/css\" href=\"https://Zorn19",
  "fix": "Purely mechanical \u2014 no game-balance number is touched anywhere in this fix (the only literal involved is a CSS hex colour that already exists in the repo's own tabs.css; nothing in `src/` gameplay math changes).\n\nRECOMMENDED (architecturally correct, kills the whole class of bug): INLINE the assets ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 92,
  "sev": "MEDIUM",
  "file": "src/modules/settings-defs.ts",
  "line": 260,
  "klass": "type-confusion",
  "numeric": false,
  "title": "AutoGigas default is the string 'true' \u2014 the feature runs (truthiness read) but its two configuration settings are permanently hidden (`== true` read)",
  "instrument": "vitest + jsdom, driving the REAL code paths end-to-end (no re-implemented copies): the real `createSetting` from settings-engine.ts (called with settings-defs.ts:260-262's verbatim arguments, including the quoted `'true'`), the real `getPageSetting` from utils.ts, the real `settingChanged` click handler, and the real every-tick `updateCustomButtons()` from settings-visibility.ts (the 970-line gate",
  "evidence": "6 tests, exactly 1 failed \u2014 the bug test. All controls passed.\n\nFAIL  #92 > BUG: yet its two knobs are HIDDEN by updateCustomButtons (== true read)\nAssertionError: expected false to be true\n  135|     expect(shown('CustomTargetZone')).toBe(true)   // false -> display:none\n  136|     expect(shown('CustomDeltaFactor')).toBe(true)\n Test Files  1 failed (1)\n      Tests  1 failed | 5 passed (6)\n\nPASSED (the controls that make the failure meaningful):\n1. POSITIVE CONTROL \u2014 \"the gate function executes and can both show and hide\": after a real\n   updateCustomButtons() pass, AutoGigas itself is VISIBLE; then setting hidebuildings=true +\n   BuyBuildingsNew=0 (making `fuckbuilding` true, settings-visib",
  "fix": "Two parts. NEITHER touches a game-balance number \u2014 the automation's behavior is unchanged (AutoGigas is already effectively ON via the truthy read; this only fixes the TYPE and therefore the visibility of its knobs).\n\n(1) LOAD-BEARING \u2014 repairs BOTH fresh AND already-persisted installs. settings-vis",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 94,
  "sev": "MEDIUM",
  "file": "src/modules/settings-defs.ts",
  "line": 286,
  "klass": "type-confusion",
  "numeric": false,
  "title": "Rsmithylogic default is the string 'false' \u2014 the `== false` early-return guard in RsmithySavings is dead, so Smithy Saving can activate while its mast",
  "instrument": "Two instruments. (1) `node -e` for the pure coercion semantics \u2014 this can only see JS `==` behavior, nothing about reachability. (2) A jsdom vitest repro (tests/_repro/f94.test.ts) driving the REAL code path end-to-end: the real `createSetting` (settings-engine.ts) with the VERBATIM factory defaults copied from settings-defs.ts:286-289 \u2192 the real `getPageSetting` (utils.ts) \u2192 the real `smithylogic",
  "evidence": "1) COERCION (node): `'false' == false` \u2192 **false**. `typeof ('false' || false)` \u2192 **string**. So createSetting's `enabled: loaded === undefined ? (defaultValue || false) : loaded` (settings-engine.ts:69) stores the truthy STRING \"false\", and getPageSetting returns it verbatim (utils.ts:62). other.ts:296's clause `getPageSetting('Rsmithylogic') == false` therefore NEVER short-circuits.\n\n2) END-TO-END (vitest, 7 tests, exactly 1 red \u2014 the bug):\n   \u00d7 THE BUG: smithylogic withholds the Shield buy with the master never clicked\n     AssertionError: expected false to be true\n     \u276f tests/_repro/f94.test.ts:60  expect(other.smithylogic('Shield','wood',true)).toBe(true)\n   Test Files 1 failed (1) | T",
  "fix": "Mechanism fix only \u2014 NO game-balance number is touched. `'false'` here is a quoted BOOLEAN, not a tuning value, so this is outside the sacrosanct-tuning rule. Two parts, and part (2) is required for correctness on existing installs:\n\n(1) Author-side: in src/modules/settings-defs.ts, pass real boolea",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 95,
  "sev": "MEDIUM",
  "file": "src/modules/settings-engine.ts",
  "line": 240,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "The magmite auto-spend safety debounce is keyed on a setting id that no longer exists (`AutoMagmiteSpender2`), so it never fires",
  "instrument": "vitest (jsdom env), calling the REAL `settingChanged()` from `src/modules/settings-engine.ts` \u2014 no mocks of the function under test. This instrument directly executes settings-engine.ts:239-245 (the multitoggle branch) and can observe the `magmiteSpenderChanged` global write; the passing positive control below proves the region is reachable and the write is visible. This is a pure click-handler / ",
  "evidence": "POSITIVE CONTROL PASSED, BUG ASSERTIONS FAILED (`npx vitest run tests/_repro/f95.test.ts` \u2192 \"Tests 2 failed | 1 passed (3)\"):\n\n  \u2713 POSITIVE CONTROL: clicking a control literally named AutoMagmiteSpender2 DOES arm the debounce\n      -> magmiteSpenderChanged === true.  The instrument CAN see settings-engine.ts:241.\n\n  \u00d7 cycling the REAL spendmagmite control into \"Always\" leaves the debounce disarmed\n      AssertionError: expected false to be true\n       \u276f tests/_repro/f95.test.ts:53  expect(globalThis.magmiteSpenderChanged).toBe(true)\n       - true\n       + false\n\n  \u00d7 => AutoTrimps2.js:200 guard passes immediately: autoMagmiteSpender() fires on the next tick\n      AssertionError: expected true",
  "fix": "One-token rename in src/modules/settings-engine.ts:240 \u2014 pure mechanism, NO game-balance number touched:\n\n    -   if (id == 'AutoMagmiteSpender2' && btn.value == 1) {\n    +   if (id == 'spendmagmite' && btn.value == 1) {\n\n`btn.value == 1` is checked PRE-increment, so it means \"the user is cycling IN",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 96,
  "sev": "MEDIUM",
  "file": "src/modules/settings-engine.ts",
  "line": 323,
  "klass": "other",
  "numeric": false,
  "title": "parseNum Math.floor()s the scientific-notation path, so any fractional e-notation input (which the tooltip explicitly advertises) is silently stored a",
  "instrument": "Two instruments, both of which CAN see this code (it is a pure string\u2192number function plus a jsdom DOM applier \u2014 no sim, no timers, no layout involved):\n(1) `node` evaluating the verbatim function body \u2014 sufficient for the semantics.\n(2) A vitest jsdom repro (tests/_repro/f96.test.ts) importing the REAL `parseNum` + `autoSetValue` from src/modules/settings-engine.ts, wiring a real `#customNumberBo",
  "evidence": "Both repro tests FAIL against current code \u2014 that failure IS the evidence.\n\n$ npx vitest run tests/_repro/f96.test.ts\n \u276f tests/_repro/f96.test.ts (2 tests | 2 failed)\n   \u00d7 floors fractional e-notation to 0 (pure)\n   \u00d7 stores amalcoordhd as 0 through the real autoSetValue DOM path, killing the > 0 gate\n\nFAIL > floors fractional e-notation to 0 (pure)\nAssertionError: expected +0 to be 0.0000025 // Object.is equality\n- Expected   0.0000025\n+ Received   0\n \u276f tests/_repro/f96.test.ts:20  expect(parseNum('2.5e-6')).toBe(0.0000025)\n\nFAIL > stores amalcoordhd as 0 through the real autoSetValue DOM path\nAssertionError: expected +0 to be 0.0000025 // Object.is equality\n- Expected   0.0000025\n+ Receive",
  "fix": "Mechanism fix, NOT a balance change \u2014 no game number is touched. The Math.floor is a parse defect, and the entire hand-rolled e-branch is redundant: native `parseFloat` already parses e-notation exactly (`parseFloat('2.5e-6') === 0.0000025`, `parseFloat('2e5') === 200000`), and does so WITHOUT the m",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 97,
  "sev": "MEDIUM",
  "file": "src/modules/settings-menu.ts",
  "line": 254,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "toggleAutoMaps() silently destroys the \"Unique\" / \"No Unique\" AutoMaps mode (3-state multitoggle treated as a boolean)",
  "instrument": "Vitest unit repro (jsdom env) importing the REAL `toggleAutoMaps` from src/modules/settings-menu.ts driven against the REAL `getPageSetting`/`setPageSetting` from src/modules/utils.ts, with a `multitoggle`-shaped `autoTrimpSettings` entry (the exact shape settings-engine's `createSetting(..., 'multitoggle', 1, ...)` produces) and the DOM nodes `setPageSetting` touches (`#AutoMaps`, `#RAutoMaps`, `",
  "evidence": "Test run (both cases fail exactly as predicted; the intermediate `toBe(0)` assertions PASS, so the pause half works and only the resume half is broken):\n\n  FAIL tests/_repro/f97.test.ts > U1: pause+resume from \"Auto Maps: Unique\" (2) restores 2\n  AssertionError: expected 1 to be 2   (line: expect(getPageSetting('AutoMaps')).toBe(2))\n  FAIL tests/_repro/f97.test.ts > U2: pause+resume from \"Auto Maps No Unique\" (2) restores 2\n  AssertionError: expected 1 to be 2   (line: expect(getPageSetting('RAutoMaps')).toBe(2))\n  Test Files 1 failed (1) \u00b7 Tests 2 failed (2)\n\nMechanism, src/modules/settings-menu.ts:254-272:\n    if (getPageSetting('AutoMaps')) { setPageSetting('AutoMaps', 0); }\n    else { se",
  "fix": "Mechanism-only; touches no game-balance number. Remember the pre-pause mode instead of hardcoding the resume value. Minimal shape in src/modules/settings-menu.ts:\n\n  const key = game.global.universe == 2 ? 'RAutoMaps' : 'AutoMaps';\n  const cur = getPageSetting(key);\n  if (cur > 0) {\n      lastAutoMa",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 98,
  "sev": "MEDIUM",
  "file": "src/modules/other-praiding.ts",
  "line": 989,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Praiding / BWraiding automap handshake is a dispatch hole for AutoMaps option 2",
  "instrument": "Direct vitest unit repro (jsdom) that imports the REAL `src/modules/other-praiding.ts` and calls the REAL exported `Praiding()` with a seeded `autoTrimpSettings` + `makeMinimalGame()` fixture and spied native mutators (mapsClicked/buyMap/selectMap/runMap/recycleMap/repeatClicked). What it CAN see: the exact `getPageSetting('AutoMaps')` branch at other-praiding.ts:989 and the resulting write to `au",
  "evidence": "npx vitest run tests/_repro/f98.test.ts\n\n \u2713 TRIPWIRE: fixture shape ok\n \u2713 POSITIVE CONTROL: AutoMaps == 1 -> Praiding suspends automapping (value -> 0)      [PASSES: value===0, prestraidon===true]\n \u00d7 BUG: AutoMaps == 2 (\"Auto Maps: Unique\") -> Praiding does NOT suspend automapping\n     AssertionError: expected 2 to be +0 // Object.is equality\n \u00d7 CONSEQUENCE: mainLoop gate (`AutoMaps > 0`) still runs autoMap during the raid\n     AssertionError: expected true to be false   (2 > 0 === true)\n\n Tests  2 failed | 2 passed (4)\n\nThe positive control is the load-bearing part: with AutoMaps=1 the SAME fixture reaches the raid branch (prestraidon flips true) and the suspend fires (value 1->0). With Aut",
  "fix": "Mechanism-only; touches NO numeric balance value.\n\nThe module already declares an unused `globalThis.praidSetting = undefined` at other-praiding.ts:8 \u2014 the save/restore slot the original author evidently intended. Use it:\n\n1. Suspend (Praiding :989, BWraiding :1416, dailyPraiding :1492): gate on \"au",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 99,
  "sev": "MEDIUM",
  "file": "src/modules/settings-visibility.ts",
  "line": 989,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "Phantom `dVoidMaps` in checkPortalSettings \u2014 the void-maps-vs-autoPortal conflict warning never fires during a Daily",
  "instrument": "Two instruments. (1) Static existence check: `grep -rn \"createSetting('dVoidMaps\" src/` \u2192 no match; `grep -rn \"'Dailyportal'\" src/modules/settings-defs.ts` \u2192 no match. Both ids are phantom; `getPageSetting` (utils.ts:57-60) returns literal `false` for any id absent from `autoTrimpSettings`. (2) A jsdom vitest unit repro (tests/_repro/f99.test.ts, since deleted) that dynamically imports the REAL ut",
  "evidence": "vitest run tests/_repro/f99.test.ts \u2192 4 tests, 2 passed, 2 failed.\n\nPASS (semantics proof): getPageSetting('dVoidMaps') === false, and `false >= 191` === false, `false >= 551` === false. The guard at settings-visibility.ts:991 is therefore dead for every real portal zone (portalLevel == -1 already returned at :982).\n\nPASS (POSITIVE CONTROL, non-Daily): AutoPortal='Custom', CustomAutoPortal=550 \u2192 portalLevel 551; VoidMaps=560 \u2192 tooltip called 1\u00d7, arg[5] === 'Void Maps Conflict'. The instrument sees the warning path.\n\nFAIL (THE BUG, Daily): AutoPortal='Corrupted' \u2192 portalLevel 191 (real); challengeActive='Daily'; DailyVoidMod=200 (void maps set 9 zones AFTER the portal \u2192 they can never run).\n ",
  "fix": "Pure id-rename mechanism fixes. NO game-balance number is touched (no thresholds, costs, rates or multipliers change; both edits swap a dead setting id for the live one that already exists).\n\n1. src/modules/settings-visibility.ts:989 \u2014 `voidmaps = getPageSetting('dVoidMaps');` \u2192 `voidmaps = getPageS",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 100,
  "sev": "MEDIUM",
  "file": "src/modules/settings-visibility.ts",
  "line": 835,
  "klass": "never-reset",
  "numeric": false,
  "title": "`hiddenBreedTimer` / `autoMapStatus` are turned OFF but never turned back ON \u2014 re-enabling the Display toggle does nothing",
  "instrument": "A jsdom vitest repro (tests/_repro/f100.test.ts, now deleted) that imports the REAL `updateCustomButtons()` from src/modules/settings-visibility.ts and calls it three times against real DOM nodes for `hiddenBreedTimer` (inside a wrapper div) and `autoMapStatus` (inside a wrapper div), flipping the `showbreedtimer` / `showautomapstatus` settings between calls via a stubbed `getPageSetting`. Every O",
  "evidence": "Test run (current code, no source edits):\n\n \u276f tests/_repro/f100.test.ts (2 tests | 2 failed)\n   \u00d7 turning showbreedtimer OFF then back ON leaves hiddenBreedTimer hidden\n   \u00d7 turning showautomapstatus OFF then back ON leaves autoMapStatus hidden\n\n AssertionError: expected 'none' to be '' // Object.is equality\n \u276f tests/_repro/f100.test.ts:75  expect(el('hiddenBreedTimer').style.display).toBe('')\n AssertionError: expected 'none' to be '' // Object.is equality\n \u276f tests/_repro/f100.test.ts:90  expect(el('autoMapStatus').style.display).toBe('')\n\nPOSITIVE CONTROL (embedded, PASSED \u2014 proves the instrument observes this exact region):\n  - after call #1 with the setting TRUE:  display === ''      (bas",
  "fix": "Mechanism-only; touches NO game-balance number.\n\nIn src/modules/settings-visibility.ts, make the two Memory-section lines symmetric so the every-tick pass restores as well as hides. The catch: the generic `turnOn` sets `parentNode.style.display = 'inline-block'` (toggleElem:24), which is right for s",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 101,
  "sev": "MEDIUM",
  "file": "src/modules/stance.ts",
  "line": 137,
  "klass": "type-confusion",
  "numeric": false,
  "title": "challengeDamage's falsy guard overwrites the deliberate missingHealth=0 that survive()'s new-squad model passes",
  "instrument": "Layer-1 vitest unit repro (jsdom env) against the real `src/modules/stance.ts` \u2014 `tests/_repro/f101.test.ts`, run with `npx vitest run`. It imports and CALLS the actual `challengeDamage` and `survive` exports with stubbed leaf deps (getCurrentEnemy / calcSpecificEnemyAttack / calcOurBlock / addPoison / getPierceAmt / challengeActive) and a `makeMinimalGame` fixture. What this instrument CAN see: t",
  "evidence": "The guard is dead-on-arrival for the value 0 (`node -e \"console.log(!0)\"` -> `true`), and both predicted failures reproduce with the exact predicted magnitudes:\n\nFAIL  f101 \u00b7 challengeDamage \u2014 explicit missingHealth=0 is silently overwritten\nAssertionError: expected 60 to be 200 // Object.is equality\n- Expected  200        (intended: bleed = maxHealth(1000) * 0.20, caller passed missingHealth = 0)\n+ Received   60        (actual:   bleed = (1000 - liveMissing 700) * 0.20)\n \u276f expect(challengeDamage(1000, 100, 200, 0, 10, 0, 2)).toBe(200)\n\nFAIL  f101 \u00b7 survive \u2014 the discarded 0 flips the new-squad verdict\nAssertionError: expected true to be false // Object.is equality\n- Expected false        (I",
  "fix": "Mechanism-only, no game-balance number is touched (the 0.20 / 0.30 bleed multipliers and every other literal stay exactly as they are).\n\nsrc/modules/stance.ts:137 \u2014 make the \"not supplied\" test distinguish an omitted argument from a deliberate zero:\n\n    -    if (!missingHealth) missingHealth = game",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 102,
  "sev": "MEDIUM",
  "file": "src/modules/stance.ts",
  "line": 107,
  "klass": "never-written",
  "numeric": false,
  "title": "oneShotPower/oneShotZone ignore their specificStance parameter, making scryer's four per-stance overkill guards identical",
  "instrument": "Two instruments, both in-process (no sim \u2014 the L0 net pins setFormation as UNCOVERED, so it is structurally blind to scryer/stance and its silence would have meant nothing).\n(1) STATIC: `grep -n \"specificStance\" src/modules/*.ts` returns exactly TWO lines in the whole repo \u2014 stance.ts:86 (oneShotZone signature) and stance.ts:107 (oneShotPower signature). Zero references in either body. This is a t",
  "evidence": "RUNTIME (the repro, run against unmodified src/):\n\n  FAIL > returns DIFFERENT overkill power for D vs H (D=4x dmg, H=0.5x dmg)\n  AssertionError: expected +0 not to be +0 // Object.is equality\n   \u276f tests/_repro/f102.test.ts:53:24\n       51|     expect(dmgCalls).toEqual([['max', false, true], ['max', false, tru\u2026   <-- PASSED\n       53|     expect(dPower).not.toBe(hPower)   // FAILS: both are 0\n\n  FAIL > scryer's four transition guards are byte-identical predicates\n  AssertionError: expected 1 to be greater than 1\n   \u276f tests/_repro/f102.test.ts:61:32\n       60|     const vals = ['D', 'X', 'B', 'H'].map((s) => oneShotPower(s, 0, true))\n       61|     expect(new Set(vals).size).toBeGreaterThan(1)",
  "fix": "Mechanism fix, and it does NOT edit a game-balance number \u2014 it reuses the stance multipliers that are ALREADY in the same file, in survive() (stance.ts:243-247). No new literal is invented; the constants are lifted, not tuned.\n\n1. Extract survive()'s existing formation table into one helper, so ther",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 103,
  "sev": "LOW",
  "file": "src/modules/time.ts",
  "line": 6,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "time.ts's formatMinutesForDescriptions CLOBBERS the game's identically-named global, corrupting every Challenge\u00b2 description + the Bone Shrine tooltip",
  "instrument": "scripts/sim/boot.mjs (the real sim harness), driven from a vitest file. It evals the REAL game files (config.js/main.js/updates.js/objects.js/...) into a jsdom global, then evals the REAL built dist/autotrimps.user.js into that SAME global. WHAT IT CAN SEE: global-identifier shadowing directly (it holds the actual `window`), and it can invoke the game's own closures at their real call sites. This ",
  "evidence": "DIFFERENTIAL (same harness, same game files, only `withAutoTrimps` flipped):\n\nWITHOUT AT (control, PASSES):\n identity: \"GAME\"\n raw125:  \"2:05\"\n raw5:    \"5 mins\"\n raw180:  \"3 hours\"\n desc:    \"Clear The Block in less than 8 hours from start of run\"\n prog:    \"Best run is 5 mins\"\n\nWITH AT (FAILS -> the bug):\n identity: \"AT\"\n raw125:  \"2:05:00\"\n raw5:    \"5 minutes 30 seconds\"\n raw180:  \"3 hours 0 minute 0 second\"\n desc:    \"Clear The Block in less than 8 hours 0 minute 0 second from start of run\"\n prog:    \"Best run is 5 minutes 30 seconds\"\n\n`desc`/`prog` are the GAME's own closures (config.js:7354/7362), invoked exactly as the game's UI invokes them. Render path CONFIRMED live: updates.js:67",
  "fix": "Non-numeric, zero game-balance numbers touched, and zero behavior change for AT (it has no call sites).\n\nPRIMARY: delete `formatMinutesForDescriptions` from src/modules/time.ts entirely (and the dangling pointer comment at utils.ts:167). Verified zero call sites in src/ and legacy/, so nothing in AT",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 104,
  "sev": "MEDIUM",
  "file": "src/modules/upgrades.ts",
  "line": 105,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "firstGiga's Daily map-stack gate is a phantom setting \u2014 `dMaxMapBonushealth` was never createSetting'd, so the gate is always open on Dailies",
  "instrument": "Two instruments, both in-repo and both able to see the region.\n\n(a) `node -e \"console.log(0 >= false)\"` \u2192 the pure-semantics half (a `value`-typed getPageSetting miss returns the boolean `false`, and `mapBonus >= false` is `mapBonus >= 0`).\n\n(b) A vitest jsdom repro (tests/_repro/f104.test.ts, since deleted) that calls the REAL `upgrades.firstGiga()` against the REAL settings catalog. Critically, ",
  "evidence": "TEST 1 \u2014 the phantom, proven against the real catalog (PASSES):\n  expect(s['MaxMapBonushealth']).toBeDefined()    // \u2713 default '10' (settings-defs.ts:391)\n  expect(s['RMaxMapBonushealth']).toBeDefined()   // \u2713 (settings-defs.ts:432)\n  expect(s['dMaxMapBonushealth']).toBeUndefined() // \u2713 PASSES \u2014 never createSetting'd\n\nTEST 2 \u2014 the behavioral divergence (FAILS on current code):\n\n  FAIL  #104 > gate D holds firstGiga back at mapBonus 0 OUTSIDE a Daily, but is WIDE OPEN on a Daily\n  AssertionError: expected true to be false // Object.is equality\n  - Expected\n  + Received\n  - false\n  + true\n   \u276f tests/_repro/f104.test.ts:106:17\n      104|     // THE ASSERTION THAT FAILS ON CURRENT CODE: a Daily ",
  "fix": "MECHANISM-ONLY \u2014 introduces NO new game-balance number, and changes none. (The only literal in play, `10`, is an existing shipped default that I am not touching.)\n\nTwo candidate fixes; I recommend A.\n\nA. (recommended, minimal) Delete the Daily ternary at upgrades.ts:105 and read the one real setting",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 105,
  "sev": "MEDIUM",
  "file": "src/modules/buildings.ts",
  "line": 184,
  "klass": "never-written",
  "numeric": false,
  "title": "`MODULES[\"upgrades\"].autoGigas` is read but never written \u2014 Warpstation Cap silently does nothing until the first Gigastation is owned",
  "instrument": "Two instruments. (1) `node -e \"console.log(undefined == false)\"` \u2192 `false` (pure semantics of the guard's left disjunct). (2) A vitest jsdom characterization repro (`tests/_repro/f105.test.ts`, modeled on tests/buildings.characterization.test.ts) that calls the REAL `buildings.buyGemEfficientHousing()` with the REAL `getPageSetting` reading a seeded `autoTrimpSettings`, and spy-logs the native `bu",
  "evidence": "SEMANTICS:\n  $ node -e \"console.log('undefined == false ->', undefined == false)\"\n  undefined == false -> false\n\nSTATIC (grep -rni \"autoGigas\" src legacy):\n  src/modules/upgrades.ts:170:  if (getPageSetting(\"AutoGigas\") && ...)        <- the REAL setting\n  src/modules/buildings.ts:26:  // - MODULES[\"upgrades\"].autoGigas == false \u2014 cross-module falsy-catch;\n  src/modules/buildings.ts:184: const firstGigaOK = MODULES[\"upgrades\"].autoGigas == false || game.upgrades.Gigastation.done > 0;\n  (no assignment anywhere; upgrades.ts:23-25 sets only targetFuelZone + customMetalRatio)\n\nBEHAVIOR (vitest, 5 tests: 1 failed / 4 passed):\n  FAIL  f105 > BEHAVIOR: with the production MODULES shape, Warpstation",
  "fix": "Mechanism-only, no numbers touched. In src/modules/buildings.ts:184 replace the dead read of the never-written `MODULES[\"upgrades\"].autoGigas` with the real setting that upgrades.ts:170 already uses:\n\n    -  const firstGigaOK = MODULES[\"upgrades\"].autoGigas == false || game.upgrades.Gigastation.done",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 106,
  "sev": "MEDIUM",
  "file": "src/modules/upgrades.ts",
  "line": 118,
  "klass": "other",
  "numeric": false,
  "title": "firstGiga computes the giga delta from the STALE FirstGigastation setting instead of the warp count it is about to pin (autoGiga's `customBase` param ",
  "instrument": "vitest (jsdom env) characterization harness \u2014 `tests/harness/gameFixture.ts::makeMinimalGame`, modelled on the existing `tests/upgrades.characterization.test.ts` L1a/L1b blocks for these very functions. WHAT IT CAN SEE: `firstGiga`/`autoGiga` are pure-data readers + a `setPageSetting` actuator \u2014 they call ZERO game methods (they read only `game.buildings.Warpstation.owned`, `game.resources.trimps.",
  "evidence": "RUN 1 \u2014 `npx vitest run tests/_repro/f106.test.ts` \u2192 \"Tests 2 failed | 1 passed (3)\".\n\nDEMO A (fresh profile: FirstGigastation at its createSetting default of 20 per src/modules/settings-defs.ts:258; player owns 6 warpstations; world 90):\n  AssertionError: expected -0.17 to be NaN\n    62|     expect(savedDelta).toBe(deltaForStaleBase)   // PASSED  -> savedDelta === autoGiga(customBase=20) === -0.17\n    63|     expect(savedDelta).toBe(deltaForPinnedBase)  // FAILED  -> autoGiga(customBase=6) === NaN\n  i.e. firstGiga pinned FirstGigastation=6 (asserted, passed) but saved the delta that autoGiga solved for base=20.\n\nDEMO B (self-consistency / fixed-point): running firstGiga(true) TWICE with not",
  "fix": "MECHANISM FIX \u2014 pass the already-computed `base` through the parameter that exists for it. src/modules/upgrades.ts:118:\n\n    -    const delta = autoGiga(deltaZ, deltaM, deltaS);\n    +    const delta = autoGiga(deltaZ, deltaM, deltaS, base);\n\n`base` is already in scope one line above (upgrades.ts:114",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 107,
  "sev": "MEDIUM",
  "file": "legacy/AutoTrimps2.js",
  "line": 204,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "Hdshrine/Rdshrine option 2 (\"DAS: Normal\") dispatches nothing \u2014 autoshrine ignores the index, reads the empty daily zone list, and the UI hides the ed",
  "instrument": "Direct unit repro of `autoshrine()` (src/modules/other.ts:484) under vitest+jsdom, driving the real exported function with an injected `game` + `autoTrimpSettings`, and spying on `game.permaBoneBonuses.boosts.consume`. What this instrument CAN see: every branch of autoshrine \u2014 the core-setting gate (:533), the `shrinezone.includes(world)` gate (:535), the cell/amount gate (:543), and the `consume(",
  "evidence": "`npx vitest run tests/_repro/f107.test.ts` \u2192 1 failed | 2 passed.\n\nFAIL > Hdshrine==2 (\"DAS: Normal\") in a Daily \u2192 should consume from the Maps-tab config\n  AssertionError: expected +0 to be 1\n  - Expected: 1\n  + Received: 0\n      at tests/_repro/f107.test.ts:69  expect(consumeCalls).toBe(1)\n\nPASS (positive control) > Hdshrine==1 with the DAILY lists filled \u2192 consumes a charge   [consume() called once]\nPASS (control)          > outside a Daily with Hshrine ON, the same Maps-tab config consumes [consume() called once]\n\nScenario: U1, Daily active, world 40, lastClearedCell 8, charges 5. Maps-tab config Hshrinezone=[40]/Hshrinecell=[10]/Hshrineamount=[3]. Hdshrine set to index 2 (\"DAS: Normal\",",
  "fix": "Non-numeric, mechanism-only \u2014 touches no game-balance value. In `autoshrine()` (src/modules/other.ts:486), stop inferring the config block from the challenge alone; only use the Daily block when the daily core setting is actually at index 1 (\"Daily AutoShrine On\"):\n\n    const dailyCore = game.global",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 108,
  "sev": "MEDIUM",
  "file": "legacy/AutoTrimps2.js",
  "line": 188,
  "klass": "dispatch-hole",
  "numeric": false,
  "title": "'AM Always' (automapsalways) force-resets AutoMaps to 1 every tick, silently destroying option 2 (\"Auto Maps: Unique\" / U2 \"Auto Maps No Unique\")",
  "instrument": "A vitest repro (tests/_repro/f108.test.ts, jsdom env) that reads the VERBATIM mainLoop source lines out of legacy/AutoTrimps2.js (SRC[187] = line 188, SRC[301] = line 302), executes each as one 100ms mainLoop tick via `new Function('getPageSetting','autoTrimpSettings', line)`, and feeds it the REAL `getPageSetting` imported from src/modules/utils.ts plus an `autoTrimpSettings` record shaped exactl",
  "evidence": "$ npx vitest run tests/_repro/f108.test.ts\n Test Files  1 failed (1)\n      Tests  3 failed | 2 passed (5)\n\nFAIL > U1: user picks \"Auto Maps: Unique\" (2) with AM Always on -> survives one tick\nAssertionError: expected 1 to be 2\n     55|     tick(U1_LINE) // one 100ms mainLoop tick\n     57|     expect(getPageSetting('AutoMaps')).toBe(2)\n- Expected: 2\n+ Received: 1\n\nFAIL > U1 downstream: the AMU unique-map gates all go false after the clobber\nAssertionError: expected false to be true\n     64|     expect(getPageSetting('AutoMaps') == 2 && getPageSetting('AMUblock') == true).toBe(true)   // PASSES before the tick\n     65|     tick(U1_LINE)\n     66|     expect(getPageSetting('AutoMaps') == 2 && ge",
  "fix": "Purely a mechanism/predicate fix \u2014 NO game-balance number is touched (the only literals involved are multitoggle option INDICES, not tuning values).\n\nChange the guard from \"not exactly 1\" to \"is OFF\", matching the correct sibling already in the same file at :390 / :397:\n\n  legacy/AutoTrimps2.js:188\n",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 110,
  "sev": "LOW",
  "file": "src/modules/settings-visibility.ts",
  "line": 11,
  "klass": "never-written",
  "numeric": false,
  "title": "`MODULES.graphs._lastTheme` is read but NEVER written \u2014 the every-tick graph theme-sync is permanently dead code",
  "instrument": "Two instruments. (1) Static: `grep -rn \"_lastTheme\" src/ legacy/ scripts/ tests/` \u2014 every hit is in settings-visibility.ts:11/12/16 (plus the two committed build-goldens, which are just copies of the same emitted lines). Graphs.js never touches `_lastTheme`; it keeps its own bare `var lastTheme = -1` (Graphs.js:1064). (2) Runtime: a vitest (jsdom) test importing the REAL exported `updateCustomButt",
  "evidence": "Runtime (vitest run tests/_repro/f110.test.ts):\n\n  \u276f tests/_repro/f110.test.ts (2 tests | 1 failed)\n     \u00d7 calls MODULES.graphs.themeChanged() when the user changes darkTheme (CURRENT CODE \u2014 FAILS)\n  AssertionError: expected \"vi.fn()\" to be called at least once\n   \u276f tests/_repro/f110.test.ts:37:43\n  Tests  1 failed | 1 passed (2)\n\nThe failing test ticks updateCustomButtons() three times across a darkTheme 0 \u2192 2 flip with `MODULES.graphs` shaped exactly as Graphs.js:95-96/257 builds it ({useDarkAlways, themeChanged}) \u2014 `themeChanged` is NEVER called. The second test (positive control) passes: seeding `MODULES.graphs._lastTheme = -1` before the same ticks makes themeChanged fire and `_lastThem",
  "fix": "Mechanism-only, no numeric balance touched. Seed the sentinel so the guard can arm: initialize `MODULES.graphs._lastTheme = -1` where Graphs.js already builds the namespace (legacy/Graphs.js:95-96, next to `MODULES.graphs.useDarkAlways = false`). One line, and it is the same sentinel value Graphs.js",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 111,
  "sev": "MEDIUM",
  "file": "legacy/Graphs.js",
  "line": 310,
  "klass": "other",
  "numeric": false,
  "title": "\"Black Graphs\" checkbox saves the INVERSE of its own state and is clobbered on every page load",
  "instrument": "A vitest/jsdom repro (`tests/_repro/f111.test.ts`, since deleted) that executes the REAL `legacy/Graphs.js` source verbatim at global scope via indirect eval \u2014 exactly how `scripts/build-userscript.mjs` concatenates it into the userscript \u2014 over a minimal DOM (`#settingsTable`, `#settingsRow`) plus stubs for the engine globals the file reads at load (`game.options.menu.darkTheme.enabled`, `MODULES",
  "evidence": "3 of 3 bug-assertions FAILED (1 positive control passed):\n\nFAIL A: load must NOT clobber a persisted darkTheme=false (user turned Black Graphs OFF)\n  bootGraphs(themeEnabled=1 /* game default theme */, persisted={darkTheme:false})\n  expect(readStored().darkTheme).toBe(false)\n    AssertionError: expected true to be false     // - false  + true\n  \u2192 the user's saved \"off\" is DESTROYED on every page load and re-persisted as true.\n\nFAIL B: checkbox state and dark CSS must agree after load (default theme)\n  bootGraphs(1, {darkTheme:true})\n  expect(boxChecked()).toBe(darkCssLoaded())\n    AssertionError: expected true to be false     // box is ticked, dark-graph.css is NOT loaded\n  \u2192 the box lies: i",
  "fix": "Pure mechanism, no game-balance numbers involved (nothing here touches tuning).\n\n1. Stop saving the inverse \u2014 `legacy/Graphs.js:310`:\n     saveSetting(\"darkTheme\", !dark)   \u2192   saveSetting(\"darkTheme\", dark)\n   so the persisted value matches the read-back at :278.\n\n2. Stop the load-time self-clobber",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 112,
  "sev": "LOW",
  "file": "legacy/Graphs.js",
  "line": 242,
  "klass": "never-written",
  "numeric": false,
  "title": "\"Live Updates\" checkbox is never initialized from the saved setting \u2014 it shows OFF while live updating is ON",
  "instrument": "A jsdom vitest repro (tests/_repro/f112.test.ts) that indirect-evals the REAL legacy/Graphs.js \u2014 its own loadGraphData() / createUI() / saveSetting() / safeLocalStorage(), nothing re-implemented \u2014 after pre-seeding localStorage with {\"live\":true}, i.e. exactly the state left behind by a user who ticked \"Live Updates\" and reloaded. It can observe: the actual DOM node #liveCheckbox created by create",
  "evidence": "$ npx vitest run tests/_repro/f112.test.ts\n\n \u276f tests/_repro/f112.test.ts (3 tests | 2 failed) 61ms\n   \u00d7 renders the checkbox UNTICKED even though the persisted setting says live=true\n   \u00d7 consequence: the first click is a no-op \u2014 it takes TWO clicks to turn Live Updates off\n   \u2713 POSITIVE CONTROL: the same assertions pass once a read-back line is added\n\nFAIL > renders the checkbox UNTICKED even though the persisted setting says live=true\nAssertionError: expected false to be true\n   97|     // THE BUG: state says live-updating is ON; the checkbox says OFF.\n   99|     expect(box.checked).toBe(true)   // <-- got false\n\nFAIL > consequence: the first click is a no-op \u2014 it takes TWO clicks to turn ",
  "fix": "One line, mechanism-only \u2014 no game-balance number is touched (this is a UI-state read-back, nothing numeric).\n\nIn createUI(), alongside the existing sibling read-backs at legacy/Graphs.js:278-279, add:\n\n    document.querySelector(\"#liveCheckbox\").checked = !!GRAPHSETTINGS.live;\n\nThis is precisely th",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 113,
  "sev": "MEDIUM",
  "file": "scripts/build-userscript.mjs",
  "line": 166,
  "klass": "harness-blind",
  "numeric": false,
  "title": "The fork's own tabs.css/mi.png are never deployed and can never be reached \u2014 the shipped build loads the UPSTREAM author's stale tabs.css, so #39's CS",
  "instrument": "Three instruments, all of which CAN see this region (it is build/deploy plumbing, not runtime game state \u2014 the sim's 4-mutator L0 net is irrelevant here):\n1. `npm run build` + grep over the real emitted `dist/autotrimps.user.js` (sees exactly what a user installs).\n2. `node -e` evaluation of the actual basepath expression from legacy/AutoTrimps2.js:1-5 (pure semantics).\n3. Live `curl` against both",
  "evidence": "Every link of the chain confirmed:\n\n(1) THE RULE ONLY LIVES IN tabs.css. src/modules/settings-engine.ts:171 (infoclick branch) now sets only `btn.setAttribute(\"style\", \"font-size: 1.1vw;\")` \u2014 the orange is in tabs.css:4:\n    .settingKind-info { background-color: #d88839 !important; color: black; } /* was inline in settings-engine */\n\n(2) THE SHIPPED BUNDLE CONTAINS NEITHER THE RULE NOR ANY <style> INJECTION.\n    $ npm run build\n    $ grep -c 'settingKind-info {' dist/autotrimps.user.js   -> 0\n    $ grep -c 'd88839'             dist/autotrimps.user.js   -> 0\n    $ grep -o 'basepath + \"tabs.css\"' dist/autotrimps.user.js -> basepath + \"tabs.css\"   (settings-boot.ts:25)\n\n(3) basepath IS ALWAYS T",
  "fix": "Mechanism-only; no game-balance number is touched (the only literal involved is a CSS color that already exists in the repo's tabs.css and was previously inline in settings-engine.ts \u2014 restoring reachability changes no tuning value).\n\nRecommended (kills the whole class, works under Tampermonkey wher",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 114,
  "sev": "MEDIUM",
  "file": "scripts/build-userscript.mjs",
  "line": 11,
  "klass": "supply-chain",
  "numeric": false,
  "title": "deLoaderize() misses a remote script injection: the 'self-contained' userscript still fetches and executes FastPriorityQueue.js from a third-party Git",
  "instrument": "Three instruments, all static-but-executed (no sim/jsdom needed \u2014 this is a build-artifact defect, fully observable at build time):\n(1) `npm run build` \u2192 grep the real shipped artifact `dist/autotrimps.user.js`. This CAN see exactly what every user executes; it is the ground truth for \"does the self-contained script fetch remote code\".\n(2) A failing vitest at tests/_repro/f114.test.ts calling the ",
  "evidence": "CONFIRMED. Four independent observations.\n\n1) The remote injection survives into the shipped artifact. `npm run build` \u2192 `[build] dist/autotrimps.user.js (1077489 bytes)`, then grep:\n    dist/autotrimps.user.js:17214  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\n    dist/autotrimps.user.js:17884  queuescript.src = \"https://Zorn192.github.io/AutoTrimps/FastPriorityQueue.js\";\nSources: src/modules/perks.ts:21 (U1) and :757 (U2), both top-level = they run at bundle-eval on every page load. This directly contradicts deLoaderize()'s own stated contract at scripts/build-userscript.mjs:95-96: \"modules are already bundled, so its remote script-injection must not run.",
  "fix": "Pure mechanism \u2014 touches NO game-balance number, and is provably behavior-identical because the vendored file is byte-identical to the fetched one.\n\n1. `scripts/build-userscript.mjs` MANIFEST (:11-27): add `'FastPriorityQueue.js'`. Position is not load-order-critical \u2014 perks.ts only *constructs* FPQ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 115,
  "sev": "MEDIUM",
  "file": "src/modules/maps.ts",
  "line": 37,
  "klass": "never-written",
  "numeric": false,
  "title": "`scryerStuck` is never assigned true \u2014 the entire Scryer perma-stuck auto-recovery is dead code",
  "instrument": "Two instruments, both able to see the region.\n\n(1) EXHAUSTIVE STATIC \u2014 a TypeScript compiler-API scope-aware AST walk over every file in `src/` (all modules + entry), collecting every AssignmentExpression / unary ++/-- whose target resolves to the name `scryerStuck` (identifier OR `globalThis.scryerStuck` property form). This can see every syntactic write in the shipped source, including ones a na",
  "evidence": "(1) STATIC \u2014 TS AST sweep of all of src/:\n\n  ASSIGNMENTS to scryerStuck:\n    src/modules/maps.ts:37   globalThis.scryerStuck = false\n  OCCURRENCES:\n    src/modules/maps.ts:37   (the init \u2014 the ONLY write)\n    src/modules/maps.ts:410  shouldDoMaps = (!enoughDamage || shouldFarm || scryerStuck);\n    src/modules/maps.ts:691  (scryerStuck)) &&\n    src/modules/maps.ts:698  if (scryerStuck) { debug(\"Got perma-stuck on cell \" + ... ) }\n\n  One write (false), three reads. No dynamic write path: the only `eval` in src/ is\n  import-export.ts:914 (user-pasted MODULE-vars box), no `globalThis[...]`/`window[...]` computed\n  writes anywhere. Zero hits for /scryerstuck/i in ../trimps-game \u2014 it is not a nati",
  "fix": "MECHANISM-ONLY, but it needs a design decision, so flag it rather than auto-applying.\n\nThe reads are correct and live; what is missing is the writer. Two viable fixes:\n\n  A. (recommended) Implement the detection \u2014 set `scryerStuck = true` when AT is in Scryer stance and\n     provably cannot clear th",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 116,
  "sev": "HIGH",
  "file": "src/modules/heirlooms.ts",
  "line": 586,
  "klass": "twin-asymmetry",
  "numeric": false,
  "title": "`Rdshouldtributefarm` is read but never written \u2014 the U2 DAILY heirloom map-staff AND tribute-staff swaps are both dead (its non-daily twin works)",
  "instrument": "Vitest + jsdom, importing the REAL src/modules/heirlooms.ts and calling the real Rdheirloomswap() / Rheirloomswap() with spy-logged native actuators (selectHeirloom/equipHeirloom on globalThis). This instrument directly executes the two suspect lines (heirlooms.ts:586, :589), so it is NOT blind to this region \u2014 proven by two positive controls on the non-daily twin (below), which the same spies obs",
  "evidence": "1) ZERO WRITERS *AND* ZERO DECLARATIONS. `grep -rn \"Rdshouldtributefarm\" src legacy scripts` (excluding the .d.ts) returns exactly TWO lines \u2014 both READS, heirlooms.ts:586 and :589. `grep -n \"Rdshouldtributefarm *=\" tests/fixtures/src-bundle.golden.js` \u2192 NONE. The only mention of the name in the whole repo besides those two reads is `var Rdshouldtributefarm: any` at src/game/at-legacy.d.ts:209 \u2014 a TYPE-ONLY ambient declaration with no runtime existence. That .d.ts line is exactly what hid this from tsc. Its twin `Rshouldtributefarm` is initialised (maps.ts:14, mapfunctions.ts:51), reset (mapfunctions.ts:156, maps.ts:1072) and set true (mapfunctions.ts:599).\n\n2) THEREFORE IT THROWS \u2014 IT DOES ",
  "fix": "Pure mechanism \u2014 NO game-balance number is touched (nothing here is a tuning literal).\n\nPRIMARY FIX (2 chars deleted, twice): the tribute-farm engine has NO daily variant \u2014 mapfunctions.ts:599 writes `Rshouldtributefarm` regardless of whether a Daily is active (unlike time-farm/praid, which genuinel",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 117,
  "sev": "LOW",
  "file": "legacy/AutoTrimps2.js",
  "line": 110,
  "klass": "never-written",
  "numeric": false,
  "title": "`breedFire` is initialised false in 2016 and never assigned true \u2014 the breeding-emergency worker-firing path is dead",
  "instrument": "Two instruments, both able to see the region.\n(1) STATIC \u2014 grep over `src/ legacy/ tests/ scripts/` + the SHIPPED artifact `dist/autotrimps.user.js` (rebuilt with `npm run build`) for any write to `breedFire` (`= `, `++`, `--`, `+=`), plus a grep over the game clone `../trimps-game` to rule out a native global. This instrument sees every assignment form in every line of code that actually ships.\n(",
  "evidence": "A. STATIC \u2014 the ONLY write to `breedFire` in the entire shipped userscript:\n\n  $ grep -n \"breedFire\" dist/autotrimps.user.js\n  109:  var breedFire = false;            <-- the one and only assignment\n  4300: ... && !breedFire) {              (guard, benign)\n  4312: ... && !breedFire) {              (guard, benign)\n  4340: if (ms != 0 && !game.jobs.Scientist.locked && !breedFire) {\n  4367: if (!game.jobs[job].locked && !breedFire) {\n  4381: if (!ratiobuy(\"Miner\", minerRatio) && breedFire && game.global.turkimpTimer === 0 && challengeActive(\"Metal\") === false)\n  4383: if (!ratiobuy(\"Lumberjack\", lumberjackRatio) && breedFire)\n  5098: ... && !haveWorkers && !breedFire) {\n\n  Write-form sweep acro",
  "fix": "Two candidate fixes; ONE of them is blocked by the sacrosanct-tuning rule and I am NOT proposing it.\n\nA. MECHANISM-ONLY, safe to ship (recommended): RETIRE the dead flag. Delete `var breedFire = false` (legacy/AutoTrimps2.js:110), the ambient decl (src/game/at-legacy.d.ts:203), the two unreachable f",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 118,
  "sev": "MEDIUM",
  "file": "src/modules/equipment.ts",
  "line": 358,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "The windstacking damage correction (`ourDamage *= trimpAA`) never applies \u2014 both call sites gate on the phantom `loomswap` / `dloomswap`",
  "instrument": "vitest (jsdom) repro at tests/_repro/f118.test.ts driving the REAL exported `equipment.autoLevelEquipment()` via the existing tests/equipment.characterization.test.ts harness pattern: real `getPageSetting` + real `autoTrimpSettings` (seeded, never mocked), game-global collaborators stubbed on globalThis, and vi.fn spies on the native mutators `buyUpgrade` / `buyEquipment`.\n\nWHAT IT CAN SEE: the de",
  "evidence": "$ npx vitest run tests/_repro/f118.test.ts\n  Tests  1 failed | 4 passed (5)\n\nPASSED \u2014 static: `createSetting` ids parsed from settings-defs.ts (571+, regex verified non-empty).\n  expect(ids).toContain('highdmg')      OK  (the leaf setting the dead guard reads DOES exist)\n  expect(ids).toContain('dhighdmg')     OK\n  expect(ids).not.toContain('loomswap')  OK  \u2190 never defined\n  expect(ids).not.toContain('dloomswap') OK  \u2190 never defined\n\nPASSED \u2014 semantics (real getPageSetting, utils.ts:57-60 returns false for an unknown id):\n  getPageSetting('loomswap')        === false\n  getPageSetting('loomswap')  > 0   === false\n  getPageSetting('dloomswap') > 0   === false\n  \u21d2 equipment.ts:358 / :360 and ma",
  "fix": "MECHANISM FIX \u2014 no game-balance number is touched. The correction factor `trimpAA` is not edited;\nonly the DEAD GUARD in front of it is replaced with the live one that the same correction already\nuses elsewhere in this codebase.\n\nThe in-tree template is calc.ts:889/:895 (calcHDratio) and calc.ts:933",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 119,
  "sev": "LOW",
  "file": "src/modules/upgrades.ts",
  "line": 105,
  "klass": "phantom-setting",
  "numeric": false,
  "title": "firstGiga()'s map-bonus gate is trivially satisfied on every Daily \u2014 `dMaxMapBonushealth` is a phantom, so `mapBonus >= maxHealthMaps` becomes `mapBon",
  "instrument": "Two instruments. (1) `node -e` evaluation of the bare comparison semantics \u2014 sufficient to prove a phantom guard is dead, since `getPageSetting` returns literal `false` for an id that was never `createSetting`'d (src/modules/utils.ts:57-60, `hasOwnProperty(setting) == false \u2192 return false`). (2) A vitest jsdom characterization repro (`tests/_repro/f119.test.ts`, modeled on the existing `tests/upgr",
  "evidence": "GREP (the phantom is real): `grep -rn \"MaxMapBonushealth\" src/` \u2192 only `createSetting('MaxMapBonushealth', \u2026)` at settings-defs.ts:391 and `createSetting('RMaxMapBonushealth', \u2026)` at 432. There is NO `createSetting('dMaxMapBonushealth', \u2026)` anywhere in src/ or legacy/. Its only appearance in the entire repo is the READ at upgrades.ts:105.\n\nNODE (the comparison is dead):\n  mapBonus 0 >= false  -> true\n  mapBonus 1 >= false  -> true\n  non-Daily: 0 >= 10   -> false;  1 >= 10 -> false\n`game.global.mapBonus` is a non-negative counter, so `mapBonus >= false` \u2261 `mapBonus >= 0` \u2261 ALWAYS TRUE.\n\nVITEST (the path executes and diverges). Identical fixture, mapBonus=0, Warpstation.owned=10, canAffordCoor",
  "fix": "Mechanism-only, NO game-balance number is touched (no literal like `= 5` or a growth rate is edited; the two values involved \u2014 MaxMapBonushealth's default 10 and the `>= 2` map-stack floor \u2014 are LEFT EXACTLY AS THEY ARE).\n\nTwo options, both pure wiring:\n\nA (recommended, mechanism fix, no new UI): ma",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 122,
  "sev": "HIGH",
  "file": "scripts/sim/recorder.mjs",
  "line": 5,
  "klass": "harness-blind",
  "numeric": false,
  "title": "The trace recorder omits AutoTrimps' highest-frequency actuators (setGather, fightManual, buyMap, mapsClicked, doPortal, equipHeirloom) while its docb",
  "instrument": "The sim itself, unmodified, in the EXACT committed-oracle configuration (save 02-mid-u1, seed 1, 1500 ticks, live build via buildUserscript()). Two node scripts (scratchpad only \u2014 no repo file was created or edited):\n\n(1) f122.mjs \u2014 boots via scripts/sim/boot.mjs + installSeededRandom + installFrozenClock, installs the REAL scripts/sim/recorder.mjs, then installs a SUPERSET counter on top that wra",
  "evidence": "A. THE SEAM GAP IS REAL (superset instrument, 02-mid-u1 / seed 1 / 1500 ticks, live build):\n\ntripwire ok: game.buildings.Shed.cost.wood is a function\n\n=== natives AT actually called ===\nbuyJob                   15   RECORDED       e.g. [\"Trainer\",true,true]\nbuyBuilding              53   RECORDED       e.g. [\"House\",true,true]\nbuyUpgrade                0   RECORDED\nrunMap                    0   RECORDED\nselectMap                 0   RECORDED\nbuyEquipment              5   RECORDED       e.g. [\"Shoulderguards\",null,true]\nsetFormation              0   RECORDED\nrecycleMap                0   RECORDED\nsetGather              1500   NOT RECORDED   e.g. [\"trimps\"]     <-- AT's single most-frequent act",
  "fix": "Mechanism-only. NO game-balance number is touched anywhere in this fix (nothing in this area is a tuning literal).\n\n1. scripts/sim/recorder.mjs:5 \u2014 widen MUTATORS to the natives AT actually drives. All are real `window` functions in ../trimps-game/main.js, so they wrap at the identical seam with zer",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 123,
  "sev": "MEDIUM",
  "file": "src/modules/portal.ts",
  "line": 46,
  "klass": "harness-blind",
  "numeric": false,
  "title": "The auto-portal decision flow is structurally unreachable in the sim: every doPortal() lives inside a stubbed setTimeout, and zonePostpone wedges at 1",
  "instrument": "Vitest sim harness (`bootGame({withAutoTrimps:true, saveString:'02-mid-u1'})` from scripts/sim/boot.mjs, jsdom + real dist bundle), run as `npx vitest run tests/_repro/f123.test.ts`.\n\nWHAT IT CAN SEE: I did NOT spy `window.doPortal` \u2014 portal.ts's internal call resolves to the MODULE-scope binding, so a window-level spy would be a false green. Instead I spied the two GAME globals that `doPortal()` ",
  "evidence": "Two tests, one green (positive control) + one red (the bug):\n\n  \u2713 POSITIVE CONTROL: with a working setTimeout the SAME state DOES portal\n  \u00d7 BUG: under the sim boot (setTimeout = () => 0) it never portals and wedges armed\n    AssertionError: expected \"vi.fn()\" to be called at least once\n     \u276f tests/_repro/f123.test.ts:65   expect(portalClicked).toHaveBeenCalled()\n\n  Test Files  1 failed (1)\n       Tests  1 failed | 1 passed (2)\n\nIn the failing run, AT's own debug line proves the decision fired:\n  \"My HeliumHr was: 0 & the Best HeliumHr was: 1000000 at zone: 3\"\ni.e. the synchronous prelude ran (`zonePostpone += 1`, portal.ts:41) \u2014 asserted `window.zonePostpone === 1` \u2014 but `portalClicked` / ",
  "fix": "Mechanism only \u2014 NO game-balance number is touched anywhere in this fix.\n\n1. scripts/sim/boot.mjs:25-27 \u2014 replace the no-op timer stubs with a VIRTUAL TIMER QUEUE instead of `() => 0`:\n   - `window.setTimeout = (fn, ms=0) => { const id=++seq; queue.push({id, due: virtualNow + ms, fn}); return id }`,",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 124,
  "sev": "MEDIUM",
  "file": "legacy/Graphs.js",
  "line": 177,
  "klass": "supply-chain",
  "numeric": false,
  "title": "Graphs.js CDN-injects an unpinned, un-SRI'd highcharts.js at UI-creation time for every user",
  "instrument": "Live Chrome + DevTools network log against the real shipped artifact \u2014 NOT the sim (the sim's L0 net observes only 4 mutators and jsdom would never fetch/execute a remote <script>, so it is structurally blind to this class; a \"could not reproduce\" from it would have been meaningless). Method: `npm run build` (real userscript), `npm run serve` (game clone v5.10.1 on :8080), `chrome-devtools` new_pa",
  "evidence": "1) STATIC \u2014 the inject is unconditional at load, not lazy on the Graphs tab:\n   legacy/Graphs.js:172 `function createUI() {`\n   legacy/Graphs.js:175-178:\n       var chartscript = document.createElement(\"script\");\n       chartscript.type = \"text/javascript\";\n       chartscript.src = \"https://code.highcharts.com/highcharts.js\";\n       head.appendChild(chartscript);\n   legacy/Graphs.js:1062 \u2014 bare top-level call `createUI()` (no guard, no event handler).\n\n2) SHIPPED ARTIFACT \u2014 survives the build:\n   $ grep -n \"code.highcharts.com\" dist/autotrimps.user.js\n     18960:  chartscript.src = \"https://code.highcharts.com/highcharts.js\";\n   $ grep -c \"integrity\" dist/autotrimps.user.js\n     0           ",
  "fix": "Pure mechanism \u2014 NO game-balance numbers involved anywhere in this fix.\n\n1. Vendor Highcharts and delete the inject. `legacy/highcharts.js` is ALREADY in the repo. Add it to `MANIFEST` in scripts/build-userscript.mjs (before 'Graphs.js') and DELETE the 4-line inject at legacy/Graphs.js:175-178. The ",
  "skepticVotes": "2/2 REAL"
 },
 {
  "idx": 127,
  "sev": "MEDIUM",
  "file": "src/modules/heirlooms.ts",
  "line": 446,
  "klass": "parity",
  "numeric": false,
  "title": "heirlooms.ts exports generateHeirloomIcon, clobbering the game's \u2014 the 'Equip On Portal' star badge disappears from every heirloom",
  "instrument": "End-to-end jsdom boot via the sim harness (`scripts/sim/boot.mjs`), driven from a vitest file. It evals the REAL trimps-game v5.10.1 clone (lz-string/decimal/config/updates/playerSpire/objects/main.js) into one shared jsdom scope, then evals the REAL built `dist/autotrimps.user.js` on top \u2014 the same order a browser userscript uses. It therefore observes exactly the binding the game's five render c",
  "evidence": "Two boots of the identical harness, differing ONLY in `withAutoTrimps`.\n\nCONTROL (game only, no AT) \u2014 PASSED all four assertions, so the instrument demonstrably sees the star, the a11y attrs and the empty-slot tooltip when they exist:\n  carried HTML contains `icon-star portalHeirloom`  \u2714\n  carried HTML contains `role=\"button\"`             \u2714\n  empty-slot HTML contains `Empty Slot`             \u2714\n  String(globalThis.generateHeirloomIcon) contains `selectHeirloom(`  \u2714\n\nWITH AutoTrimps \u2014 the global is replaced (AT's monkeypatch marker `newSelectHeirloom(` IS present in the function source, proving the clobber executed), and the render regresses:\n\n  AssertionError: onPortal star badge: expected '<",
  "fix": "Re-sync `src/modules/heirlooms.ts:446-458` with `../trimps-game/main.js:7374-7387`, changing ONLY the onclick (`selectHeirloom(` \u2192 `newSelectHeirloom(`), which is the sole reason AT overrides this function. Concretely, restore the three drifted lines: (1) the empty-slot early return's `onmouseover='",
  "skepticVotes": "2/2 REAL"
 }
]